# obrobka M6 — шари й пряме кадрування: план реалізації

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** дати віджету накладання зображень одне на одне й кадрування
тягненням, не зламавши межу портів, на якій тримаються MCP і пакетний режим.

**Architecture:** дві незалежні зміни в чистому ядрі — `Position` як частка
люфту плюс `zoom` у `FitOptions` (частина A) і операція `composite` з типом
`Layer` (частина B). Інтерфейс отримує два нові компоненти: `Stage.svelte`
(інтерактивне прев'ю, спільне для обох частин) і `LayerPanel.svelte`.
Частина A йде першою, бо створює сцену, на якій B рухає шари.

**Tech Stack:** TypeScript 7, Svelte 5 (руни), Astro 7, Vitest 4,
Playwright 1.62, pnpm 11, Node ≥ 22.12.

## Global Constraints

- Ядро (`packages/core`) не має ані DOM, ані Node API. Жодних `document`,
  `canvas`, `OffscreenCanvas`, `fs`.
- Специфікація: `docs/superpowers/specs/2026-08-23-obrobka-m6-design.md`.
- Коментарі й тексти тестів — українською, як у решті репозиторію.
- **Словники uk та en мусять мати однакові ключі**, і в англійському
  не має бути кирилиці — це перевіряє `apps/web/test/i18n.test.ts`.
- **У Svelte-компонентах не має бути вшитої кирилиці** поза коментарями —
  той самий тест. Кожен видимий рядок іде через `t`.
- `zoom` — не менше за `1`; менше затискається до `1`.
- `Position.fx` і `Position.fy` затискаються в `0..1`.
- Геометрія шару нормалізована відносно **полотна після `fit`**.
- Накладене зображення зберігається зменшеним до 2048 px по більшій стороні.
- Перевірки після кожної задачі: `pnpm typecheck` і `pnpm test`.
- Нові тести ядра йдуть у `packages/core/test/layers.test.ts` — ім'я
  `composite.test.ts` уже зайняте композицією з маскою.

---

# Частина A — пряме кадрування

## Task 1: `Position` як частка люфту

**Files:**
- Modify: `packages/core/src/types.ts` (тип `Position`)
- Modify: `packages/core/src/ops/fit.ts` (функція `place`, рядки 14–33)
- Test: `packages/core/test/fit.test.ts`

**Interfaces:**
- Consumes: нічого
- Produces: `Position` приймає `{ readonly fx: number; readonly fy: number }`.
  `place()` повертає `[round((outerW − innerW) × fx), round((outerH − innerH) × fy)]`.

- [ ] **Step 1: Написати тест, що падає**

Додати в кінець `packages/core/test/fit.test.ts`:

```ts
describe('прив’язка часткою люфту', () => {
  it('нулі збігаються з top-left', () => {
    const opts = { width: 100, height: 100, mode: 'cover' as const };
    const named = fit(solidImage(200, 100, RED), { ...opts, position: 'top-left' });
    const frac = fit(solidImage(200, 100, RED), { ...opts, position: { fx: 0, fy: 0 } });
    expect(Array.from(frac.data)).toEqual(Array.from(named.data));
  });

  it('половини збігаються з center', () => {
    const opts = { width: 100, height: 100, mode: 'cover' as const };
    const named = fit(solidImage(200, 100, RED), { ...opts, position: 'center' });
    const frac = fit(solidImage(200, 100, RED), { ...opts, position: { fx: 0.5, fy: 0.5 } });
    expect(Array.from(frac.data)).toEqual(Array.from(named.data));
  });

  it('одиниці збігаються з bottom-right', () => {
    const opts = { width: 100, height: 100, mode: 'contain' as const };
    const named = fit(solidImage(200, 100, RED), { ...opts, position: 'bottom-right' });
    const frac = fit(solidImage(200, 100, RED), { ...opts, position: { fx: 1, fy: 1 } });
    expect(Array.from(frac.data)).toEqual(Array.from(named.data));
  });

  it('зміщує кадр по горизонталі', () => {
    // Ліва половина червона, права синя; кадр 50×100 з 200×100.
    const img = solidImage(200, 100, RED);
    for (let y = 0; y < 100; y++) {
      for (let x = 100; x < 200; x++) {
        const i = (y * 200 + x) * 4;
        img.data[i] = 0; img.data[i + 2] = 255;
      }
    }
    const opts = { width: 50, height: 100, mode: 'cover' as const };
    expect(pixelAt(fit(img, { ...opts, position: { fx: 0, fy: 0 } }), 25, 50)).toEqual(RED);
    expect(pixelAt(fit(img, { ...opts, position: { fx: 1, fy: 0 } }), 25, 50)).toEqual(BLUE);
  });

  it('затискає значення поза 0..1', () => {
    const opts = { width: 100, height: 100, mode: 'contain' as const };
    const clamped = fit(solidImage(200, 100, RED), { ...opts, position: { fx: -3, fy: 9 } });
    const edge = fit(solidImage(200, 100, RED), { ...opts, position: { fx: 0, fy: 1 } });
    expect(Array.from(clamped.data)).toEqual(Array.from(edge.data));
  });
});
```

- [ ] **Step 2: Запустити тест і переконатись, що він падає**

Run: `pnpm vitest run packages/core/test/fit.test.ts`
Expected: FAIL — TypeScript відхилить `{ fx, fy }` як `Position`, або
`place` поверне `NaN`, бо читає `position.x`.

- [ ] **Step 3: Розширити тип**

У `packages/core/src/types.ts` замінити оголошення `Position`:

```ts
/**
 * Дев'ять точок прив'язки, частка вільного місця або зміщення в пікселях.
 *
 * Частка — узагальнення іменованих точок: `top-left` це `{fx: 0, fy: 0}`,
 * `center` — `{fx: 0.5, fy: 0.5}`. На відміну від пікселів вона означає
 * те саме після зміни цільового розміру й на кожному файлі пакета, тож
 * саме її дає тягнення в інтерфейсі.
 */
export type Position =
  | 'center' | 'top' | 'bottom' | 'left' | 'right'
  | 'top-left' | 'top-right' | 'bottom-left' | 'bottom-right'
  | { readonly fx: number; readonly fy: number }
  | { readonly x: number; readonly y: number };
```

- [ ] **Step 4: Навчити `place` рахувати частку**

У `packages/core/src/ops/fit.ts` замінити перший рядок тіла `place`:

```ts
function clamp01(v: number): number {
  return v < 0 ? 0 : v > 1 ? 1 : Number.isFinite(v) ? v : 0;
}

/** Зміщення внутрішнього прямокутника всередині зовнішнього. */
function place(
  outerW: number, outerH: number, innerW: number, innerH: number, position: Position,
): readonly [number, number] {
  if (typeof position === 'object') {
    if ('fx' in position) {
      return [
        Math.round((outerW - innerW) * clamp01(position.fx)),
        Math.round((outerH - innerH) * clamp01(position.fy)),
      ];
    }
    return [Math.round(position.x), Math.round(position.y)];
  }
  // ...решта switch лишається без змін
```

- [ ] **Step 5: Запустити тести**

Run: `pnpm vitest run packages/core/test/fit.test.ts`
Expected: PASS, усі попередні тести `fit` теж зелені.

- [ ] **Step 6: Перевірити типи й закомітити**

```bash
pnpm typecheck && pnpm test
git add packages/core/src/types.ts packages/core/src/ops/fit.ts packages/core/test/fit.test.ts
git commit -m "feat(core): прив'язка як частка вільного місця"
```

---

## Task 2: `zoom` у `FitOptions`

**Files:**
- Modify: `packages/core/src/types.ts` (`FitOptions`)
- Modify: `packages/core/src/ops/fit.ts` (функція `fit`)
- Test: `packages/core/test/fit.test.ts`

**Interfaces:**
- Consumes: Task 1
- Produces: `FitOptions.zoom?: number` — множник масштабу, застосований
  після обмеження `allowUpscale`, затиснутий знизу одиницею.

- [ ] **Step 1: Написати тест, що падає**

```ts
describe('наближення', () => {
  it('cover із zoom 2 лишає вдвічі меншу частину кадру', () => {
    // Смуга 8 px посередині 200×200: при zoom 2 вона має стати 16 px.
    const img = solidImage(200, 200, RED);
    for (let y = 96; y < 104; y++) {
      for (let x = 0; x < 200; x++) {
        const i = (y * 200 + x) * 4;
        img.data[i] = 0; img.data[i + 2] = 255;
      }
    }
    const plain = fit(img, { width: 200, height: 200, mode: 'cover' });
    const zoomed = fit(img, { width: 200, height: 200, mode: 'cover', zoom: 2 });
    const blueRows = (r: typeof plain): number => {
      let n = 0;
      for (let y = 0; y < r.height; y++) if (pixelAt(r, 100, y).b > 128) n++;
      return n;
    };
    expect(blueRows(plain)).toBe(8);
    expect(blueRows(zoomed)).toBe(16);
  });

  it('діє попри вимкнений allowUpscale', () => {
    // 100×100 у кадр 100×100: масштаб 1, allowUpscale не дозволяє більше,
    // але явний жест людини має право.
    const out = fit(solidImage(100, 100, RED), {
      width: 100, height: 100, mode: 'contain', zoom: 2, allowUpscale: false,
    });
    // Зображення вдвічі більше за полотно — кути тепер зайняті ним, не полями.
    expect(pixelAt(out, 0, 0)).toEqual(RED);
  });

  it('менше за одиницю затискається до одиниці', () => {
    const one = fit(solidImage(200, 100, RED), { width: 100, height: 100, mode: 'cover' });
    const small = fit(solidImage(200, 100, RED), {
      width: 100, height: 100, mode: 'cover', zoom: 0.25,
    });
    expect(Array.from(small.data)).toEqual(Array.from(one.data));
  });

  it('ігнорується там, де люфту немає', () => {
    for (const mode of ['fill', 'inside', 'outside'] as const) {
      const plain = fit(solidImage(200, 100, RED), { width: 100, height: 100, mode });
      const zoomed = fit(solidImage(200, 100, RED), { width: 100, height: 100, mode, zoom: 3 });
      expect({ mode, w: zoomed.width, h: zoomed.height })
        .toEqual({ mode, w: plain.width, h: plain.height });
    }
  });
});
```

- [ ] **Step 2: Запустити й переконатись, що падає**

Run: `pnpm vitest run packages/core/test/fit.test.ts -t наближення`
Expected: FAIL — `zoom` не існує в типі.

- [ ] **Step 3: Додати поле в тип**

У `packages/core/src/types.ts`, всередині `FitOptions`, після `allowUpscale`:

```ts
  /**
   * Множник масштабу, не менше за 1. Типово 1.
   *
   * Застосовується після обмеження allowUpscale: той прапорець відповідає
   * на питання «чи можна збільшувати те, що менше за кадр», а zoom —
   * явний жест людини, яка вже дивиться на результат.
   *
   * Має сенс лише для contain і cover: у fill, inside та outside вільного
   * місця немає, і рухати нічого.
   */
  readonly zoom?: number;
```

- [ ] **Step 4: Застосувати в `fit`**

У `packages/core/src/ops/fit.ts`, одразу після рядка
`if (fitsInside && scale > 1 && opts.allowUpscale !== true) scale = 1;`:

```ts
  // Наближення — після allowUpscale: див. коментар до поля zoom.
  // У inside та outside вихід дорівнює масштабованому зображенню, тож
  // наближення там означало б просто інший розмір файлу, а не інший кадр.
  if (mode === 'contain' || mode === 'cover') {
    const zoom = opts.zoom ?? 1;
    if (zoom > 1) scale *= zoom;
  }
```

- [ ] **Step 5: Запустити тести**

Run: `pnpm vitest run packages/core/test/fit.test.ts`
Expected: PASS.

- [ ] **Step 6: Закомітити**

```bash
pnpm typecheck && pnpm test
git add packages/core/src/types.ts packages/core/src/ops/fit.ts packages/core/test/fit.test.ts
git commit -m "feat(core): наближення в fit"
```

---

## Task 3: стан віджета — зміщення й наближення

**Files:**
- Modify: `apps/web/src/lib/worker-api.ts` (`WidgetState`, `buildJob`)
- Test: `apps/web/test/buildjob.test.ts`

**Interfaces:**
- Consumes: Task 1, Task 2
- Produces: `WidgetState` отримує `zoom: number`. Поле `position` уже
  типоване як `Position`, тож приймає `{fx, fy}` без змін типу.
  `buildJob` кладе `zoom` в операцію `fit`.

- [ ] **Step 1: Написати тест, що падає**

Додати в `apps/web/test/buildjob.test.ts`:

```ts
describe('кадрування тягненням', () => {
  const fitOp = (s: WidgetState) =>
    buildJob(s).ops.find((o) => o.type === 'fit') as { zoom?: number; position?: unknown };

  it('одиничне наближення не потрапляє в Job', () => {
    expect(fitOp({ ...base, zoom: 1 }).zoom).toBeUndefined();
  });

  it('наближення передається в fit', () => {
    expect(fitOp({ ...base, zoom: 2.5 }).zoom).toBe(2.5);
  });

  it('зміщення часткою передається як є', () => {
    expect(fitOp({ ...base, position: { fx: 0.25, fy: 0.75 } }).position)
      .toEqual({ fx: 0.25, fy: 0.75 });
  });
});
```

Також додати `zoom: 1,` у літерал `base` угорі файлу.

- [ ] **Step 2: Запустити й переконатись, що падає**

Run: `pnpm vitest run apps/web/test/buildjob.test.ts`
Expected: FAIL — `zoom` немає в `WidgetState`.

- [ ] **Step 3: Розширити стан і `buildJob`**

У `apps/web/src/lib/worker-api.ts`, в `WidgetState` після `upscale`:

```ts
  /** Наближення кадру, 1 — без наближення. */
  readonly zoom: number;
```

У `buildJob`, замінити виклик `ops.push({ type: 'fit', ... })` на:

```ts
  // Одиничне наближення в Job не кладемо: воно нічого не змінює, а в
  // пакетному режимі зайве поле робило б два однакові прогони різними.
  ops.push({
    type: 'fit',
    width: s.width,
    height: s.height,
    mode: s.mode,
    pad: s.padTransparent ? 'transparent' : parseHexColor(s.padColor),
    allowUpscale: s.allowUpscale,
    position: s.position,
    ...(s.zoom > 1 ? { zoom: s.zoom } : {}),
  });
```

- [ ] **Step 4: Запустити тести**

Run: `pnpm vitest run apps/web/test/buildjob.test.ts`
Expected: PASS.

- [ ] **Step 5: Полагодити місця, де `WidgetState` створюється**

`pnpm typecheck` покаже, що `apps/web/src/components/Widget.svelte`
не задає `zoom`. Додати `zoom: 1,` в ініціалізатор `$state<WidgetState>`
після `upscale: 1,`.

- [ ] **Step 6: Закомітити**

```bash
pnpm typecheck && pnpm test
git add apps/web/src/lib/worker-api.ts apps/web/test/buildjob.test.ts apps/web/src/components/Widget.svelte
git commit -m "feat(web): наближення й зміщення в стані віджета"
```

---

## Task 4: `Stage.svelte` — інтерактивна сцена

**Files:**
- Create: `apps/web/src/components/Stage.svelte`
- Modify: `apps/web/src/lib/i18n.ts` (інтерфейс `Dict` і обидва словники)

**Interfaces:**
- Consumes: Task 3
- Produces: компонент із пропсами
  `{ src: string; dims: {w,h} | null; targetW: number; targetH: number;
     mode: FitMode; position: Position; zoom: number; t: Dict;
     onchange: (p: { position: Position; zoom: number }) => void }`.
  Викликає `onchange` **один раз** на завершення жесту, не щокадру.

- [ ] **Step 1: Додати рядки в словник**

У `apps/web/src/lib/i18n.ts`, в інтерфейс `Dict` всередині блоку `crop`:

```ts
    drag: string;
    dragHint: string;
    zoom: string;
    reset: string;
```

В український словник, у блок `crop`:

```ts
    drag: 'Кадр',
    dragHint: 'Тягніть зображення, щоб посунути кадр. Колесо або два пальці — наблизити.',
    zoom: 'Наближення',
    reset: 'Скинути кадр',
```

В англійський:

```ts
    drag: 'Framing',
    dragHint: 'Drag the image to move the frame. Wheel or two fingers to zoom.',
    zoom: 'Zoom',
    reset: 'Reset framing',
```

- [ ] **Step 2: Перевірити парність ключів**

Run: `pnpm vitest run apps/web/test/i18n.test.ts`
Expected: PASS — інакше ключі розійшлись.

- [ ] **Step 3: Створити компонент**

`apps/web/src/components/Stage.svelte`:

```svelte
<script lang="ts">
  import type { FitMode, Position } from '@obrobka/core';
  import type { Dict } from '../lib/i18n.js';

  /**
   * Кадр, яким керують пальцем або мишею.
   *
   * Живий відгук — це CSS-трансформ на `<img>`: жодного декодування й
   * жодного повідомлення у воркер. Справжній Job перезапускається один
   * раз, на завершення жесту. Інакше кожен рух пальця запускав би повний
   * пайплайн, а з увімкненою моделлю — ще й сесію ONNX.
   */
  let {
    src, dims, targetW, targetH, mode, position, zoom, t, onchange,
  }: {
    src: string;
    dims: { w: number; h: number } | null;
    targetW: number;
    targetH: number;
    mode: FitMode;
    position: Position;
    zoom: number;
    t: Dict;
    onchange: (p: { position: Position; zoom: number }) => void;
  } = $props();

  /** Кадрувати можна лише там, де є вільне місце. */
  const movable = $derived(mode === 'contain' || mode === 'cover');

  /** Частки, з якими працює жест. Іменована прив'язка переводиться в них. */
  const NAMED: Record<string, [number, number]> = {
    'top-left': [0, 0], top: [0.5, 0], 'top-right': [1, 0],
    left: [0, 0.5], center: [0.5, 0.5], right: [1, 0.5],
    'bottom-left': [0, 1], bottom: [0.5, 1], 'bottom-right': [1, 1],
  };

  function toFractions(p: Position): [number, number] {
    if (typeof p === 'object' && 'fx' in p) return [p.fx, p.fy];
    if (typeof p === 'object') return [0.5, 0.5];
    return NAMED[p] ?? [0.5, 0.5];
  }

  let fx = $state(0.5);
  let fy = $state(0.5);
  let z = $state(1);

  // Стан ззовні — джерело правди, поки жест не почався.
  $effect(() => {
    if (dragging) return;
    const [a, b] = toFractions(position);
    fx = a; fy = b; z = zoom;
  });

  let dragging = $state(false);
  let frame: HTMLDivElement;

  /**
   * Скільки пікселів прев'ю відповідає повному ходу частки.
   *
   * Це і є люфт: наскільки зображення більше за кадр. Без нього
   * тягнення на маленькому прев'ю рухало б кадр так само, як на великому,
   * і жест не збігався б із тим, що видно.
   */
  const slack = $derived.by(() => {
    if (dims === null) return { x: 0, y: 0 };
    const box = frame?.getBoundingClientRect();
    if (box === undefined) return { x: 0, y: 0 };
    const scale = mode === 'cover'
      ? Math.max(box.width / dims.w, box.height / dims.h)
      : Math.min(box.width / dims.w, box.height / dims.h);
    const w = dims.w * scale * z;
    const h = dims.h * scale * z;
    return { x: Math.abs(w - box.width), y: Math.abs(h - box.height) };
  });

  const pointers = new Map<number, { x: number; y: number }>();
  let startFx = 0;
  let startFy = 0;
  let startZ = 1;
  let startSpread = 0;
  let startX = 0;
  let startY = 0;

  function spread(): number {
    const [a, b] = Array.from(pointers.values());
    if (a === undefined || b === undefined) return 0;
    return Math.hypot(a.x - b.x, a.y - b.y);
  }

  function onPointerDown(e: PointerEvent): void {
    if (!movable) return;
    (e.currentTarget as HTMLElement).setPointerCapture(e.pointerId);
    pointers.set(e.pointerId, { x: e.clientX, y: e.clientY });
    dragging = true;
    startFx = fx; startFy = fy; startZ = z;
    startX = e.clientX; startY = e.clientY;
    if (pointers.size === 2) startSpread = spread();
  }

  function onPointerMove(e: PointerEvent): void {
    if (!dragging || !pointers.has(e.pointerId)) return;
    pointers.set(e.pointerId, { x: e.clientX, y: e.clientY });

    if (pointers.size >= 2) {
      const now = spread();
      if (startSpread > 0 && now > 0) z = clampZoom(startZ * (now / startSpread));
      return;
    }
    // У contain зображення менше за полотно, тож рух пальця й рух кадру
    // збігаються; у cover ми рухаємо вікно по зображенню, тобто навпаки.
    const sign = mode === 'cover' ? 1 : -1;
    if (slack.x > 0) fx = clamp01(startFx + (sign * (startX - e.clientX)) / slack.x);
    if (slack.y > 0) fy = clamp01(startFy + (sign * (startY - e.clientY)) / slack.y);
  }

  function onPointerUp(e: PointerEvent): void {
    pointers.delete(e.pointerId);
    if (pointers.size > 0) return;
    dragging = false;
    commit();
  }

  function onWheel(e: WheelEvent): void {
    if (!movable) return;
    e.preventDefault();
    z = clampZoom(z * (e.deltaY < 0 ? 1.1 : 1 / 1.1));
    schedule();
  }

  /**
   * Колесо приходить чергою подій без «кінця жесту», тож перезапуск
   * відкладається: інакше один прокрут дав би півсотні прогонів.
   */
  let timer: ReturnType<typeof setTimeout> | null = null;
  function schedule(): void {
    if (timer !== null) clearTimeout(timer);
    timer = setTimeout(() => { timer = null; commit(); }, 180);
  }

  function commit(): void {
    onchange({ position: { fx, fy }, zoom: z });
  }

  function reset(): void {
    fx = 0.5; fy = 0.5; z = 1;
    commit();
  }

  function clamp01(v: number): number { return v < 0 ? 0 : v > 1 ? 1 : v; }
  function clampZoom(v: number): number { return v < 1 ? 1 : v > 8 ? 8 : v; }

  /** Позиціювання прев'ю тими самими частками, що піде в Job. */
  const objectPosition = $derived(`${(fx * 100).toFixed(2)}% ${(fy * 100).toFixed(2)}%`);
  const objectFit = $derived(mode === 'cover' ? 'cover' : 'contain');

  function onKeyDown(e: KeyboardEvent): void {
    if (!movable) return;
    const step = e.shiftKey ? 0.1 : 0.02;
    const moves: Record<string, () => void> = {
      ArrowLeft: () => { fx = clamp01(fx - step); },
      ArrowRight: () => { fx = clamp01(fx + step); },
      ArrowUp: () => { fy = clamp01(fy - step); },
      ArrowDown: () => { fy = clamp01(fy + step); },
      '+': () => { z = clampZoom(z * 1.1); },
      '=': () => { z = clampZoom(z * 1.1); },
      '-': () => { z = clampZoom(z / 1.1); },
    };
    const move = moves[e.key];
    if (move === undefined) return;
    e.preventDefault();
    move();
    schedule();
  }
</script>

<figure class="stage-pane">
  <figcaption>
    <span class="tag">{t.crop.drag}</span>
    {#if z > 1}
      <span class="zoom" data-testid="zoom-value">{z.toFixed(1)}×</span>
    {/if}
    {#if movable}
      <button type="button" class="reset" data-testid="framing-reset"
              title={t.crop.reset} aria-label={t.crop.reset} onclick={reset}>⟲</button>
    {/if}
  </figcaption>

  <div
    class="frame checker"
    class:movable
    class:dragging
    bind:this={frame}
    style={`aspect-ratio: ${targetW} / ${targetH}`}
    role={movable ? 'slider' : undefined}
    tabindex={movable ? 0 : undefined}
    aria-label={movable ? t.crop.drag : undefined}
    aria-valuemin={movable ? 0 : undefined}
    aria-valuemax={movable ? 100 : undefined}
    aria-valuenow={movable ? Math.round(fx * 100) : undefined}
    data-testid="stage"
    onpointerdown={onPointerDown}
    onpointermove={onPointerMove}
    onpointerup={onPointerUp}
    onpointercancel={onPointerUp}
    onwheel={onWheel}
    onkeydown={onKeyDown}
  >
    {#if src !== ''}
      <img
        {src}
        alt={t.before}
        draggable="false"
        style={`object-fit: ${objectFit}; object-position: ${objectPosition}; transform: scale(${z});`}
      />
    {/if}
  </div>

  {#if movable}<p class="hint">{t.crop.dragHint}</p>{/if}
</figure>

<style>
  .stage-pane { display: grid; gap: 0.45rem; min-width: 0; }
  .stage-pane figcaption { display: flex; align-items: center; gap: 0.5rem; }
  .zoom {
    font-family: var(--font-mono); font-size: 0.72rem; color: var(--fg-muted);
    font-variant-numeric: tabular-nums;
  }
  .reset {
    margin-inline-start: auto;
    width: 1.6rem; height: 1.6rem;
    display: grid; place-items: center;
    border: 1px solid var(--line-strong); border-radius: var(--r-sm);
    background: var(--bg-sunken); cursor: pointer; line-height: 1;
  }
  .reset:hover { border-color: var(--accent); }

  .frame {
    position: relative;
    overflow: hidden;
    display: grid;
    place-items: center;
    border: 1px solid var(--line);
    border-radius: var(--r-md);
    --checker-size: 14px;
  }
  /* Без цього палець гортає сторінку замість кадру. */
  .frame.movable { cursor: grab; touch-action: none; }
  .frame.dragging { cursor: grabbing; }
  .frame img {
    width: 100%; height: 100%;
    user-select: none; -webkit-user-drag: none;
  }
  .hint { font-size: 0.78rem; color: var(--fg-faint); max-width: 40ch; }
</style>
```

- [ ] **Step 4: Перевірити, що кирилиці в розмітці немає**

Run: `pnpm vitest run apps/web/test/i18n.test.ts`
Expected: PASS. Українські коментарі в `<script>` дозволені, видимі
рядки мають іти лише через `t`.

- [ ] **Step 5: Закомітити**

```bash
pnpm typecheck && pnpm test
git add apps/web/src/components/Stage.svelte apps/web/src/lib/i18n.ts
git commit -m "feat(web): сцена з кадруванням тягненням"
```

---

## Task 5: під'єднати сцену до віджета

**Files:**
- Modify: `apps/web/src/components/Widget.svelte` (панель «Було» в блоці `.stage`)
- Test: `apps/web/e2e/framing.spec.ts` (створити)

**Interfaces:**
- Consumes: Task 3, Task 4
- Produces: тягнення по `[data-testid="stage"]` змінює `state.position`
  на `{fx, fy}` і перезапускає обробку.

- [ ] **Step 1: Написати e2e-тест, що падає**

Створити `apps/web/e2e/framing.spec.ts`:

```ts
import { test, expect, type Page } from '@playwright/test';
import { join } from 'node:path';

const SPHERE = join(import.meta.dirname, 'sphere.png');

async function ready(page: Page): Promise<void> {
  await page.goto('/');
  await expect(page.locator('input[type=file][data-ready="true"]')).toBeAttached({ timeout: 60_000 });
  await page.setInputFiles('input[type=file]', SPHERE);
  await expect(page.getByTestId('result')).toBeVisible({ timeout: 60_000 });
}

test('сцена показується для contain і зникає для розтягування', async ({ page }) => {
  await ready(page);
  await expect(page.getByTestId('stage')).toBeVisible();
  await page.getByRole('button', { name: 'Розтягнути' }).click();
  await expect(page.getByTestId('stage')).toBeVisible();
  // Кадрувати нічого — підказки й скидання немає
  await expect(page.getByTestId('framing-reset')).toHaveCount(0);
});

test('тягнення переводить прив’язку в частки', async ({ page }) => {
  await ready(page);
  await page.getByRole('button', { name: 'Покрити' }).click();
  await page.getByTestId('ratio-16:9').click();
  await expect(page.getByTestId('stage')).toBeVisible();

  const box = (await page.getByTestId('stage').boundingBox())!;
  await page.mouse.move(box.x + box.width / 2, box.y + box.height / 2);
  await page.mouse.down();
  await page.mouse.move(box.x + 8, box.y + box.height / 2, { steps: 8 });
  await page.mouse.up();

  // Жодна з дев'яти крапок більше не активна: прив'язка стала власною
  await expect(page.locator('[data-testid^="anchor-"][aria-checked="true"]')).toHaveCount(0);
  await expect(page.locator('.error')).toHaveCount(0);
});

test('колесо наближає й показує кратність', async ({ page }) => {
  await ready(page);
  const stage = page.getByTestId('stage');
  const box = (await stage.boundingBox())!;
  await page.mouse.move(box.x + box.width / 2, box.y + box.height / 2);
  await page.mouse.wheel(0, -300);
  await expect(page.getByTestId('zoom-value')).toBeVisible();
  await page.getByTestId('framing-reset').click();
  await expect(page.getByTestId('zoom-value')).toHaveCount(0);
});

/**
 * Найдорожча помилка тут — перезапускати пайплайн на кожен рух пальця.
 * Лічимо не виклики, а результати: кожен прогін віддає новий objectURL,
 * тож зміни src досить, щоб побачити зайві прогони.
 */
test('тягнення не перезапускає обробку щокадру', async ({ page }) => {
  await ready(page);
  await page.getByRole('button', { name: 'Покрити' }).click();
  await expect(page.getByTestId('result')).toBeVisible({ timeout: 60_000 });

  await page.evaluate(() => {
    const w = window as unknown as { __runs: number };
    w.__runs = 0;
    const img = document.querySelector('[data-testid="result"]')!;
    new MutationObserver(() => { w.__runs++; })
      .observe(img, { attributes: true, attributeFilter: ['src'] });
  });

  const box = (await page.getByTestId('stage').boundingBox())!;
  await page.mouse.move(box.x + box.width / 2, box.y + box.height / 2);
  await page.mouse.down();
  await page.mouse.move(box.x + 10, box.y + 10, { steps: 25 });
  await page.mouse.up();
  await page.waitForTimeout(1500);

  const runs = await page.evaluate(() => (window as unknown as { __runs: number }).__runs);
  expect(runs).toBeGreaterThan(0);
  expect(runs).toBeLessThanOrEqual(2);
});
```

- [ ] **Step 2: Запустити й переконатись, що падає**

```bash
pnpm --filter @obrobka/web build && pnpm exec playwright test apps/web/e2e/framing.spec.ts
```
Expected: FAIL — `[data-testid="stage"]` не існує.

- [ ] **Step 3: Замінити панель «Було» на сцену**

У `apps/web/src/components/Widget.svelte` додати імпорт після інших:

```ts
  import Stage from './Stage.svelte';
```

Замінити перший `<figure class="pane">` у блоці `.stage` (той, що з
`{t.before}`) на:

```svelte
      <Stage
        src={sourceUrl}
        dims={sourceDims}
        targetW={state.width}
        targetH={state.height}
        mode={state.mode}
        position={state.position}
        zoom={state.zoom}
        {t}
        onchange={(p) => {
          state.position = p.position;
          state.zoom = p.zoom;
          void process();
        }}
      />
```

- [ ] **Step 4: Показати розмір джерела під сценою**

Підпис `{sourceDims.w}×{sourceDims.h} · {kb(sourceSize)}` жив у знятій
панелі. Повернути його окремим рядком одразу після `<Stage …/>`:

```svelte
      <p class="meta source-meta">
        {#if sourceDims}{sourceDims.w}×{sourceDims.h}{/if} · {kb(sourceSize)}
      </p>
```

і додати в `<style>`:

```css
  .source-meta { grid-column: 1; }
```

- [ ] **Step 5: Запустити e2e**

```bash
pnpm --filter @obrobka/web build && pnpm exec playwright test apps/web/e2e/framing.spec.ts apps/web/e2e/crop.spec.ts apps/web/e2e/layout.spec.ts
```
Expected: PASS. `crop.spec.ts` і `layout.spec.ts` — перевірка, що
перебудова панелі нічого не зламала.

- [ ] **Step 6: Закомітити**

```bash
pnpm typecheck && pnpm test
git add apps/web/src/components/Widget.svelte apps/web/e2e/framing.spec.ts
git commit -m "feat(web): кадрування тягненням у віджеті"
```

---

# Частина B — шари

## Task 6: тип `Layer` і операція `composite`

**Files:**
- Modify: `packages/core/src/types.ts` (`BlendMode`, `Layer`, `Op`)
- Create: `packages/core/src/ops/composite.ts`
- Modify: `packages/core/src/index.ts` (експорт)
- Test: `packages/core/test/layers.test.ts` (створити)

**Interfaces:**
- Consumes: нічого з попередніх задач
- Produces: `composite(base: RasterImage, layers: readonly Layer[]): RasterImage`,
  типи `BlendMode` і `Layer`, варіант операції `{ type: 'composite'; layers }`.

- [ ] **Step 1: Написати тести, що падають**

Створити `packages/core/test/layers.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { composite } from '../src/ops/composite.js';
import { resample } from '../src/ops/resample.js';
import { solidImage, pixelAt, expectPixel } from './helpers.js';
import type { Layer, RGBA } from '../src/types.js';

const RED: RGBA = { r: 255, g: 0, b: 0, a: 255 };
const BLUE: RGBA = { r: 0, g: 0, b: 255, a: 255 };
const WHITE: RGBA = { r: 255, g: 255, b: 255, a: 255 };
const CLEAR: RGBA = { r: 0, g: 0, b: 0, a: 0 };

const layer = (over: Partial<Layer> = {}): Layer => ({
  image: solidImage(20, 20, BLUE), x: 0.5, y: 0.5, scale: 0.5, ...over,
});

describe('composite', () => {
  it('не чіпає основу поза шаром', () => {
    const out = composite(solidImage(100, 100, RED), [layer()]);
    expect(pixelAt(out, 2, 2)).toEqual(RED);
    expect(pixelAt(out, 97, 97)).toEqual(RED);
  });

  it('кладе шар у центр', () => {
    const out = composite(solidImage(100, 100, RED), [layer()]);
    expect(pixelAt(out, 50, 50)).toEqual(BLUE);
  });

  it('масштаб рахується від ширини основи', () => {
    // scale 0.5 на основі 100 px — шар 50 px завширшки, тобто 25..75.
    const out = composite(solidImage(100, 100, RED), [layer()]);
    expect(pixelAt(out, 26, 50)).toEqual(BLUE);
    expect(pixelAt(out, 23, 50)).toEqual(RED);
  });

  it('те саме місце на основах різного розміру', () => {
    // scale 0.2 навмисно: при 0.5 лівий край шару сів би рівно на нуль
    // на обох основах, і тест проходив би, навіть якби нормалізації не було.
    const where = (side: number): number => {
      const out = composite(solidImage(side, side, RED), [
        layer({ x: 0.25, y: 0.5, scale: 0.2 }),
      ]);
      let first = -1;
      for (let x = 0; x < side; x++) if (pixelAt(out, x, side / 2).b > 128) { first = x; break; }
      return first / side;
    };
    expect(Math.abs(where(100) - where(400))).toBeLessThan(0.01);
  });

  it('висота шару йде з власного співвідношення, не з основи', () => {
    // Шар 20×10 при scale 0.5 на основі 100×100 має бути 50×25.
    const out = composite(solidImage(100, 100, RED), [
      layer({ image: solidImage(20, 10, BLUE) }),
    ]);
    expect(pixelAt(out, 50, 50)).toEqual(BLUE);
    expect(pixelAt(out, 50, 36)).toEqual(RED);
    expect(pixelAt(out, 50, 39)).toEqual(BLUE);
  });

  it('прозорість домножує альфу шару', () => {
    const out = composite(solidImage(100, 100, RED), [layer({ opacity: 0.5 })]);
    expectPixel(out, 50, 50, { r: 128, g: 0, b: 128, a: 255 }, 2);
  });

  it('нульова прозорість не видно за жодного режиму', () => {
    const out = composite(solidImage(100, 100, RED), [
      layer({ opacity: 0, blend: 'multiply' }),
    ]);
    expect(pixelAt(out, 50, 50)).toEqual(RED);
  });

  it('multiply множить канали', () => {
    const out = composite(solidImage(100, 100, WHITE), [
      layer({ image: solidImage(20, 20, RED), blend: 'multiply' }),
    ]);
    expectPixel(out, 50, 50, RED, 2);
  });

  it('screen освітлює', () => {
    const out = composite(solidImage(100, 100, RED), [
      layer({ blend: 'screen' }),
    ]);
    // червоне + синє на екрані дає пурпурове
    expectPixel(out, 50, 50, { r: 255, g: 0, b: 255, a: 255 }, 2);
  });

  it('порядок масиву — це порядок накладання', () => {
    const out = composite(solidImage(100, 100, WHITE), [
      layer({ image: solidImage(20, 20, RED) }),
      layer({ image: solidImage(20, 20, BLUE) }),
    ]);
    expect(pixelAt(out, 50, 50)).toEqual(BLUE);
  });

  it('прозорий край шару не темніє', () => {
    // Синій диск на прозорому тлі, покладений на біле. Правильна
    // композиція дає r = g = 255·(1 − a), а b лишається 255 по всьому
    // ряду. Якби альфа не була premultiplied, чорний колір повністю
    // прозорих пікселів затікав би в край і збивав саме b — та сама
    // темна облямівка, від якої бережеться resample.
    const src = solidImage(64, 64, CLEAR);
    for (let y = 0; y < 64; y++) {
      for (let x = 0; x < 64; x++) {
        if (Math.hypot(x - 31.5, y - 31.5) > 28) continue;
        const i = (y * 64 + x) * 4;
        src.data[i + 2] = 255; src.data[i + 3] = 255;
      }
    }
    const out = composite(solidImage(100, 100, WHITE), [
      layer({ image: src, scale: 0.4 }),
    ]);
    for (let x = 30; x < 70; x++) {
      const p = pixelAt(out, x, 50);
      expect({ x, b: p.b }).toEqual({ x, b: 255 });
      expect({ x, r: p.r }).toEqual({ x, r: p.g });
    }
  });

  it('зменшення шару не гірше за resample', () => {
    // Смугастий шар, зведений удвічі: якби ми брали білінійну вибірку
    // замість усереднення по площі, смуги дали б муар.
    const stripes = solidImage(64, 64, WHITE);
    for (let y = 0; y < 64; y++) {
      for (let x = 0; x < 64; x++) {
        if (x % 2 === 0) continue;
        const i = (y * 64 + x) * 4;
        stripes.data[i] = 0; stripes.data[i + 1] = 0; stripes.data[i + 2] = 0;
      }
    }
    const out = composite(solidImage(32, 32, RED), [
      { image: stripes, x: 0.5, y: 0.5, scale: 1 },
    ]);
    const direct = resample(stripes, 32, 32);
    expectPixel(out, 16, 16, pixelAt(direct, 16, 16), 3);
  });

  it('поворот на 90° міняє сторони місцями', () => {
    const out = composite(solidImage(100, 100, RED), [
      layer({ image: solidImage(40, 10, BLUE), scale: 0.4, rotation: 90 }),
    ]);
    // До повороту шар 40×10 → 40×10 px; після — 10 завширшки, 40 заввишки
    expect(pixelAt(out, 50, 35)).toEqual(BLUE);
    expect(pixelAt(out, 35, 50)).toEqual(RED);
  });

  it('порожній список шарів повертає копію, а не ту саму пам’ять', () => {
    const base = solidImage(10, 10, RED);
    const out = composite(base, []);
    expect(out.data).not.toBe(base.data);
    expect(Array.from(out.data)).toEqual(Array.from(base.data));
  });
});
```

- [ ] **Step 2: Запустити й переконатись, що падає**

Run: `pnpm vitest run packages/core/test/layers.test.ts`
Expected: FAIL — `../src/ops/composite.js` не існує.

- [ ] **Step 3: Додати типи**

У `packages/core/src/types.ts`, після `OutlineOptions`:

```ts
export type BlendMode =
  | 'normal' | 'multiply' | 'screen' | 'overlay' | 'darken' | 'lighten';

/**
 * Зображення, накладене поверх основи.
 *
 * Геометрія нормалізована відносно основи — тобто полотна після fit, бо
 * саме там composite і виконується. Без цього «один водяний знак на
 * двадцять файлів різного розміру» був би неможливий: у пікселях те саме
 * число означало б на кожному файлі інше місце.
 */
export interface Layer {
  readonly image: RasterImage;
  /** Центр шару як частка ширини основи. */
  readonly x: number;
  /** Центр шару як частка висоти основи. */
  readonly y: number;
  /**
   * Ширина шару як частка ширини основи. Висота береться з власного
   * співвідношення шару, тож накладене ніколи не розтягується.
   */
  readonly scale: number;
  /** Градуси за годинниковою стрілкою навколо центра. Типово 0. */
  readonly rotation?: number;
  /** 0..1. Типово 1. Домножує альфу шару після змішування. */
  readonly opacity?: number;
  /** Типово 'normal'. */
  readonly blend?: BlendMode;
}
```

У той самий файл, у `Op`, додати варіант перед закривальною дужкою типу:

```ts
  | {
      /**
       * Накладає зображення поверх. Порядок масиву — порядок накладання:
       * перший шар лежить найнижче.
       */
      readonly type: 'composite';
      readonly layers: readonly Layer[];
    }
```

- [ ] **Step 4: Написати операцію**

Створити `packages/core/src/ops/composite.ts`:

```ts
import type { BlendMode, Layer, RasterImage } from '../types.js';
import { resample } from './resample.js';

/**
 * Формули змішування, канали 0..1.
 *
 * Набір навмисно малий: це ті режими, які людина справді розрізняє на око
 * й уміє назвати. Решта з PDF-специфікації відрізняється так, що пояснити
 * її в підказці на два рядки неможливо.
 */
const BLEND: Record<BlendMode, (b: number, s: number) => number> = {
  normal: (_b, s) => s,
  multiply: (b, s) => b * s,
  screen: (b, s) => b + s - b * s,
  overlay: (b, s) => (b <= 0.5 ? 2 * b * s : 1 - 2 * (1 - b) * (1 - s)),
  darken: (b, s) => Math.min(b, s),
  lighten: (b, s) => Math.max(b, s),
};

/** Білінійна вибірка з premultiplied-буфера. Поза межами — прозорість. */
function sample(
  src: Float32Array, w: number, h: number, x: number, y: number, out: Float32Array,
): void {
  const x0 = Math.floor(x);
  const y0 = Math.floor(y);
  const tx = x - x0;
  const ty = y - y0;
  out[0] = 0; out[1] = 0; out[2] = 0; out[3] = 0;
  for (let dy = 0; dy < 2; dy++) {
    const sy = y0 + dy;
    if (sy < 0 || sy >= h) continue;
    const wy = dy === 0 ? 1 - ty : ty;
    if (wy === 0) continue;
    for (let dx = 0; dx < 2; dx++) {
      const sx = x0 + dx;
      if (sx < 0 || sx >= w) continue;
      const weight = wy * (dx === 0 ? 1 - tx : tx);
      if (weight === 0) continue;
      const i = (sy * w + sx) * 4;
      out[0]! += src[i]! * weight;
      out[1]! += src[i + 1]! * weight;
      out[2]! += src[i + 2]! * weight;
      out[3]! += src[i + 3]! * weight;
    }
  }
}

/** Переводить RGBA 0..255 зі звичайною альфою в premultiplied 0..1. */
function premultiply(img: RasterImage): Float32Array {
  const out = new Float32Array(img.width * img.height * 4);
  for (let i = 0; i < out.length; i += 4) {
    const a = img.data[i + 3]! / 255;
    out[i] = (img.data[i]! / 255) * a;
    out[i + 1] = (img.data[i + 1]! / 255) * a;
    out[i + 2] = (img.data[i + 2]! / 255) * a;
    out[i + 3] = a;
  }
  return out;
}

/**
 * Кладе один шар на полотно.
 *
 * Спершу масштаб наявним resample, і лише потім поворот. Спокуса зробити
 * одним зворотним афінним перетвором дає гірший результат: resample при
 * зменшенні усереднює по площі, а білінійна вибірка бере два відліки
 * незалежно від того, скільки пікселів накриває один вихідний. Логотип
 * 2000 px, зведений до 200 px, розсипався б на аліасинг. Поворот уже
 * правильно розміреного шару — приблизно 1:1, там вибірки досить.
 */
function drawLayer(dst: Uint8ClampedArray, dw: number, dh: number, layer: Layer): void {
  const renderW = Math.max(1, Math.round(layer.scale * dw));
  const renderH = Math.max(1, Math.round(renderW * (layer.image.height / layer.image.width)));
  const scaled = resample(layer.image, renderW, renderH);
  const src = premultiply(scaled);

  const cx = layer.x * dw;
  const cy = layer.y * dh;
  const rad = ((layer.rotation ?? 0) * Math.PI) / 180;
  const cos = Math.cos(-rad);
  const sin = Math.sin(-rad);

  // Габарит повернутого шару: інакше довелось би обходити все полотно.
  const halfW = renderW / 2;
  const halfH = renderH / 2;
  const spanX = Math.abs(halfW * Math.cos(rad)) + Math.abs(halfH * Math.sin(rad));
  const spanY = Math.abs(halfW * Math.sin(rad)) + Math.abs(halfH * Math.cos(rad));
  const x0 = Math.max(0, Math.floor(cx - spanX));
  const y0 = Math.max(0, Math.floor(cy - spanY));
  const x1 = Math.min(dw, Math.ceil(cx + spanX));
  const y1 = Math.min(dh, Math.ceil(cy + spanY));
  if (x1 <= x0 || y1 <= y0) return;

  const opacity = layer.opacity ?? 1;
  const blend = BLEND[layer.blend ?? 'normal'];
  const px = new Float32Array(4);

  for (let y = y0; y < y1; y++) {
    for (let x = x0; x < x1; x++) {
      const ux = x + 0.5 - cx;
      const uy = y + 0.5 - cy;
      const sx = ux * cos - uy * sin + halfW;
      const sy = ux * sin + uy * cos + halfH;
      sample(src, renderW, renderH, sx - 0.5, sy - 0.5, px);

      const sa = px[3]! * opacity;
      if (sa <= 0) continue;

      const i = (y * dw + x) * 4;
      const ba = dst[i + 3]! / 255;
      const outA = sa + ba * (1 - sa);
      if (outA <= 0) { dst[i + 3] = 0; continue; }

      for (let c = 0; c < 3; c++) {
        // Шар лежить у premultiplied, основа — у звичайній альфі.
        const sc = px[3]! > 0 ? px[c]! / px[3]! : 0;
        const bc = dst[i + c]! / 255;
        // Змішування діє лише там, де під шаром щось є: над порожнечею
        // multiply дав би чорноту замість самого шару.
        const mixed = (1 - ba) * sc + ba * blend(bc, sc);
        const out = mixed * sa + bc * ba * (1 - sa);
        dst[i + c] = Math.round((out / outA) * 255);
      }
      dst[i + 3] = Math.round(outA * 255);
    }
  }
}

/** Накладає шари на основу в порядку масиву: перший — найнижче. */
export function composite(base: RasterImage, layers: readonly Layer[]): RasterImage {
  const data = new Uint8ClampedArray(base.data);
  for (const layer of layers) {
    if (layer.image.width < 1 || layer.image.height < 1) continue;
    drawLayer(data, base.width, base.height, layer);
  }
  return { data, width: base.width, height: base.height };
}
```

- [ ] **Step 5: Експортувати з ядра**

У `packages/core/src/index.ts` додати після рядка з `outline`:

```ts
export { composite } from './ops/composite.js';
```

- [ ] **Step 6: Запустити тести**

Run: `pnpm vitest run packages/core/test/layers.test.ts`
Expected: PASS — усі п'ятнадцять.

- [ ] **Step 7: Закомітити**

```bash
pnpm typecheck && pnpm test
git add packages/core/src/types.ts packages/core/src/ops/composite.ts \
        packages/core/src/index.ts packages/core/test/layers.test.ts
git commit -m "feat(core): накладання шарів"
```

---

## Task 7: `composite` у пайплайні

**Files:**
- Modify: `packages/core/src/ops/pipeline.ts` (`applyOp`)
- Test: `packages/core/test/pipeline.test.ts`

**Interfaces:**
- Consumes: Task 6
- Produces: `runJob` виконує операцію `composite`.

- [ ] **Step 1: Написати тест, що падає**

Наявний `fakeCodec` у `pipeline.test.ts` кодує результат у JSON із самими
розмірами, тож пікселі через нього не побачити. Для цього тесту потрібен
кодек, який перехоплює готове зображення. Додати всередину `describe('runJob')`:

```ts
  it('шар лягає на полотно після fit', async () => {
    const BLUE = { r: 0, g: 0, b: 255, a: 255 } as const;
    let final: RasterImage | null = null;
    const capturing: Codec = {
      canDecode: (mime) => mime === 'image/fake',
      canEncode: (format) => format === 'png',
      decode: async () => source,
      encode: async (img) => { final = img; return new Uint8Array(0); },
    };
    const job: Job = {
      ops: [
        { type: 'fit', width: 50, height: 50, mode: 'cover' },
        {
          type: 'composite',
          layers: [{ image: solidImage(10, 10, BLUE), x: 0.5, y: 0.5, scale: 1 }],
        },
      ],
      output: { format: 'png' },
    };
    await runJob(input, 'image/fake', job, { codec: capturing });

    expect(final).not.toBeNull();
    expect(final!.width).toBe(50);
    // Шар накриває полотно цілком (scale 1), тож центр має бути синім
    const i = (25 * 50 + 25) * 4;
    expect(final!.data[i + 2]).toBeGreaterThan(200);
    // Кут теж — інакше composite виконався до fit і його обрізало
    expect(final!.data[2]).toBeGreaterThan(200);
  });
```

- [ ] **Step 2: Запустити й переконатись, що падає**

Run: `pnpm vitest run packages/core/test/pipeline.test.ts -t шар`
Expected: FAIL — «Невідома операція: composite».

- [ ] **Step 3: Додати гілку**

У `packages/core/src/ops/pipeline.ts`: імпорт

```ts
import { composite } from './composite.js';
```

і в `applyOp`, перед `case 'upscale'`:

```ts
    case 'composite': return composite(img, op.layers);
```

- [ ] **Step 4: Запустити тести**

Run: `pnpm vitest run packages/core`
Expected: PASS.

- [ ] **Step 5: Закомітити**

```bash
pnpm typecheck && pnpm test
git add packages/core/src/ops/pipeline.ts packages/core/test/pipeline.test.ts
git commit -m "feat(core): composite у пайплайні"
```

---

## Task 8: декодування накладених у воркері

**Files:**
- Modify: `apps/web/src/lib/worker.ts` (константа `MAX_OVERLAY_SIDE`, метод `decodeOverlay`)
- Modify: `apps/web/src/lib/worker-api.ts` (інтерфейс `WorkerApi`)

**Interfaces:**
- Consumes: Task 6
- Produces: `WorkerApi.decodeOverlay(bytes: ArrayBuffer, mime: string):
  Promise<{ data: ArrayBuffer; width: number; height: number }>` — RGBA,
  зменшене до 2048 px по більшій стороні.

> Константа живе у `worker.ts`, а не в `worker-api.ts`, хоч логічно
> належала б до опису API. Причина технічна: `worker-api.ts` спавнить
> воркери через `new Worker(new URL('./worker.ts', …))`, і зустрічний
> імпорт замкнув би цикл — а заразом затягнув би код спавнення в сам
> воркер. Межа тут важить більше за симетрію.

- [ ] **Step 1: Оголосити метод в API**

У `apps/web/src/lib/worker-api.ts`, в інтерфейс `WorkerApi`:

```ts
  decodeOverlay(
    bytes: ArrayBuffer, mime: string,
  ): Promise<{ data: ArrayBuffer; width: number; height: number }>;
```

- [ ] **Step 2: Реалізувати у воркері**

У `apps/web/src/lib/worker.ts`, поруч з іншими константами модуля:

```ts
/**
 * Межа роздільності накладеного зображення.
 *
 * Чотири шари 4000×3000 у RGBA — це близько 190 МБ у вкладці, і жоден із
 * них не малюється в такому розмірі: наклейку видно на частку ширини
 * кадру. Дві тисячі лишають запас навіть для шару на всю ширину 4K.
 */
const MAX_OVERLAY_SIDE = 2048;
```

і в об'єкт `api`, після `preview`:

```ts
  /**
   * Декодує накладене зображення в RGBA й одразу зменшує до межі.
   *
   * Повертаємо саме пікселі, а не файл: шар лежить у Job, а Job має
   * лишатись придатним до structured clone. Кодувати назад у PNG лише
   * для того, щоб декодувати ще раз на кожен прогін, було б безглуздо.
   */
  async decodeOverlay(
    bytes: ArrayBuffer, mime: string,
  ): Promise<{ data: ArrayBuffer; width: number; height: number }> {
    const img = await codec.decode(new Uint8Array(bytes), mime);
    const capped = Math.max(img.width, img.height) > MAX_OVERLAY_SIDE
      ? fit(img, { width: MAX_OVERLAY_SIDE, height: MAX_OVERLAY_SIDE, mode: 'inside' })
      : img;
    const copy = new Uint8ClampedArray(capped.data);
    return Comlink.transfer(
      { data: copy.buffer, width: capped.width, height: capped.height },
      [copy.buffer],
    );
  },
```

`fit` уже імпортовано у `worker.ts` для `preview` — додаткового імпорту
не потрібно.

- [ ] **Step 3: Перевірити типи**

Run: `pnpm typecheck`
Expected: чисто.

- [ ] **Step 4: Закомітити**

```bash
pnpm test
git add apps/web/src/lib/worker.ts apps/web/src/lib/worker-api.ts
git commit -m "feat(web): декодування накладених зображень у воркері"
```

---

## Task 9: шари в стані віджета

**Files:**
- Modify: `apps/web/src/lib/worker-api.ts` (`WidgetState.layers`, `buildJob`)
- Test: `apps/web/test/buildjob.test.ts`

**Interfaces:**
- Consumes: Task 6, Task 8
- Produces: `WidgetState.layers: readonly UiLayer[]`, де

```ts
export interface UiLayer extends Layer {
  readonly id: number;
  readonly name: string;
  /** Прихований шар лишається в списку, але не потрапляє в Job. */
  readonly hidden: boolean;
  /** objectURL мініатюри для списку. */
  readonly thumb: string;
}
```

`buildJob` додає `{ type: 'composite', layers }` **останньою** операцією,
і лише коли видимих шарів більше нуля.

- [ ] **Step 1: Написати тест, що падає**

Додати в `apps/web/test/buildjob.test.ts`:

```ts
const px = (w: number, h: number) => ({
  data: new Uint8ClampedArray(w * h * 4), width: w, height: h,
});

const uiLayer = (over: Partial<UiLayer> = {}): UiLayer => ({
  id: 1, name: 'a.png', hidden: false, thumb: '',
  image: px(4, 4), x: 0.5, y: 0.5, scale: 0.3, ...over,
});

describe('шари', () => {
  it('без шарів операції немає', () => {
    expect(types(base)).toEqual(['fit']);
  });

  it('шари йдуть останніми, після fit', () => {
    expect(types({ ...base, layers: [uiLayer()] })).toEqual(['fit', 'composite']);
  });

  it('після збільшення й кадрування теж останні', () => {
    expect(types({ ...base, framing: 'smart', upscale: 2, layers: [uiLayer()] }))
      .toEqual(['smartCrop', 'upscale', 'fit', 'composite']);
  });

  it('приховані шари не потрапляють у Job', () => {
    expect(types({ ...base, layers: [uiLayer({ hidden: true })] })).toEqual(['fit']);
  });

  it('у Job не їдуть поля інтерфейсу', () => {
    const op = buildJob({ ...base, layers: [uiLayer()] }).ops
      .find((o) => o.type === 'composite') as { layers: readonly object[] };
    expect(Object.keys(op.layers[0]!).sort())
      .toEqual(['image', 'scale', 'x', 'y']);
  });
});
```

Додати `layers: [],` у літерал `base` і імпорт `type UiLayer`.

- [ ] **Step 2: Запустити й переконатись, що падає**

Run: `pnpm vitest run apps/web/test/buildjob.test.ts`
Expected: FAIL — `layers` немає в `WidgetState`.

- [ ] **Step 3: Розширити стан**

У `apps/web/src/lib/worker-api.ts`: імпортувати `Layer` з `@obrobka/core`,
додати інтерфейс `UiLayer` (як у блоці Interfaces вище) і поле в `WidgetState`:

```ts
  readonly layers: readonly UiLayer[];
```

- [ ] **Step 4: Додати операцію в `buildJob`**

Наприкінці `buildJob`, після `ops.push({ type: 'fit', … })`:

```ts
  // Шари — останні, вже по готовому полотну. Водяний знак має лягти на те
  // зображення, яке людина завантажить, і в те місце, яке вона бачила:
  // до fit його перемолов би ресемплер, до upscale — нейромережа
  // домальовувала б деталі логотипу, якого в оригіналі не було.
  //
  // У Job ідуть лише поля ядра: id, назва, мініатюра й ознака видимості
  // існують для списку в інтерфейсі й у пакеті нічого не означають.
  const visible = s.layers.filter((l) => !l.hidden);
  if (visible.length > 0) {
    ops.push({
      type: 'composite',
      layers: visible.map((l) => ({
        image: l.image, x: l.x, y: l.y, scale: l.scale,
        ...(l.rotation !== undefined ? { rotation: l.rotation } : {}),
        ...(l.opacity !== undefined ? { opacity: l.opacity } : {}),
        ...(l.blend !== undefined ? { blend: l.blend } : {}),
      })),
    });
  }
```

- [ ] **Step 5: Запустити тести й полагодити ініціалізатор**

Run: `pnpm vitest run apps/web/test/buildjob.test.ts`
Далі `pnpm typecheck` покаже брак `layers` у `Widget.svelte` —
додати `layers: [],` в `$state<WidgetState>`.

- [ ] **Step 6: Закомітити**

```bash
pnpm typecheck && pnpm test
git add apps/web/src/lib/worker-api.ts apps/web/test/buildjob.test.ts apps/web/src/components/Widget.svelte
git commit -m "feat(web): шари в стані віджета"
```

---

## Task 10: `LayerPanel.svelte`

**Files:**
- Create: `apps/web/src/components/LayerPanel.svelte`
- Modify: `apps/web/src/lib/i18n.ts` (блок `layers` у `Dict` і обох словниках)

**Interfaces:**
- Consumes: Task 9
- Produces: компонент із пропсами
  `{ layers: readonly UiLayer[]; selected: number | null; t: Dict;
     onadd: (files: readonly File[]) => void;
     onpatch: (id: number, patch: Partial<UiLayer>) => void;
     onremove: (id: number) => void;
     onmove: (id: number, delta: -1 | 1) => void;
     onselect: (id: number | null) => void }`.

- [ ] **Step 1: Додати рядки в словник**

В інтерфейс `Dict`, поруч із блоком `crop`:

```ts
  layers: {
    section: string;
    add: string;
    empty: string;
    up: string;
    down: string;
    remove: string;
    hide: string;
    show: string;
    opacity: string;
    size: string;
    rotation: string;
    blend: string;
    modes: Record<string, string>;
  };
```

Український словник:

```ts
  layers: {
    section: 'Шари',
    add: 'Додати зображення',
    empty: 'Покладіть картинку поверх — логотип, наклейку, водяний знак.',
    up: 'Вище',
    down: 'Нижче',
    remove: 'Прибрати',
    hide: 'Сховати',
    show: 'Показати',
    opacity: 'Прозорість',
    size: 'Розмір',
    rotation: 'Поворот',
    blend: 'Накладання',
    modes: {
      normal: 'Звичайне',
      multiply: 'Множення',
      screen: 'Освітлення',
      overlay: 'Перекриття',
      darken: 'Темніше',
      lighten: 'Світліше',
    },
  },
```

Англійський:

```ts
  layers: {
    section: 'Layers',
    add: 'Add image',
    empty: 'Put an image on top — a logo, a sticker, a watermark.',
    up: 'Up',
    down: 'Down',
    remove: 'Remove',
    hide: 'Hide',
    show: 'Show',
    opacity: 'Opacity',
    size: 'Size',
    rotation: 'Rotation',
    blend: 'Blend',
    modes: {
      normal: 'Normal',
      multiply: 'Multiply',
      screen: 'Screen',
      overlay: 'Overlay',
      darken: 'Darken',
      lighten: 'Lighten',
    },
  },
```

- [ ] **Step 2: Перевірити парність**

Run: `pnpm vitest run apps/web/test/i18n.test.ts`
Expected: PASS.

- [ ] **Step 3: Створити компонент**

`apps/web/src/components/LayerPanel.svelte`:

```svelte
<script lang="ts">
  import type { BlendMode } from '@obrobka/core';
  import type { UiLayer } from '../lib/worker-api.js';
  import type { Dict } from '../lib/i18n.js';

  /**
   * Список шарів і налаштування обраного.
   *
   * Порядок списку — зверху найвищий шар, як у будь-якому редакторі,
   * тобто зворотний до порядку масиву, який задає накладання знизу вгору.
   */
  let { layers, selected, t, onadd, onpatch, onremove, onmove, onselect }: {
    layers: readonly UiLayer[];
    selected: number | null;
    t: Dict;
    onadd: (files: readonly File[]) => void;
    onpatch: (id: number, patch: Partial<UiLayer>) => void;
    onremove: (id: number) => void;
    onmove: (id: number, delta: -1 | 1) => void;
    onselect: (id: number | null) => void;
  } = $props();

  const BLENDS: BlendMode[] = ['normal', 'multiply', 'screen', 'overlay', 'darken', 'lighten'];
  const top = $derived([...layers].reverse());
  const current = $derived(layers.find((l) => l.id === selected) ?? null);
</script>

<fieldset class="layers">
  <legend>{t.layers.section}</legend>

  <label class="add btn">
    {t.layers.add}
    <input
      type="file"
      multiple
      accept="image/png,image/jpeg,image/webp,image/avif"
      data-testid="layer-add"
      onchange={(e) => {
        const files = Array.from(e.currentTarget.files ?? []);
        e.currentTarget.value = '';
        if (files.length > 0) onadd(files);
      }}
    />
  </label>

  {#if layers.length === 0}
    <p class="empty">{t.layers.empty}</p>
  {:else}
    <ul class="list">
      {#each top as l (l.id)}
        <li class="item" class:on={l.id === selected}>
          <button
            type="button"
            class="pick"
            data-testid={`layer-${l.id}`}
            onclick={() => onselect(l.id === selected ? null : l.id)}
          >
            <img class="thumb checker" src={l.thumb} alt="" />
            <span class="name">{l.name}</span>
          </button>
          <button type="button" class="icon" title={l.hidden ? t.layers.show : t.layers.hide}
                  aria-label={l.hidden ? t.layers.show : t.layers.hide}
                  data-testid={`layer-hide-${l.id}`}
                  onclick={() => onpatch(l.id, { hidden: !l.hidden })}
          >{l.hidden ? '○' : '●'}</button>
          <button type="button" class="icon" title={t.layers.up} aria-label={t.layers.up}
                  onclick={() => onmove(l.id, 1)}>↑</button>
          <button type="button" class="icon" title={t.layers.down} aria-label={t.layers.down}
                  onclick={() => onmove(l.id, -1)}>↓</button>
          <button type="button" class="icon" title={t.layers.remove} aria-label={t.layers.remove}
                  data-testid={`layer-remove-${l.id}`}
                  onclick={() => onremove(l.id)}>✕</button>
        </li>
      {/each}
    </ul>
  {/if}

  {#if current !== null}
    <div class="controls">
      <label class="field">
        <span>{t.layers.size} <em>{Math.round(current.scale * 100)} %</em></span>
        <input type="range" min="2" max="200" value={Math.round(current.scale * 100)}
               data-testid="layer-scale"
               oninput={(e) => onpatch(current.id, { scale: e.currentTarget.valueAsNumber / 100 })} />
      </label>
      <label class="field">
        <span>{t.layers.opacity} <em>{Math.round((current.opacity ?? 1) * 100)} %</em></span>
        <input type="range" min="0" max="100" value={Math.round((current.opacity ?? 1) * 100)}
               data-testid="layer-opacity"
               oninput={(e) => onpatch(current.id, { opacity: e.currentTarget.valueAsNumber / 100 })} />
      </label>
      <label class="field">
        <span>{t.layers.rotation} <em>{Math.round(current.rotation ?? 0)}°</em></span>
        <input type="range" min="-180" max="180" value={Math.round(current.rotation ?? 0)}
               data-testid="layer-rotation"
               oninput={(e) => onpatch(current.id, { rotation: e.currentTarget.valueAsNumber })} />
      </label>
      <label class="field">
        <span>{t.layers.blend}</span>
        <select value={current.blend ?? 'normal'} data-testid="layer-blend"
                onchange={(e) => onpatch(current.id, { blend: e.currentTarget.value as BlendMode })}>
          {#each BLENDS as m (m)}
            <option value={m}>{t.layers.modes[m]}</option>
          {/each}
        </select>
      </label>
    </div>
  {/if}
</fieldset>

<style>
  .layers { border: 0; padding: 0; margin: 0; min-width: 0; display: grid; gap: 0.6rem; }
  .layers legend {
    padding: 0; margin-bottom: 0.5rem;
    font-size: 0.78rem; font-weight: 600;
    letter-spacing: 0.04em; text-transform: uppercase; color: var(--fg-muted);
  }
  .add { position: relative; overflow: hidden; justify-self: start; }
  .add input { position: absolute; inset: 0; opacity: 0; cursor: pointer; }
  .empty { font-size: 0.8rem; color: var(--fg-faint); max-width: 44ch; }

  .list { list-style: none; padding: 0; margin: 0; display: grid; gap: 0.35rem; }
  .item {
    display: flex; align-items: center; gap: 0.35rem;
    padding: 0.3rem 0.4rem;
    border: 1px solid var(--line); border-radius: var(--r-sm);
    background: var(--bg-sunken);
  }
  .item.on { border-color: var(--accent); }
  .pick {
    display: flex; align-items: center; gap: 0.5rem;
    flex: 1; min-width: 0;
    border: 0; background: none; cursor: pointer; text-align: start;
  }
  .thumb {
    width: 2rem; height: 2rem; object-fit: contain;
    border-radius: 4px; --checker-size: 8px; flex: none;
  }
  .name {
    font-size: 0.8rem; overflow: hidden;
    text-overflow: ellipsis; white-space: nowrap;
  }
  .icon {
    width: 1.7rem; height: 1.7rem; flex: none;
    display: grid; place-items: center;
    border: 1px solid var(--line); border-radius: var(--r-sm);
    background: var(--bg-raised); cursor: pointer; line-height: 1;
    font-size: 0.75rem;
  }
  .icon:hover { border-color: var(--accent); }

  .controls {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(9rem, 1fr));
    gap: 0.8rem;
  }
  .field { display: grid; gap: 0.35rem; }
  .field > span {
    font-size: 0.78rem; font-weight: 600;
    letter-spacing: 0.04em; text-transform: uppercase; color: var(--fg-muted);
  }
  .field > span em {
    font-style: normal; font-family: var(--font-mono); color: var(--accent-text);
  }
  .field input[type='range'] { accent-color: var(--accent-bg); width: 100%; }
  .field select {
    padding: 0.5rem 0.6rem;
    border: 1px solid var(--line-strong); border-radius: var(--r-sm);
    background: var(--bg-sunken);
  }
</style>
```

- [ ] **Step 4: Перевірити відсутність вшитої кирилиці**

Run: `pnpm vitest run apps/web/test/i18n.test.ts`
Expected: PASS.

- [ ] **Step 5: Закомітити**

```bash
pnpm typecheck && pnpm test
git add apps/web/src/components/LayerPanel.svelte apps/web/src/lib/i18n.ts
git commit -m "feat(web): панель шарів"
```

---

## Task 11: під'єднати шари до віджета

**Files:**
- Modify: `apps/web/src/components/Widget.svelte`
- Test: `apps/web/e2e/layers.spec.ts` (створити)

**Interfaces:**
- Consumes: Task 8, Task 9, Task 10
- Produces: робочі шари в інтерфейсі; `addLayers`, `patchLayer`,
  `removeLayer`, `moveLayer` у `Widget.svelte`.

- [ ] **Step 1: Написати e2e-тест, що падає**

Створити `apps/web/e2e/layers.spec.ts`:

```ts
import { test, expect, type Page } from '@playwright/test';
import { join } from 'node:path';

const SPHERE = join(import.meta.dirname, 'sphere.png');
const FIXTURE = join(import.meta.dirname, 'fixture.png');

async function ready(page: Page): Promise<void> {
  await page.goto('/');
  await expect(page.locator('input[type=file][data-ready="true"]')).toBeAttached({ timeout: 60_000 });
  await page.setInputFiles('input[type=file]', SPHERE);
  await expect(page.getByTestId('result')).toBeVisible({ timeout: 60_000 });
}

/** Середній піксель результату — щоб побачити, що шар справді ліг. */
async function centre(page: Page): Promise<{ r: number; g: number; b: number }> {
  return page.getByTestId('result').evaluate(async (el) => {
    const img = el as HTMLImageElement;
    await img.decode();
    const c = new OffscreenCanvas(img.naturalWidth, img.naturalHeight);
    const ctx = c.getContext('2d')!;
    ctx.drawImage(img, 0, 0);
    const d = ctx.getImageData(Math.floor(img.naturalWidth / 2), Math.floor(img.naturalHeight / 2), 1, 1).data;
    return { r: d[0]!, g: d[1]!, b: d[2]! };
  });
}

test('накладене зображення видно в результаті', async ({ page }) => {
  await ready(page);
  const before = await centre(page);
  await page.setInputFiles('[data-testid="layer-add"]', FIXTURE);
  await expect(page.locator('[data-testid^="layer-"][data-testid$="0"]').first())
    .toBeVisible({ timeout: 30_000 });
  await expect.poll(async () => JSON.stringify(await centre(page)), { timeout: 60_000 })
    .not.toBe(JSON.stringify(before));
  await expect(page.locator('.error')).toHaveCount(0);
});

test('прихований шар зникає з результату', async ({ page }) => {
  await ready(page);
  const clean = JSON.stringify(await centre(page));
  await page.setInputFiles('[data-testid="layer-add"]', FIXTURE);
  await expect.poll(async () => JSON.stringify(await centre(page)), { timeout: 60_000 })
    .not.toBe(clean);
  await page.locator('[data-testid^="layer-hide-"]').first().click();
  await expect.poll(async () => JSON.stringify(await centre(page)), { timeout: 60_000 })
    .toBe(clean);
});

test('вилучення шару прибирає його зі списку', async ({ page }) => {
  await ready(page);
  await page.setInputFiles('[data-testid="layer-add"]', FIXTURE);
  const remove = page.locator('[data-testid^="layer-remove-"]').first();
  await expect(remove).toBeVisible({ timeout: 30_000 });
  await remove.click();
  await expect(page.locator('[data-testid^="layer-remove-"]')).toHaveCount(0);
});
```

- [ ] **Step 2: Запустити й переконатись, що падає**

```bash
pnpm --filter @obrobka/web build && pnpm exec playwright test apps/web/e2e/layers.spec.ts
```
Expected: FAIL — `[data-testid="layer-add"]` не існує.

- [ ] **Step 3: Додати логіку шарів у віджет**

У `apps/web/src/components/Widget.svelte`, імпорти:

```ts
  import LayerPanel from './LayerPanel.svelte';
```

і додати `type UiLayer` до наявного імпорту з `../lib/worker-api.js`.

У блок стану, поруч з іншими `$state`:

```ts
  let selectedLayer = $state<number | null>(null);
  let nextLayerId = 0;

  /**
   * Приймає накладені зображення.
   *
   * Декодування йде у воркер: там уже є кодек із HEIC, а головний потік
   * не має ставати на 4000×3000 PNG. Мініатюра — окремий objectURL,
   * бо в списку показувати нічого іншого.
   */
  async function addLayers(files: readonly File[]): Promise<void> {
    for (const file of files) {
      const bytes = new Uint8Array(await file.arrayBuffer());
      const mime = sniffMime(bytes);
      if (mime === null) { error = t.errUnknownFormat; continue; }
      try {
        const copy = bytes.slice();
        const decoded = await getWorker().decodeOverlay(copy.buffer, mime);
        const id = nextLayerId++;
        state.layers = [...state.layers, {
          id,
          name: file.name,
          hidden: false,
          thumb: URL.createObjectURL(new Blob([bytes.slice()], { type: mime })),
          image: {
            data: new Uint8ClampedArray(decoded.data),
            width: decoded.width,
            height: decoded.height,
          },
          x: 0.5, y: 0.5, scale: 0.35,
        }];
        selectedLayer = id;
      } catch (e) {
        error = e instanceof Error ? e.message : t.errProcess;
      }
    }
    await process();
  }

  function patchLayer(id: number, patch: Partial<UiLayer>): void {
    state.layers = state.layers.map((l) => (l.id === id ? { ...l, ...patch } : l));
    void process();
  }

  function removeLayer(id: number): void {
    const gone = state.layers.find((l) => l.id === id);
    if (gone !== undefined && gone.thumb !== '') URL.revokeObjectURL(gone.thumb);
    state.layers = state.layers.filter((l) => l.id !== id);
    if (selectedLayer === id) selectedLayer = null;
    void process();
  }

  /** Порядок масиву — порядок накладання, тож «вище» це до кінця. */
  function moveLayer(id: number, delta: -1 | 1): void {
    const from = state.layers.findIndex((l) => l.id === id);
    const to = from + delta;
    if (from < 0 || to < 0 || to >= state.layers.length) return;
    const next = [...state.layers];
    const [moved] = next.splice(from, 1);
    next.splice(to, 0, moved!);
    state.layers = next;
    void process();
  }
```

У розмітку, одразу після блоку з `TierPicker` і перед `{#if error !== ''}`:

```svelte
  <LayerPanel
    layers={state.layers}
    selected={selectedLayer}
    {t}
    onadd={(files) => void addLayers(files)}
    onpatch={patchLayer}
    onremove={removeLayer}
    onmove={moveLayer}
    onselect={(id) => { selectedLayer = id; }}
  />
```

- [ ] **Step 4: Скидати шари разом із файлом**

У `resetSingle()` шари не чіпаємо — вони належать сеансу, а не файлу.
Але мініатюри треба відкликати при виході. Додати в кінець `<script>`:

```ts
  // Мініатюри — objectURL: без відкликання вкладка тримала б їх до
  // перезавантаження сторінки.
  $effect(() => () => {
    for (const l of state.layers) if (l.thumb !== '') URL.revokeObjectURL(l.thumb);
  });
```

- [ ] **Step 5: Розширити перевірку розкладки на нову панель**

Панель шарів малюється завжди, тож у `apps/web/e2e/layout.spec.ts`
у тіло тесту `${view.name}: підказки не лежать на кнопках`, після
останнього `expect(await overlaps(page)).toEqual([]);` додати:

```ts
    // Панель шарів приносить свої підказки й свій список кнопок
    await page.setInputFiles('[data-testid="layer-add"]', FIXTURE);
    await expect(page.locator('[data-testid^="layer-remove-"]'))
      .toHaveCount(1, { timeout: 30_000 });
    expect(await overlaps(page)).toEqual([]);
```

і вгорі файлу:

```ts
import { join } from 'node:path';

const FIXTURE = join(import.meta.dirname, 'fixture.png');
```

- [ ] **Step 6: Запустити e2e**

```bash
pnpm --filter @obrobka/web build
pnpm exec playwright test apps/web/e2e/layers.spec.ts apps/web/e2e/layout.spec.ts
```
Expected: PASS.

- [ ] **Step 7: Закомітити**

```bash
pnpm typecheck && pnpm test
git add apps/web/src/components/Widget.svelte apps/web/e2e/layers.spec.ts apps/web/e2e/layout.spec.ts
git commit -m "feat(web): шари у віджеті"
```

---

## Task 12: тягнення шару по сцені

**Files:**
- Modify: `apps/web/src/components/Stage.svelte`
- Modify: `apps/web/src/components/Widget.svelte`
- Test: `apps/web/e2e/layers.spec.ts`

**Interfaces:**
- Consumes: Task 4, Task 11
- Produces: `Stage` отримує додаткові пропси
  `{ layers?: readonly UiLayer[]; selectedLayer?: number | null;
     onlayermove?: (id: number, x: number, y: number) => void }`.
  Коли шар обрано, тягнення рухає його, а не кадр.

- [ ] **Step 1: Написати тест, що падає**

Додати в `apps/web/e2e/layers.spec.ts`:

```ts
test('тягнення рухає обраний шар, а не кадр', async ({ page }) => {
  await ready(page);
  await page.setInputFiles('[data-testid="layer-add"]', FIXTURE);
  await expect(page.getByTestId('layer-ghost-0')).toBeVisible({ timeout: 30_000 });

  const before = (await page.getByTestId('layer-ghost-0').boundingBox())!;
  const stage = (await page.getByTestId('stage').boundingBox())!;
  await page.mouse.move(before.x + before.width / 2, before.y + before.height / 2);
  await page.mouse.down();
  await page.mouse.move(stage.x + stage.width - 12, stage.y + 12, { steps: 8 });
  await page.mouse.up();

  const after = (await page.getByTestId('layer-ghost-0').boundingBox())!;
  expect(after.x).toBeGreaterThan(before.x + 10);
  // Прив'язка кадру не мала змінитись: тягнули шар
  await expect(page.getByTestId('anchor-center')).toHaveAttribute('aria-checked', 'true');
});
```

- [ ] **Step 2: Запустити й переконатись, що падає**

```bash
pnpm --filter @obrobka/web build && pnpm exec playwright test apps/web/e2e/layers.spec.ts -g тягнення
```
Expected: FAIL — `layer-ghost-0` не існує.

- [ ] **Step 3: Показати шари на сцені**

У `apps/web/src/components/Stage.svelte` розширити пропси:

```ts
    layers?: readonly UiLayer[];
    selectedLayer?: number | null;
    onlayermove?: (id: number, x: number, y: number) => void;
```

зі значеннями за замовчуванням `layers = []`, `selectedLayer = null`,
`onlayermove = undefined`, та імпортом `import type { UiLayer } from '../lib/worker-api.js';`.

У розмітку, всередині `.frame` після `<img>`:

```svelte
    {#each layers as l (l.id)}
      {#if !l.hidden}
        <img
          class="ghost"
          class:on={l.id === selectedLayer}
          src={l.thumb}
          alt=""
          draggable="false"
          data-testid={`layer-ghost-${l.id}`}
          style={`left: ${l.x * 100}%; top: ${l.y * 100}%; width: ${l.scale * 100}%;
                  transform: translate(-50%, -50%) rotate(${l.rotation ?? 0}deg);
                  opacity: ${l.opacity ?? 1};`}
        />
      {/if}
    {/each}
```

і стилі:

```css
  .ghost {
    position: absolute;
    height: auto;
    pointer-events: none;
    user-select: none;
  }
  /* Обраний шар видно, що обраний, — інакше незрозуміло, що рухатиметься */
  .ghost.on { outline: 1px dashed var(--accent); outline-offset: 2px; }
```

- [ ] **Step 4: Перемкнути жест на шар, коли той обрано**

У `Stage.svelte`, на початку `onPointerDown`, замінити перевірку:

```ts
  /** Обраний шар перехоплює жест: інакше його не було б чим рухати. */
  const layerDrag = $derived(
    selectedLayer !== null && layers.some((l) => l.id === selectedLayer && !l.hidden),
  );

  let startLx = 0;
  let startLy = 0;

  function onPointerDown(e: PointerEvent): void {
    if (!movable && !layerDrag) return;
    (e.currentTarget as HTMLElement).setPointerCapture(e.pointerId);
    pointers.set(e.pointerId, { x: e.clientX, y: e.clientY });
    dragging = true;
    startFx = fx; startFy = fy; startZ = z;
    startX = e.clientX; startY = e.clientY;
    const picked = layers.find((l) => l.id === selectedLayer);
    startLx = picked?.x ?? 0.5;
    startLy = picked?.y ?? 0.5;
    if (pointers.size === 2) startSpread = spread();
  }
```

і на початку однопальцевої гілки `onPointerMove`, перед обчисленням `sign`:

```ts
    if (layerDrag && selectedLayer !== null) {
      const box = frame.getBoundingClientRect();
      onlayermove?.(
        selectedLayer,
        clamp01(startLx + (e.clientX - startX) / box.width),
        clamp01(startLy + (e.clientY - startY) / box.height),
      );
      return;
    }
```

У `onPointerUp` не викликати `commit()`, коли жест був про шар:

```ts
  function onPointerUp(e: PointerEvent): void {
    pointers.delete(e.pointerId);
    if (pointers.size > 0) return;
    dragging = false;
    if (!layerDrag) commit();
  }
```

- [ ] **Step 5: Передати пропси з віджета**

У `Widget.svelte`, до виклику `<Stage …/>` додати:

```svelte
        layers={state.layers}
        selectedLayer={selectedLayer}
        onlayermove={(id, x, y) => patchLayer(id, { x, y })}
```

- [ ] **Step 6: Запустити e2e**

```bash
pnpm --filter @obrobka/web build
pnpm exec playwright test apps/web/e2e/layers.spec.ts apps/web/e2e/framing.spec.ts
```
Expected: PASS.

- [ ] **Step 7: Закомітити**

```bash
pnpm typecheck && pnpm test
git add apps/web/src/components/Stage.svelte apps/web/src/components/Widget.svelte apps/web/e2e/layers.spec.ts
git commit -m "feat(web): тягнення шару по сцені"
```

---

## Task 13: MCP-інструмент `composite_images`

**Files:**
- Modify: `apps/mcp/src/tools.ts` (функція `compositeImages`)
- Modify: `apps/mcp/src/index.ts` (реєстрація інструмента)
- Test: `apps/mcp/test/tools.test.ts`

**Interfaces:**
- Consumes: Task 6, Task 7
- Produces:

```ts
export interface CompositeArgs {
  readonly input: string;
  readonly output: string;
  readonly overlays: readonly {
    readonly path: string;
    readonly x?: number;
    readonly y?: number;
    readonly scale?: number;
    readonly rotation?: number;
    readonly opacity?: number;
    readonly blend?: BlendMode;
  }[];
  readonly format?: OutputFormat;
  readonly quality?: number | undefined;
}
export async function compositeImages(args: CompositeArgs): Promise<ToolResult>;
```

- [ ] **Step 1: Написати тест, що падає**

Файл уже має `dir` і `srcPath` із `beforeAll` (200×100, суцільний
`rgb(200, 30, 40)`) і імпортує `nodeCodec`. Дописати в кінець:

```ts
/** Однотонний PNG у тимчасовій теці — накладене зображення для перевірок. */
async function solidPng(
  name: string, w: number, h: number, r: number, g: number, b: number,
): Promise<string> {
  const data = new Uint8ClampedArray(w * h * 4);
  for (let i = 0; i < data.length; i += 4) {
    data[i] = r; data[i + 1] = g; data[i + 2] = b; data[i + 3] = 255;
  }
  const path = join(dir, name);
  await writeFile(path, await nodeCodec.encode({ data, width: w, height: h }, { format: 'png' }));
  return path;
}

describe('compositeImages', () => {
  it('кладе накладене в центр і не чіпає кути', async () => {
    const over = await solidPng('over.png', 20, 20, 0, 0, 255);
    const out = join(dir, 'composited.png');
    const res = await compositeImages({
      input: srcPath, output: out,
      overlays: [{ path: over, x: 0.5, y: 0.5, scale: 0.4 }],
    });
    expect(res.width).toBe(200);
    expect(res.height).toBe(100);

    const img = await nodeCodec.decode(await readFile(out), 'image/png');
    const at = (x: number, y: number): number[] => {
      const i = (y * img.width + x) * 4;
      return [img.data[i]!, img.data[i + 1]!, img.data[i + 2]!];
    };
    expect(at(100, 50)[2]).toBeGreaterThan(200);
    expect(at(2, 2)[0]).toBeGreaterThan(180);
  });

  it('прозорість домішує основу', async () => {
    const over = await solidPng('over-half.png', 20, 20, 0, 0, 255);
    const out = join(dir, 'half.png');
    await compositeImages({
      input: srcPath, output: out,
      overlays: [{ path: over, x: 0.5, y: 0.5, scale: 0.4, opacity: 0.5 }],
    });
    const img = await nodeCodec.decode(await readFile(out), 'image/png');
    const i = (50 * img.width + 100) * 4;
    // Половина від синього поверх rgb(200,30,40): синій десь посередині
    expect(img.data[i + 2]!).toBeGreaterThan(120);
    expect(img.data[i + 2]!).toBeLessThan(180);
  });

  it('порожній список — помилка, а не тихий прохід', async () => {
    await expect(compositeImages({
      input: srcPath, output: join(dir, 'never.png'), overlays: [],
    })).rejects.toThrow(/хоча б одне/);
  });
});
```

Додати `compositeImages` до імпорту з `../src/tools.js` і `readFile`
до імпорту з `node:fs/promises`.

- [ ] **Step 2: Запустити й переконатись, що падає**

Run: `pnpm vitest run apps/mcp/test/tools.test.ts -t composite`
Expected: FAIL — `compositeImages` не експортується.

- [ ] **Step 3: Реалізувати інструмент**

У `apps/mcp/src/tools.ts`, наприкінці файлу:

```ts
export interface CompositeOverlay {
  readonly path: string;
  /** Центр як частка ширини основи. Типово 0.5. */
  readonly x?: number;
  /** Центр як частка висоти основи. Типово 0.5. */
  readonly y?: number;
  /** Ширина як частка ширини основи. Типово 0.35. */
  readonly scale?: number;
  readonly rotation?: number;
  readonly opacity?: number;
  readonly blend?: BlendMode;
}

export interface CompositeArgs {
  readonly input: string;
  readonly output: string;
  readonly overlays: readonly CompositeOverlay[];
  readonly format?: OutputFormat;
  readonly quality?: number | undefined;
}

/**
 * Накладає зображення поверх іншого.
 *
 * Геометрія нормалізована, тож один виклик із тими самими числами дасть
 * той самий кадр на файлах різного розміру — це і є випадок водяного
 * знака на цілій теці.
 */
export async function compositeImages(args: CompositeArgs): Promise<ToolResult> {
  if (args.overlays.length === 0) {
    throw new Error('Потрібне хоча б одне накладене зображення');
  }
  const layers: Layer[] = [];
  for (const o of args.overlays) {
    const { bytes, mime } = await readImage(o.path);
    layers.push({
      image: await codec.decode(bytes, mime),
      x: o.x ?? 0.5,
      y: o.y ?? 0.5,
      scale: o.scale ?? 0.35,
      ...(o.rotation !== undefined ? { rotation: o.rotation } : {}),
      ...(o.opacity !== undefined ? { opacity: o.opacity } : {}),
      ...(o.blend !== undefined ? { blend: o.blend } : {}),
    });
  }
  const format = args.format ?? 'png';
  return runAndReport(args.input, args.output, {
    ops: [{ type: 'composite', layers }],
    output: outputOf(format, args.quality),
  });
}
```

Додати `BlendMode` і `Layer` до імпорту з `@obrobka/core` угорі файлу.

- [ ] **Step 4: Зареєструвати інструмент**

У `apps/mcp/src/index.ts` додати `compositeImages` до імпорту з `./tools.js`
і зареєструвати після `smart_crop`:

```ts
  const BLEND = z.enum(['normal', 'multiply', 'screen', 'overlay', 'darken', 'lighten']);

  server.registerTool('composite_images', {
    description:
      'Накладає одне або кілька зображень поверх основи — логотип, наклейку, ' +
      'водяний знак. Геометрія задається частками сторін основи, а не пікселями: ' +
      'ті самі числа дадуть той самий кадр на файлах будь-якого розміру, ' +
      'тож один набір значень годиться для цілої теки.',
    inputSchema: z.object({
      input: z.string().describe('Абсолютний шлях до основи'),
      output: z.string().describe('Абсолютний шлях для запису результату'),
      overlays: z.array(z.object({
        path: z.string().describe('Абсолютний шлях до накладеного зображення'),
        x: z.number().min(0).max(1).optional()
          .describe('Центр як частка ширини основи. Типово 0.5.'),
        y: z.number().min(0).max(1).optional()
          .describe('Центр як частка висоти основи. Типово 0.5.'),
        scale: z.number().positive().max(4).optional()
          .describe('Ширина як частка ширини основи. Висота — з власного '
            + 'співвідношення. Типово 0.35.'),
        rotation: z.number().min(-360).max(360).optional()
          .describe('Градуси за годинниковою стрілкою. Типово 0.'),
        opacity: z.number().min(0).max(1).optional().describe('0..1. Типово 1.'),
        blend: BLEND.optional().describe('Режим накладання. Типово normal.'),
      })).min(1).describe('Порядок списку — порядок накладання: перший лежить найнижче'),
      format: FORMAT.optional().describe('Формат результату. Типово png.'),
      quality: z.number().int().min(1).max(100).optional().describe('Якість 1..100'),
    }),
    outputSchema: RESULT,
  }, async (args) => report(await compositeImages(args)));
```

- [ ] **Step 5: Запустити тести**

Run: `pnpm vitest run apps/mcp`
Expected: PASS.

- [ ] **Step 6: Закомітити**

```bash
pnpm typecheck && pnpm test
git add apps/mcp/src/tools.ts apps/mcp/src/index.ts apps/mcp/test/tools.test.ts
git commit -m "feat(mcp): інструмент composite_images"
```

---

## Task 14: документація

**Files:**
- Modify: `README.md` (таблиця можливостей, перелік інструментів MCP)
- Modify: `docs/superpowers/plans/2026-08-23-obrobka-m6.md` (розділ «Що вийшло»)

**Interfaces:**
- Consumes: усі попередні задачі
- Produces: нічого коду

- [ ] **Step 1: Оновити таблицю можливостей у README**

У таблицю «What it does» додати два рядки:

```markdown
| **Layers** | images composited on top — logo, sticker, watermark; move, scale, rotate, blend |
| **Framing by hand** | drag the image to move the crop, wheel or pinch to zoom |
```

- [ ] **Step 2: Оновити перелік інструментів MCP**

У README замінити «Gives an agent eight tools» на «nine tools» і додати
`composite_images` у перелік.

- [ ] **Step 3: Виміряти й дописати підсумок**

У репозиторії заведено, що документація несе числа, а не враження.
Зміряти три речі й записати їх у розділ «Що вийшло» наприкінці цього файлу:

```bash
# 1. Скільки коштує composite на типовому кадрі.
node --input-type=module -e "
import { composite } from './packages/core/dist/ops/composite.js';
const img = (w, h) => ({ data: new Uint8ClampedArray(w * h * 4).fill(200), width: w, height: h });
const base = img(1920, 1080);
const layer = { image: img(512, 512), x: 0.85, y: 0.9, scale: 0.15 };
for (let i = 0; i < 3; i++) composite(base, [layer]);
const t = performance.now();
for (let i = 0; i < 10; i++) composite(base, [layer]);
console.log('composite 1920×1080 + шар 15 %:', ((performance.now() - t) / 10).toFixed(1), 'мс');
const rot = { ...layer, rotation: 30 };
const t2 = performance.now();
for (let i = 0; i < 10; i++) composite(base, [rot]);
console.log('те саме з поворотом 30°:', ((performance.now() - t2) / 10).toFixed(1), 'мс');
"
```

```bash
# 2. Скільки прогонів дає одне тягнення — з тесту в framing.spec.ts.
pnpm exec playwright test apps/web/e2e/framing.spec.ts -g щокадру --reporter=list
```

```bash
# 3. Наскільки виріс бандл острівця.
pnpm --filter @obrobka/web build && ls -la apps/web/dist/_astro/Widget.*.js
```

Записати всі три у вигляді таблиці. Якщо `composite` із поворотом
виявиться дорожчим за 100 мс на 1920×1080 — зафіксувати це як відоме
обмеження, а не ховати.

- [ ] **Step 4: Повний прогін**

```bash
pnpm typecheck && pnpm test
pnpm --filter @obrobka/web build && pnpm exec playwright test
```
Expected: усе зелене.

- [ ] **Step 5: Закомітити**

```bash
git add README.md docs/superpowers/plans/2026-08-23-obrobka-m6.md
git commit -m "docs: шари й пряме кадрування в README"
```

---

# Що вийшло

Виконано повністю. 348 модульних тестів, 64 браузерні — зелені.

## Скільки коштує накладання

Заміряно на полотні 1920×1080, десять прогонів після трьох холостих:

| Що | Час | З них resample |
|---|---|---|
| копія полотна без шарів | 1,4 мс | — |
| шар на 15 % ширини (джерело 512 px) | 10,9 мс | 3,4 мс |
| той самий шар, поворот 30° | 10,8 мс | 3,4 мс |
| шар на всю ширину (джерело 2048 px) | 214,1 мс | 103,1 мс |

Два висновки.

**Поворот безкоштовний.** 10,8 проти 10,9 мс — різниця в межах шуму.
Це прямий наслідок рішення «спершу масштаб, потім поворот»: обертати
доводиться вже зменшений шар, а не оригінал. Якби порядок був
зворотний, поворот ішов би по 512², і вартість була б зовсім інша.

**Дорогий випадок — не накладання, а масштабування.** З 214 мс половина
йде на resample 2048→1920. Це ціна якості: усереднення по площі проти
білінійної вибірки. Для наклейки на частку кадру — типового випадку —
загальна вартість 11 мс, тобто непомітна. Межа в 2048 px тут і рятує:
без неї той самий шар із 4000 px коштував би ще вчетверо більше.

## Скільки прогонів дає одне тягнення

Тягнення на 25 кроків миші — **не більше двох** прогонів пайплайна
(перевіряє `apps/web/e2e/framing.spec.ts`). Живий відгук іде CSS-ом,
у воркер летить лише завершений жест.

## Вага острівця

| | Розмір |
|---|---|
| `Widget.*.js` | 53,8 КБ |
| `Widget.*.css` | 20,2 КБ |

## Три пастки, що коштували найбільше

**Стилі компонента не потрапляли у збірку.** Компонент, використаний
лише всередині `{#if}`, опинявся в JS-чанку, і Rollup викидав його
CSS-модуль — рівно те, що колись зловило панель EXIF. Сцена приїжджала
голим HTML: без рамки, без шахівниці й, найгірше, без `touch-action:
none`, тобто на телефоні палець гортав сторінку замість кадру. Перевірено
експериментом на тій самій збірці: досить додати безумовний виклик
компонента — і стилі з'являються. Тепер сцена рендериться завжди, а
видимістю керує `hidden`.

**Проксі Svelte не переживає structured clone.** Стан віджета
реактивний, тож `{ fx, fy }`, покладені в нього, стають проксі — і Job
не доїжджав до воркера, а падав із «could not be cloned». Те саме
чекало на шари, де проксями стають і сам шар, і його `RasterImage`, і
`Uint8ClampedArray` усередині. `buildJob` тепер копіює і те, і те в
звичайні дані, на що є тести.

**Відкликання objectURL випереджало завантаження.** `process()`
відкликав старе посилання ще до того, як браузер дочитав із нього
картинку; замість зображення приходив `EncodingError`. На повільних
прогонах цього не видно — а повзунок прозорості шле подію на кожен
піксель руху, і там воно спрацьовувало щоразу. Прогін дістав мітку,
застарілий мовчить, старе посилання відкликається лише після того, як
нове завантажилось.
