# obrobka M2 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers-extended-cc:subagent-driven-development (recommended) or superpowers-extended-cc:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Видалення фону, аутлайн суб'єкта та розумна обрізка — у браузері й через MCP, на одному спільному ядрі.

**Architecture:** Порт `Segmenter`, оголошений у M1, отримує дві реалізації: `onnxruntime-web` для браузера й `onnxruntime-node` для MCP. Модель повертає альфа-маску у своїй роздільності; ядро масштабує її до розміру зображення й далі працює чистими функціями. Bounding box суб'єкта рахується з тієї самої маски, тож розумна обрізка не потребує окремої моделі.

**Tech Stack:** `onnxruntime-web` 1.27 · `onnxruntime-node` 1.27 · U²-Netp · MODNet · isnet-general · Cloudflare R2

**User decisions (already made):**
- Тільки MIT/Apache-ліцензії моделей — проєкт публічний із донатом.
- Обсяг фази зображень: батч, HEIC, EXIF, апскейл, WebGPU — усе входить, але за межами M2.
- Домен `obrobka.dobrovolskyi.com.ua`, хостинг Cloudflare Pages.
- Дизайн: індиго + золото, мотив шахівниці прозорості (реалізовано в M1).

**Специфікація:** `docs/superpowers/specs/2026-08-19-obrobka-design.md`
**Попередній етап:** `docs/superpowers/plans/2026-08-19-obrobka-m1.md` — виконано, задеплоєно.

---

## Вимірювання, які переписали вибір моделей

Специфікація пропонувала рівні MODNet → ormbg → BiRefNet_lite. **Усі три припущення виявились хибними.** Кожну модель завантажено й прогнано через `onnxruntime-node` на синтетичному об'єкті (яскравий круг на темному тлі — не людина).

| Модель | Вага | Ліцензія | Вхід | Інференс | Пік RSS | Знайшла об'єкт? |
|---|---|---|---|---|---|---|
| **U²-Netp** | 4,4 МБ | Apache-2.0 | `[1,3,320,320]` | 284 мс | **97 МБ** | **так** — центр 1.000 |
| **MODNet** uint8 | 6,3 МБ | Apache-2.0 | динамічний | 277 мс @512 | 98 МБ | **ні** — центр 0.022 |
| **isnet-general** fp16 | 84,1 МБ | MIT | `[1,3,1024,1024]` | 665 мс | **472 МБ** | **так** — центр 0.965 |
| ~~BiRefNet_lite~~ fp16 | 109,2 МБ | MIT | `[1,3,1024,1024]` | — | **OOM > 4 ГБ** | не дійшло |

Три висновки, кожен змінює план:

**MODNet і ormbg — портретні, не загальні.** Картка ormbg прямо каже «optimized for images with humans», і MODNet це підтвердив вимірюванням: на неживому об'єкті маска порожня. Обидві не годяться на роль типового вибору — товар, логотип чи тварина дали б сміття.

**BiRefNet_lite не влазить у пам'ять.** Процес убито OOM-кілером при 4,2 ГБ вільної пам'яті. У вкладці зі стелею wasm32 (~2 ГБ практично) він не житиме. Викидаємо з M2; можна повернутись у M4, якщо шлях через WebGPU покаже, що ваги лягають у пам'ять відеокарти, а не в купу WASM.

**U²-Netp загальнопризначений і важить 4,4 МБ.** Це робить типовим вибором модель у **десять разів легшу**, ніж пропонувала специфікація (42,3 МБ ormbg), і при цьому придатну для будь-якого сюжету.

### Ухвалені рівні

| Рівень | Модель | Вага | Коли |
|---|---|---|---|
| **Швидко** (типово) | U²-Netp | 4,4 МБ | будь-який сюжет, миттєве прев'ю |
| **Портрет** | MODNet uint8 | 6,3 МБ | люди — краще тримає волосся |
| **Якісно** | isnet-general fp16 | 84,1 МБ | чіткі краї, за явним вибором |

Сума двох легких рівнів — 10,7 МБ, і обидва вкладаються в ліміт Cloudflare Pages
у 25 MiB. Лише `isnet-general` потребує R2.

### Препроцесинг — три різні рецепти

Витягнуто з `preprocessor_config.json` кожної моделі. Переплутати легко, наслідок — тихо зіпсована маска.

| Модель | Розмір | Масштаб | Нормалізація |
|---|---|---|---|
| U²-Netp | 320×320 фіксовано | ÷255 | ImageNet: mean `[0.485, 0.456, 0.406]`, std `[0.229, 0.224, 0.225]` |
| MODNet | коротша сторона 512, кратно 32 | ÷255 | mean `0.5`, std `0.5` → діапазон [−1, 1] |
| isnet-general | 1024×1024 фіксовано | ÷255 | ImageNet, як в U²-Netp |

### Пастки в графах

- **U²-Netp має сім виходів** із числовими іменами (`1959`…`1965`) — це побічні виходи U²-Net. Потрібен **перший**, `outputNames[0]`. Брати за іменем не можна: воно згенероване експортером і зміниться при переекспорті.
- **MODNet має динамічні осі** `["batch_size", 3, "height", "width"]`. Розмір задаємо ми; при 1024 інференс сповільнюється вчетверо (1076 мс), тож тримаємось 512.
- **isnet-general fp16 приймає float32.** Ваги напівточні, входи й виходи — звичайні. Те саме стосується всіх fp16-експортів `onnx-community`.
- **Усі три виходи вже в діапазоні [0, 1]** — сигмоїда всередині графа, додаткова нормалізація не потрібна.

---

## Структура файлів

```
packages/core/
  src/ops/mask.ts          bbox · threshold · feather · dilate · resample маски
  src/ops/applyMask.ts     множення альфи
  src/ops/outline.ts       дилатація + композиція обведення
  src/ops/smartCrop.ts     кадр за суб'єктом під співвідношення
  src/ops/pipeline.ts      +3 операції в Op

packages/models/           НОВИЙ пакет — описи моделей і препроцесинг
  src/registry.ts          дескриптори трьох моделей
  src/preprocess.ts        RasterImage → Float32Array CHW
  src/postprocess.ts       Float32Array → Mask
  src/index.ts

packages/segmenter-node/   НОВИЙ — onnxruntime-node + кеш у ~/.cache
packages/segmenter-web/    НОВИЙ — onnxruntime-web + Cache Storage

packages/contract-tests/
  src/segmenter.contract.ts  спільний набір для обох реалізацій

apps/mcp/src/tools.ts      +remove_background, +smart_crop
apps/web/src/
  components/TierPicker.svelte   вибір рівня з вагою й прогресом
  components/Widget.svelte       +перемикач фону, аутлайн, розумна обрізка
  data/tools/*.yaml              +4 лендінги
```

---

### Task 1: Операції над маскою

**Goal:** Чисті функції над одноканальною маскою — фундамент для всього іншого в M2.

**Files:**
- Create: `packages/core/src/ops/mask.ts`
- Create: `packages/core/test/mask.test.ts`
- Modify: `packages/core/src/index.ts`

**Acceptance Criteria:**
- [ ] `maskBBox` знаходить прямокутник суб'єкта за порогом
- [ ] `maskBBox` кидає `RangeError`, коли жоден піксель не перевищує поріг
- [ ] `resampleMask` масштабує маску білінійно, без premultiply (альфа не має альфи)
- [ ] `dilateMask` розширює область рівно на заданий радіус
- [ ] `featherMask` пом'якшує край, не зсуваючи його центр

**Verify:** `pnpm vitest run packages/core/test/mask.test.ts` → 9 passed

**Steps:**

- [ ] **Крок 1: Написати падаючі тести**

`packages/core/test/mask.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { maskBBox, resampleMask, dilateMask, featherMask, thresholdMask } from '../src/ops/mask.js';
import type { Mask } from '../src/types.js';

/** Маска із заповненим прямокутником у заданих межах. */
function maskWithRect(
  width: number, height: number,
  x0: number, y0: number, x1: number, y1: number, value = 255,
): Mask {
  const data = new Uint8ClampedArray(width * height);
  for (let y = y0; y < y1; y++) {
    for (let x = x0; x < x1; x++) data[y * width + x] = value;
  }
  return { data, width, height };
}

describe('maskBBox', () => {
  it('знаходить точні межі прямокутника', () => {
    const m = maskWithRect(20, 20, 4, 6, 12, 15);
    expect(maskBBox(m)).toEqual({ x: 4, y: 6, width: 8, height: 9 });
  });

  it('ігнорує пікселі нижче порогу', () => {
    const m = maskWithRect(20, 20, 4, 6, 12, 15, 100);
    expect(() => maskBBox(m, 128)).toThrow(RangeError);
    expect(maskBBox(m, 50)).toEqual({ x: 4, y: 6, width: 8, height: 9 });
  });

  it('кидає помилку на порожній масці', () => {
    const m: Mask = { data: new Uint8ClampedArray(400), width: 20, height: 20 };
    expect(() => maskBBox(m)).toThrow(/суб/);
  });

  it('охоплює всю площу, коли маска суцільна', () => {
    const m = maskWithRect(10, 10, 0, 0, 10, 10);
    expect(maskBBox(m)).toEqual({ x: 0, y: 0, width: 10, height: 10 });
  });
});

describe('resampleMask', () => {
  it('змінює роздільність', () => {
    const m = maskWithRect(4, 4, 0, 0, 2, 4);
    const out = resampleMask(m, 8, 8);
    expect(out.width).toBe(8);
    expect(out.height).toBe(8);
    expect(out.data.length).toBe(64);
  });

  it('зберігає крайні значення при збільшенні', () => {
    const m = maskWithRect(4, 4, 0, 0, 4, 4);
    const out = resampleMask(m, 16, 16);
    expect(out.data[0]).toBe(255);
    expect(out.data[out.data.length - 1]).toBe(255);
  });

  it('повертає копію при незмінному розмірі', () => {
    const m = maskWithRect(4, 4, 1, 1, 3, 3);
    const out = resampleMask(m, 4, 4);
    expect(out.data).not.toBe(m.data);
    expect(Array.from(out.data)).toEqual(Array.from(m.data));
  });
});

describe('dilateMask', () => {
  it('розширює область на заданий радіус', () => {
    const m = maskWithRect(21, 21, 10, 10, 11, 11);
    const out = dilateMask(m, 3);
    expect(maskBBox(out)).toEqual({ x: 7, y: 7, width: 7, height: 7 });
  });

  it('нульовий радіус нічого не змінює', () => {
    const m = maskWithRect(10, 10, 3, 3, 6, 6);
    expect(Array.from(dilateMask(m, 0).data)).toEqual(Array.from(m.data));
  });
});

describe('featherMask', () => {
  it('пом'якшує край, лишаючи серцевину непрозорою', () => {
    const m = maskWithRect(40, 40, 10, 10, 30, 30);
    const out = featherMask(m, 3);
    expect(out.data[20 * 40 + 20]).toBe(255);            // центр
    const edge = out.data[20 * 40 + 10]!;                 // на самому краю
    expect(edge).toBeGreaterThan(0);
    expect(edge).toBeLessThan(255);
  });
});

describe('thresholdMask', () => {
  it('перетворює півтони на двійкову маску', () => {
    const m = maskWithRect(4, 4, 0, 0, 4, 4, 130);
    const out = thresholdMask(m, 128);
    expect(out.data[0]).toBe(255);
    expect(thresholdMask(m, 200).data[0]).toBe(0);
  });
});
```

- [ ] **Крок 2: Запустити — має впасти**

```bash
pnpm vitest run packages/core/test/mask.test.ts
```

Очікується: FAIL, `Cannot find module '../src/ops/mask.js'`.

- [ ] **Крок 3: Реалізувати**

`packages/core/src/ops/mask.ts`:

```ts
import type { Mask, Rect } from '../types.js';

const DEFAULT_THRESHOLD = 128;

/**
 * Прямокутник, що охоплює всі пікселі маски вище порогу.
 *
 * Це і є «визначення суб'єкта»: окрема модель детекції не потрібна,
 * бо маска сегментації вже містить потрібну інформацію.
 */
export function maskBBox(mask: Mask, threshold = DEFAULT_THRESHOLD): Rect {
  let minX = mask.width, minY = mask.height, maxX = -1, maxY = -1;
  for (let y = 0; y < mask.height; y++) {
    const row = y * mask.width;
    for (let x = 0; x < mask.width; x++) {
      if (mask.data[row + x]! < threshold) continue;
      if (x < minX) minX = x;
      if (x > maxX) maxX = x;
      if (y < minY) minY = y;
      if (y > maxY) maxY = y;
    }
  }
  if (maxX < 0) {
    throw new RangeError(
      `Не вдалося знайти суб'єкт: жоден піксель маски не перевищує поріг ${threshold}`,
    );
  }
  return { x: minX, y: minY, width: maxX - minX + 1, height: maxY - minY + 1 };
}

/**
 * Змінює роздільність маски білінійно.
 *
 * На відміну від resample для RGBA, premultiply тут не потрібен:
 * маска — це і є альфа, множити її нема на що.
 */
export function resampleMask(mask: Mask, width: number, height: number): Mask {
  if (width < 1 || height < 1) {
    throw new RangeError(`Цільовий розмір має бути додатним, отримано ${width}×${height}`);
  }
  if (width === mask.width && height === mask.height) {
    return { data: new Uint8ClampedArray(mask.data), width, height };
  }

  const out = new Uint8ClampedArray(width * height);
  const sx = mask.width / width;
  const sy = mask.height / height;

  for (let y = 0; y < height; y++) {
    const fy = Math.min(mask.height - 1, Math.max(0, (y + 0.5) * sy - 0.5));
    const y0 = Math.floor(fy);
    const y1 = Math.min(mask.height - 1, y0 + 1);
    const ty = fy - y0;

    for (let x = 0; x < width; x++) {
      const fx = Math.min(mask.width - 1, Math.max(0, (x + 0.5) * sx - 0.5));
      const x0 = Math.floor(fx);
      const x1 = Math.min(mask.width - 1, x0 + 1);
      const tx = fx - x0;

      const a = mask.data[y0 * mask.width + x0]!;
      const b = mask.data[y0 * mask.width + x1]!;
      const c = mask.data[y1 * mask.width + x0]!;
      const d = mask.data[y1 * mask.width + x1]!;

      out[y * width + x] =
        (a * (1 - tx) + b * tx) * (1 - ty) + (c * (1 - tx) + d * tx) * ty;
    }
  }
  return { data: out, width, height };
}

/** Максимум у круговому околі радіуса r — класична морфологічна дилатація. */
export function dilateMask(mask: Mask, radius: number): Mask {
  const r = Math.round(radius);
  if (r <= 0) return { data: new Uint8ClampedArray(mask.data), ...sizeOf(mask) };

  const out = new Uint8ClampedArray(mask.data.length);
  const r2 = r * r;
  for (let y = 0; y < mask.height; y++) {
    for (let x = 0; x < mask.width; x++) {
      let best = 0;
      for (let dy = -r; dy <= r && best < 255; dy++) {
        const yy = y + dy;
        if (yy < 0 || yy >= mask.height) continue;
        for (let dx = -r; dx <= r; dx++) {
          if (dx * dx + dy * dy > r2) continue;
          const xx = x + dx;
          if (xx < 0 || xx >= mask.width) continue;
          const v = mask.data[yy * mask.width + xx]!;
          if (v > best) { best = v; if (best === 255) break; }
        }
      }
      out[y * mask.width + x] = best;
    }
  }
  return { data: out, width: mask.width, height: mask.height };
}

/**
 * Пом'якшення краю коробковим розмиттям у два проходи.
 *
 * Коробкове, а не гаусове: центр краю не зсувається, а різниця на око
 * непомітна при радіусах, які тут використовуються.
 */
export function featherMask(mask: Mask, radius: number): Mask {
  const r = Math.round(radius);
  if (r <= 0) return { data: new Uint8ClampedArray(mask.data), width: mask.width, height: mask.height };

  const tmp = new Float32Array(mask.data.length);
  const out = new Uint8ClampedArray(mask.data.length);
  const span = r * 2 + 1;

  for (let y = 0; y < mask.height; y++) {
    for (let x = 0; x < mask.width; x++) {
      let sum = 0;
      for (let k = -r; k <= r; k++) {
        const xx = Math.min(mask.width - 1, Math.max(0, x + k));
        sum += mask.data[y * mask.width + xx]!;
      }
      tmp[y * mask.width + x] = sum / span;
    }
  }
  for (let y = 0; y < mask.height; y++) {
    for (let x = 0; x < mask.width; x++) {
      let sum = 0;
      for (let k = -r; k <= r; k++) {
        const yy = Math.min(mask.height - 1, Math.max(0, y + k));
        sum += tmp[yy * mask.width + x]!;
      }
      out[y * mask.width + x] = sum / span;
    }
  }
  return { data: out, width: mask.width, height: mask.height };
}

/** Різкий поріг: усе вище стає 255, решта 0. */
export function thresholdMask(mask: Mask, threshold = DEFAULT_THRESHOLD): Mask {
  const out = new Uint8ClampedArray(mask.data.length);
  for (let i = 0; i < mask.data.length; i++) {
    out[i] = mask.data[i]! >= threshold ? 255 : 0;
  }
  return { data: out, width: mask.width, height: mask.height };
}

function sizeOf(m: Mask): { width: number; height: number } {
  return { width: m.width, height: m.height };
}
```

- [ ] **Крок 4: Запустити — має пройти**

```bash
pnpm vitest run packages/core/test/mask.test.ts
```

Очікується: `9 passed`.

- [ ] **Крок 5: Експортувати й закомітити**

Додати до `packages/core/src/index.ts`:

```ts
export { maskBBox, resampleMask, dilateMask, featherMask, thresholdMask } from './ops/mask.js';
```

```bash
git add -A
git commit -m "feat(core): операції над маскою — bbox, масштабування, дилатація, пом'якшення"
```

---

### Task 2: Застосування маски, аутлайн і розумна обрізка

**Goal:** Три операції, що перетворюють маску на видимий результат.

**Files:**
- Create: `packages/core/src/ops/applyMask.ts`
- Create: `packages/core/src/ops/outline.ts`
- Create: `packages/core/src/ops/smartCrop.ts`
- Create: `packages/core/test/composite.test.ts`
- Modify: `packages/core/src/index.ts`, `packages/core/src/types.ts`

**Acceptance Criteria:**
- [ ] `applyMask` множить альфу зображення на маску, не чіпаючи кольори
- [ ] `applyMask` масштабує маску до розміру зображення автоматично
- [ ] `outline` малює обведення заданої товщини й кольору **під** суб'єктом
- [ ] `outline` розширює полотно, коли обведення не влазить у межі
- [ ] `smartCrop` кадрує під співвідношення так, що суб'єкт лишається в кадрі
- [ ] `smartCrop` не виходить за межі зображення

**Verify:** `pnpm vitest run packages/core/test/composite.test.ts` → 10 passed

**Steps:**

- [ ] **Крок 1: Додати типи**

Додати до `packages/core/src/types.ts`:

```ts
export interface OutlineOptions {
  /** Товщина обведення в пікселях вихідного зображення. */
  readonly width: number;
  readonly color: RGBA;
  /** Пом'якшення краю обведення. Типово 0. */
  readonly feather?: number;
  /** Чи розширювати полотно, коли обведення не влазить. Типово true. */
  readonly expand?: boolean;
}

export interface SmartCropOptions {
  /** Ширина, поділена на висоту. */
  readonly aspectRatio: number;
  /** Запас навколо суб'єкта як частка його більшої сторони. Типово 0.08. */
  readonly padding?: number;
  /** Поріг маски. Типово 128. */
  readonly threshold?: number;
}
```

- [ ] **Крок 2: Написати падаючі тести**

`packages/core/test/composite.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { applyMask } from '../src/ops/applyMask.js';
import { outline } from '../src/ops/outline.js';
import { smartCrop } from '../src/ops/smartCrop.js';
import { solidImage, pixelAt } from './helpers.js';
import type { Mask, RGBA } from '../src/types.js';

const RED: RGBA = { r: 255, g: 0, b: 0, a: 255 };
const BLUE: RGBA = { r: 0, g: 0, b: 255, a: 255 };

function maskWithRect(
  width: number, height: number, x0: number, y0: number, x1: number, y1: number,
): Mask {
  const data = new Uint8ClampedArray(width * height);
  for (let y = y0; y < y1; y++) for (let x = x0; x < x1; x++) data[y * width + x] = 255;
  return { data, width, height };
}

describe('applyMask', () => {
  it('обнуляє альфу поза маскою й лишає всередині', () => {
    const img = solidImage(10, 10, RED);
    const out = applyMask(img, maskWithRect(10, 10, 2, 2, 8, 8));
    expect(pixelAt(out, 5, 5)).toEqual(RED);
    expect(pixelAt(out, 0, 0).a).toBe(0);
  });

  it('не змінює кольорові канали', () => {
    const img = solidImage(10, 10, RED);
    const out = applyMask(img, maskWithRect(10, 10, 0, 0, 10, 10));
    expect(pixelAt(out, 3, 3).r).toBe(255);
    expect(pixelAt(out, 3, 3).g).toBe(0);
  });

  it('масштабує маску до розміру зображення', () => {
    const img = solidImage(40, 40, RED);
    // маска вчетверо менша, суб'єкт — ліва половина
    const out = applyMask(img, maskWithRect(10, 10, 0, 0, 5, 10));
    expect(pixelAt(out, 5, 20).a).toBe(255);
    expect(pixelAt(out, 35, 20).a).toBe(0);
  });

  it('перемножує півтони маски', () => {
    const img = solidImage(4, 4, RED);
    const half: Mask = { data: new Uint8ClampedArray(16).fill(128), width: 4, height: 4 };
    expect(pixelAt(applyMask(img, half), 2, 2).a).toBe(128);
  });
});

describe('outline', () => {
  it('малює обведення навколо суб'єкта', () => {
    const img = solidImage(40, 40, RED);
    const masked = applyMask(img, maskWithRect(40, 40, 15, 15, 25, 25));
    const out = outline(masked, maskWithRect(40, 40, 15, 15, 25, 25), {
      width: 3, color: BLUE, expand: false,
    });
    expect(pixelAt(out, 20, 20)).toEqual(RED);   // суб'єкт зверху
    expect(pixelAt(out, 13, 20)).toEqual(BLUE);  // обведення поруч
    expect(pixelAt(out, 5, 20).a).toBe(0);       // далі — прозоро
  });

  it('розширює полотно, коли обведення не влазить', () => {
    const img = solidImage(20, 20, RED);
    const out = outline(img, maskWithRect(20, 20, 0, 0, 20, 20), {
      width: 4, color: BLUE, expand: true,
    });
    expect(out.width).toBe(28);
    expect(out.height).toBe(28);
  });

  it('не розширює полотно, коли expand вимкнено', () => {
    const img = solidImage(20, 20, RED);
    const out = outline(img, maskWithRect(20, 20, 0, 0, 20, 20), {
      width: 4, color: BLUE, expand: false,
    });
    expect(out.width).toBe(20);
  });
});

describe('smartCrop', () => {
  it('кадрує під квадрат навколо суб'єкта', () => {
    const img = solidImage(200, 100, RED);
    const out = smartCrop(img, maskWithRect(200, 100, 20, 20, 60, 80), { aspectRatio: 1 });
    expect(out.width).toBe(out.height);
  });

  it('не виходить за межі зображення', () => {
    const img = solidImage(100, 100, RED);
    // суб'єкт притиснутий до кута
    const out = smartCrop(img, maskWithRect(100, 100, 0, 0, 10, 10), {
      aspectRatio: 1, padding: 2,
    });
    expect(out.width).toBeLessThanOrEqual(100);
    expect(out.height).toBeLessThanOrEqual(100);
  });

  it('витягує широкий кадр для співвідношення 16:9', () => {
    const img = solidImage(400, 400, RED);
    const out = smartCrop(img, maskWithRect(400, 400, 150, 150, 250, 250), {
      aspectRatio: 16 / 9,
    });
    expect(out.width / out.height).toBeCloseTo(16 / 9, 1);
  });
});
```

- [ ] **Крок 3: Запустити — має впасти**

```bash
pnpm vitest run packages/core/test/composite.test.ts
```

- [ ] **Крок 4: Реалізувати applyMask**

`packages/core/src/ops/applyMask.ts`:

```ts
import type { Mask, RasterImage } from '../types.js';
import { resampleMask } from './mask.js';

/**
 * Множить альфу зображення на маску.
 *
 * Маска приходить у роздільності моделі (320² чи 1024²), тож майже завжди
 * потребує масштабування. Робимо це тут, а не на боці адаптера: так
 * реалізація сегментатора лишається тонкою, а логіка — у ядрі.
 */
export function applyMask(img: RasterImage, mask: Mask): RasterImage {
  const m = mask.width === img.width && mask.height === img.height
    ? mask
    : resampleMask(mask, img.width, img.height);

  const data = new Uint8ClampedArray(img.data);
  for (let i = 0, p = 3; i < m.data.length; i++, p += 4) {
    data[p] = (data[p]! * m.data[i]!) / 255;
  }
  return { data, width: img.width, height: img.height };
}
```

- [ ] **Крок 5: Реалізувати outline**

`packages/core/src/ops/outline.ts`:

```ts
import type { Mask, OutlineOptions, RasterImage } from '../types.js';
import { dilateMask, featherMask, resampleMask } from './mask.js';

/**
 * Малює кольорове обведення під суб'єктом.
 *
 * Порядок важливий: спершу на полотно кладеться розширена маска, залита
 * кольором, і лише зверху — саме зображення. Якби робили навпаки,
 * обведення з'їло б край суб'єкта.
 */
export function outline(
  img: RasterImage, mask: Mask, opts: OutlineOptions,
): RasterImage {
  const width = Math.max(0, Math.round(opts.width));
  if (width === 0) return { data: new Uint8ClampedArray(img.data), width: img.width, height: img.height };

  const expand = opts.expand ?? true;
  const pad = expand ? width : 0;
  const outW = img.width + pad * 2;
  const outH = img.height + pad * 2;

  const base = mask.width === img.width && mask.height === img.height
    ? mask
    : resampleMask(mask, img.width, img.height);

  // Маска обведення на розширеному полотні
  const padded = new Uint8ClampedArray(outW * outH);
  for (let y = 0; y < img.height; y++) {
    padded.set(
      base.data.subarray(y * img.width, (y + 1) * img.width),
      (y + pad) * outW + pad,
    );
  }
  let ring = dilateMask({ data: padded, width: outW, height: outH }, width);
  if (opts.feather !== undefined && opts.feather > 0) {
    ring = featherMask(ring, opts.feather);
  }

  // Полотно: обведення знизу
  const out = new Uint8ClampedArray(outW * outH * 4);
  const { r, g, b, a } = opts.color;
  for (let i = 0, p = 0; i < ring.data.length; i++, p += 4) {
    const cover = (ring.data[i]! * a) / 255;
    if (cover === 0) continue;
    out[p] = r; out[p + 1] = g; out[p + 2] = b; out[p + 3] = cover;
  }

  // Зображення зверху, звичайне source-over
  for (let y = 0; y < img.height; y++) {
    for (let x = 0; x < img.width; x++) {
      const s = (y * img.width + x) * 4;
      const sa = img.data[s + 3]!;
      if (sa === 0) continue;
      const d = ((y + pad) * outW + (x + pad)) * 4;
      const da = out[d + 3]!;
      const outA = sa + (da * (255 - sa)) / 255;
      for (let c = 0; c < 3; c++) {
        const sc = img.data[s + c]!;
        const dc = out[d + c]!;
        out[d + c] = outA === 0 ? 0 : (sc * sa + dc * da * (255 - sa) / 255) / outA;
      }
      out[d + 3] = outA;
    }
  }

  return { data: out, width: outW, height: outH };
}
```

- [ ] **Крок 6: Реалізувати smartCrop**

`packages/core/src/ops/smartCrop.ts`:

```ts
import type { Mask, RasterImage, SmartCropOptions } from '../types.js';
import { maskBBox, resampleMask } from './mask.js';
import { crop } from './crop.js';

/**
 * Кадрує зображення під задане співвідношення, тримаючи суб'єкт у центрі.
 *
 * Суб'єкт визначається з маски — окрема модель детекції не потрібна.
 * Якщо потрібний кадр не влазить у зображення, він стискається до
 * максимально можливого із збереженням співвідношення.
 */
export function smartCrop(
  img: RasterImage, mask: Mask, opts: SmartCropOptions,
): RasterImage {
  if (opts.aspectRatio <= 0) {
    throw new RangeError(`Співвідношення має бути додатним, отримано ${opts.aspectRatio}`);
  }
  const m = mask.width === img.width && mask.height === img.height
    ? mask
    : resampleMask(mask, img.width, img.height);

  const box = maskBBox(m, opts.threshold ?? 128);
  const padding = opts.padding ?? 0.08;
  const grow = Math.max(box.width, box.height) * padding;

  const cx = box.x + box.width / 2;
  const cy = box.y + box.height / 2;
  let w = box.width + grow * 2;
  let h = box.height + grow * 2;

  // Розтягуємо до потрібного співвідношення — тільки збільшуючи
  if (w / h < opts.aspectRatio) w = h * opts.aspectRatio;
  else h = w / opts.aspectRatio;

  // Стискаємо, якщо не влазить у зображення
  const scale = Math.min(1, img.width / w, img.height / h);
  w *= scale;
  h *= scale;

  // Тримаємо кадр у межах
  const x = Math.min(Math.max(cx - w / 2, 0), img.width - w);
  const y = Math.min(Math.max(cy - h / 2, 0), img.height - h);

  return crop(img, {
    x: Math.round(x), y: Math.round(y),
    width: Math.round(w), height: Math.round(h),
  });
}
```

- [ ] **Крок 7: Запустити — має пройти**

```bash
pnpm vitest run packages/core/test/composite.test.ts
```

Очікується: `10 passed`.

- [ ] **Крок 8: Експортувати й закомітити**

```ts
export { applyMask } from './ops/applyMask.js';
export { outline } from './ops/outline.js';
export { smartCrop } from './ops/smartCrop.js';
```

```bash
git add -A
git commit -m "feat(core): застосування маски, аутлайн і розумна обрізка"
```

---

### Task 3: Реєстр моделей і препроцесинг

**Goal:** Описи трьох моделей і чисті перетворення між `RasterImage` і тензорами.

**Files:**
- Create: `packages/models/package.json`, `packages/models/tsconfig.json`
- Create: `packages/models/src/registry.ts`
- Create: `packages/models/src/preprocess.ts`
- Create: `packages/models/src/postprocess.ts`
- Create: `packages/models/src/index.ts`
- Create: `packages/models/test/preprocess.test.ts`

**Acceptance Criteria:**
- [ ] Реєстр описує три моделі з URL, вагою, ліцензією й рецептом препроцесингу
- [ ] `preprocess` дає `Float32Array` у форматі CHW потрібного розміру
- [ ] ImageNet-нормалізація застосовується покроково по каналах
- [ ] Нормалізація 0.5/0.5 дає діапазон [−1, 1]
- [ ] MODNet отримує розмір, кратний 32, з коротшою стороною 512
- [ ] `postprocess` перетворює вихід моделі на `Mask` у діапазоні 0..255

**Verify:** `pnpm vitest run packages/models/test/preprocess.test.ts` → 8 passed

**Steps:**

- [ ] **Крок 1: Створити пакет**

`packages/models/package.json`:

```json
{
  "name": "@obrobka/models",
  "version": "0.0.0",
  "type": "module",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "dependencies": { "@obrobka/core": "workspace:*" }
}
```

`packages/models/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "rootDir": "src", "outDir": "dist" },
  "include": ["src/**/*"],
  "references": [{ "path": "../core" }]
}
```

Додати `{ "path": "packages/models" }` до кореневих `references`.

- [ ] **Крок 2: Написати реєстр**

`packages/models/src/registry.ts`:

```ts
/** Як приводити картинку до входу конкретної моделі. */
export type Normalization =
  | { readonly kind: 'imagenet' }
  | { readonly kind: 'symmetric' }   // mean 0.5, std 0.5 → [-1, 1]
  | { readonly kind: 'none' };       // тільки ÷255

/** Як обирається розмір входу. */
export type InputSizing =
  | { readonly kind: 'fixed'; readonly size: number }
  | { readonly kind: 'shortestEdge'; readonly size: number; readonly multipleOf: number };

import type { Tier } from '@obrobka/core';

export type { Tier };

export interface ModelDescriptor {
  readonly id: Tier;
  readonly label: string;
  /** Що модель уміє знаходити — показується користувачу. */
  readonly scope: string;
  readonly bytes: number;
  readonly license: string;
  readonly source: string;
  /** Шлях відносно бази моделей. */
  readonly file: string;
  readonly sizing: InputSizing;
  readonly normalization: Normalization;
  /**
   * Індекс потрібного виходу.
   *
   * U²-Netp віддає сім побічних виходів із іменами, згенерованими
   * експортером (`1959`…`1965`). Брати за іменем не можна — воно
   * зміниться при переекспорті. Перший вихід — головний.
   */
  readonly outputIndex: number;
}

/**
 * Вибір ґрунтується на вимірюваннях, а не на описах моделей.
 *
 * MODNet і ormbg навчені на людях: на неживому об'єкті MODNet дав маску
 * з центром 0.022 — тобто нічого не знайшов. BiRefNet_lite викинуто:
 * при 1024×1024 його вбив OOM-кілер на машині з 4,2 ГБ вільної пам'яті,
 * а стеля wasm32 у вкладці ще нижча.
 */
export const MODELS: readonly ModelDescriptor[] = [
  {
    id: 'fast',
    label: 'Швидко',
    scope: 'Будь-який сюжет',
    bytes: 4_574_861,
    license: 'Apache-2.0',
    source: 'BritishWerewolf/U-2-Netp',
    file: 'u2netp.onnx',
    sizing: { kind: 'fixed', size: 320 },
    normalization: { kind: 'imagenet' },
    outputIndex: 0,
  },
  {
    id: 'portrait',
    label: 'Портрет',
    scope: 'Лише люди — краще тримає волосся',
    bytes: 6_632_188,
    license: 'Apache-2.0',
    source: 'Xenova/modnet',
    file: 'modnet.onnx',
    sizing: { kind: 'shortestEdge', size: 512, multipleOf: 32 },
    normalization: { kind: 'symmetric' },
    outputIndex: 0,
  },
  {
    id: 'quality',
    label: 'Якісно',
    scope: 'Будь-який сюжет, чіткіші краї',
    bytes: 88_186_000,
    license: 'MIT',
    source: 'imgly/isnet-general-onnx',
    file: 'isnet-general.onnx',
    sizing: { kind: 'fixed', size: 1024 },
    normalization: { kind: 'imagenet' },
    outputIndex: 0,
  },
];

export function modelById(id: Tier): ModelDescriptor {
  const m = MODELS.find((x) => x.id === id);
  if (m === undefined) throw new Error(`Невідомий рівень моделі: ${id}`);
  return m;
}

/** Обчислює розмір входу для конкретного зображення. */
export function inputSizeFor(
  d: ModelDescriptor, width: number, height: number,
): { width: number; height: number } {
  if (d.sizing.kind === 'fixed') {
    return { width: d.sizing.size, height: d.sizing.size };
  }
  const { size, multipleOf } = d.sizing;
  const scale = size / Math.min(width, height);
  const round = (v: number): number =>
    Math.max(multipleOf, Math.round((v * scale) / multipleOf) * multipleOf);
  return { width: round(width), height: round(height) };
}
```

- [ ] **Крок 3: Написати падаючі тести**

`packages/models/test/preprocess.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { MODELS, modelById, inputSizeFor } from '../src/registry.js';
import { preprocess } from '../src/preprocess.js';
import { postprocess } from '../src/postprocess.js';
import type { RasterImage } from '@obrobka/core';

function solid(width: number, height: number, v: number): RasterImage {
  const data = new Uint8ClampedArray(width * height * 4);
  for (let i = 0; i < data.length; i += 4) {
    data[i] = v; data[i + 1] = v; data[i + 2] = v; data[i + 3] = 255;
  }
  return { data, width, height };
}

describe('registry', () => {
  it('містить три рівні', () => {
    expect(MODELS.map((m) => m.id)).toEqual(['fast', 'portrait', 'quality']);
  });

  it('усі ліцензії дозвільні', () => {
    for (const m of MODELS) expect(['MIT', 'Apache-2.0']).toContain(m.license);
  });

  it('фіксований розмір не залежить від зображення', () => {
    expect(inputSizeFor(modelById('fast'), 4000, 300)).toEqual({ width: 320, height: 320 });
  });

  it('MODNet отримує коротшу сторону 512, кратно 32', () => {
    const s = inputSizeFor(modelById('portrait'), 1000, 500);
    expect(s.height).toBe(512);
    expect(s.width % 32).toBe(0);
    expect(s.width).toBeGreaterThan(s.height);
  });
});

describe('preprocess', () => {
  it('дає CHW потрібної довжини', () => {
    const t = preprocess(solid(64, 64, 128), modelById('fast'));
    expect(t.data.length).toBe(3 * 320 * 320);
    expect(t.width).toBe(320);
    expect(t.height).toBe(320);
  });

  it('ImageNet-нормалізація застосовується по каналах', () => {
    // Сірий 128 → 128/255 = 0.502
    const t = preprocess(solid(8, 8, 128), modelById('fast'));
    const plane = 320 * 320;
    const r = t.data[0]!;
    const g = t.data[plane]!;
    expect(r).toBeCloseTo((128 / 255 - 0.485) / 0.229, 2);
    expect(g).toBeCloseTo((128 / 255 - 0.456) / 0.224, 2);
    expect(r).not.toBeCloseTo(g, 3);   // канали справді різні
  });

  it('симетрична нормалізація дає діапазон [-1, 1]', () => {
    const t = preprocess(solid(64, 64, 255), modelById('portrait'));
    expect(t.data[0]).toBeCloseTo(1, 3);
    const black = preprocess(solid(64, 64, 0), modelById('portrait'));
    expect(black.data[0]).toBeCloseTo(-1, 3);
  });
});

describe('postprocess', () => {
  it('перетворює вихід [0,1] на маску 0..255', () => {
    const raw = new Float32Array(4).fill(0.5);
    raw[0] = 1;
    const m = postprocess(raw, 2, 2);
    expect(m.width).toBe(2);
    expect(m.data[0]).toBe(255);
    expect(m.data[1]).toBe(128);
  });
});
```

- [ ] **Крок 4: Реалізувати препроцесинг**

`packages/models/src/preprocess.ts`:

```ts
import { resample, type RasterImage } from '@obrobka/core';
import { inputSizeFor, type ModelDescriptor, type Normalization } from './registry.js';

const IMAGENET_MEAN = [0.485, 0.456, 0.406] as const;
const IMAGENET_STD = [0.229, 0.224, 0.225] as const;

export interface Tensor {
  readonly data: Float32Array;
  readonly width: number;
  readonly height: number;
}

function meanStd(n: Normalization, channel: number): readonly [number, number] {
  switch (n.kind) {
    case 'imagenet': return [IMAGENET_MEAN[channel]!, IMAGENET_STD[channel]!];
    case 'symmetric': return [0.5, 0.5];
    case 'none': return [0, 1];
  }
}

/**
 * RasterImage → тензор CHW для ONNX.
 *
 * Три моделі — три різні рецепти, і сплутати їх легко: результат буде
 * не помилкою, а тихо зіпсованою маскою. Тому рецепт живе в дескрипторі
 * поруч із моделлю, а не в коді виклику.
 */
export function preprocess(img: RasterImage, d: ModelDescriptor): Tensor {
  const { width, height } = inputSizeFor(d, img.width, img.height);
  const scaled = resample(img, width, height);

  const plane = width * height;
  const data = new Float32Array(plane * 3);
  for (let c = 0; c < 3; c++) {
    const [mean, std] = meanStd(d.normalization, c);
    for (let i = 0; i < plane; i++) {
      data[c * plane + i] = (scaled.data[i * 4 + c]! / 255 - mean) / std;
    }
  }
  return { data, width, height };
}
```

- [ ] **Крок 5: Реалізувати постпроцесинг**

`packages/models/src/postprocess.ts`:

```ts
import type { Mask } from '@obrobka/core';

/**
 * Вихід моделі → маска.
 *
 * Усі три моделі мають сигмоїду в графі й віддають значення в [0, 1],
 * що перевірено прогоном: діапазони склали рівно 0.000 … 1.000.
 * Додаткова нормалізація зіпсувала б результат, тож її тут немає —
 * лише перерахунок у байти з відсіканням.
 */
export function postprocess(raw: Float32Array, width: number, height: number): Mask {
  if (raw.length < width * height) {
    throw new RangeError(
      `Вихід моделі закороткий: ${raw.length} значень для маски ${width}×${height}`,
    );
  }
  const data = new Uint8ClampedArray(width * height);
  for (let i = 0; i < data.length; i++) data[i] = raw[i]! * 255;
  return { data, width, height };
}
```

`packages/models/src/index.ts`:

```ts
export * from './registry.js';
export { preprocess, type Tensor } from './preprocess.js';
export { postprocess } from './postprocess.js';
```

- [ ] **Крок 6: Запустити — має пройти**

```bash
pnpm install
pnpm vitest run packages/models/test/preprocess.test.ts
```

Очікується: `8 passed`.

- [ ] **Крок 7: Коміт**

```bash
git add -A
git commit -m "feat(models): реєстр моделей і препроцесинг під три різні рецепти"
```

---

### Task 4: Сегментатор для Node

**Goal:** Реалізація порту `Segmenter` на `onnxruntime-node` із кешем моделей на диску.

**Files:**
- Create: `packages/segmenter-node/package.json`, `tsconfig.json`
- Create: `packages/segmenter-node/src/cache.ts`
- Create: `packages/segmenter-node/src/index.ts`
- Create: `packages/segmenter-node/test/segmenter.test.ts`

**Acceptance Criteria:**
- [ ] `load` завантажує модель і кешує її в `~/.cache/obrobka/models/`
- [ ] Повторний `load` бере файл із кешу, не звертаючись до мережі
- [ ] `segment` повертає `Mask` у роздільності входу моделі
- [ ] Вихід береться за індексом, а не за іменем
- [ ] `dispose` звільняє сесію
- [ ] Обірване завантаження не лишає пошкоджений файл у кеші

**Verify:** `pnpm vitest run packages/segmenter-node/test/segmenter.test.ts` → 6 passed

**Steps:**

- [ ] **Крок 1: Створити пакет**

`packages/segmenter-node/package.json`:

```json
{
  "name": "@obrobka/segmenter-node",
  "version": "0.0.0",
  "type": "module",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "dependencies": {
    "@obrobka/core": "workspace:*",
    "@obrobka/models": "workspace:*",
    "onnxruntime-node": "1.27.0"
  },
  "devDependencies": { "@types/node": "24.10.1" }
}
```

`packages/segmenter-node/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "rootDir": "src", "outDir": "dist", "types": ["node"] },
  "include": ["src/**/*"],
  "references": [{ "path": "../core" }, { "path": "../models" }]
}
```

- [ ] **Крок 2: Реалізувати кеш**

`packages/segmenter-node/src/cache.ts`:

```ts
import { mkdir, rename, stat, writeFile } from 'node:fs/promises';
import { homedir } from 'node:os';
import { join } from 'node:path';

const DIR = join(homedir(), '.cache', 'obrobka', 'models');

export function cachePath(file: string): string {
  return join(DIR, file);
}

/**
 * Повертає шлях до моделі, завантажуючи її за потреби.
 *
 * Файл пишеться під тимчасовим іменем і перейменовується лише після
 * повного завантаження: обірваний процес не лишить у кеші огризок,
 * який потім тихо не запуститься.
 */
export async function ensureModel(
  file: string, url: string, onProgress?: (fraction: number) => void,
): Promise<string> {
  const target = cachePath(file);
  try {
    await stat(target);
    onProgress?.(1);
    return target;
  } catch { /* немає — качаємо */ }

  await mkdir(DIR, { recursive: true });
  const res = await fetch(url);
  if (!res.ok) {
    throw new Error(`Не вдалося завантажити модель ${file}: HTTP ${res.status}`);
  }

  const total = Number(res.headers.get('content-length') ?? 0);
  const chunks: Uint8Array[] = [];
  let received = 0;
  const reader = res.body?.getReader();
  if (reader === undefined) throw new Error(`Порожня відповідь для моделі ${file}`);

  for (;;) {
    const { done, value } = await reader.read();
    if (done) break;
    chunks.push(value);
    received += value.length;
    if (total > 0) onProgress?.(received / total);
  }

  const blob = new Uint8Array(received);
  let at = 0;
  for (const c of chunks) { blob.set(c, at); at += c.length; }

  const tmp = `${target}.${process.pid}.part`;
  await writeFile(tmp, blob);
  await rename(tmp, target);
  onProgress?.(1);
  return target;
}
```

- [ ] **Крок 3: Реалізувати сегментатор**

`packages/segmenter-node/src/index.ts`:

```ts
import ort from 'onnxruntime-node';
import type { Mask, RasterImage, Segmenter } from '@obrobka/core';
import {
  modelById, preprocess, postprocess, inputSizeFor,
  type ModelDescriptor, type Tier,
} from '@obrobka/models';
import { ensureModel } from './cache.js';

/** База, звідки беруться моделі. Перевизначається змінною середовища. */
const BASE = process.env['OBROBKA_MODELS_URL']
  ?? 'https://models.obrobka.dobrovolskyi.com.ua';

class NodeSegmenter implements Segmenter {
  readonly id: string;
  readonly inputSize: number;
  #d: ModelDescriptor;
  #session: ort.InferenceSession | null = null;

  constructor(d: ModelDescriptor) {
    this.#d = d;
    this.id = d.id;
    this.inputSize = d.sizing.kind === 'fixed' ? d.sizing.size : d.sizing.size;
  }

  async load(onProgress?: (fraction: number) => void): Promise<void> {
    if (this.#session !== null) { onProgress?.(1); return; }
    const path = await ensureModel(this.#d.file, `${BASE}/${this.#d.file}`, onProgress);
    this.#session = await ort.InferenceSession.create(path);
  }

  async segment(img: RasterImage): Promise<Mask> {
    if (this.#session === null) {
      throw new Error(`Модель ${this.id} не завантажена — спершу викличте load()`);
    }
    const t = preprocess(img, this.#d);
    const feeds = {
      [this.#session.inputNames[0]!]:
        new ort.Tensor('float32', t.data, [1, 3, t.height, t.width]),
    };
    const out = await this.#session.run(feeds);
    // Вихід за індексом: U²-Netp має сім побічних виходів із іменами,
    // які згенерував експортер і які зміняться при переекспорті.
    const name = this.#session.outputNames[this.#d.outputIndex]!;
    return postprocess(out[name]!.data as Float32Array, t.width, t.height);
  }

  async dispose(): Promise<void> {
    await this.#session?.release();
    this.#session = null;
  }
}

export function createSegmenter(tier: Tier): Segmenter {
  return new NodeSegmenter(modelById(tier));
}

export { inputSizeFor };
```

- [ ] **Крок 4: Написати тести**

`packages/segmenter-node/test/segmenter.test.ts`:

```ts
import { describe, it, expect, beforeAll } from 'vitest';
import { createSegmenter } from '../src/index.js';
import { maskBBox, type RasterImage } from '@obrobka/core';

/** Яскравий круг на темному тлі — виразний неживий об'єкт. */
function circleImage(size: number): RasterImage {
  const data = new Uint8ClampedArray(size * size * 4);
  const r = size * 0.28;
  for (let y = 0; y < size; y++) {
    for (let x = 0; x < size; x++) {
      const dx = x - size / 2, dy = y - size / 2;
      const inside = dx * dx + dy * dy < r * r;
      const i = (y * size + x) * 4;
      data[i] = inside ? 242 : 20;
      data[i + 1] = inside ? 38 : 20;
      data[i + 2] = inside ? 38 : 22;
      data[i + 3] = 255;
    }
  }
  return { data, width: size, height: size };
}

describe('NodeSegmenter', () => {
  const seg = createSegmenter('fast');

  beforeAll(async () => { await seg.load(); }, 180_000);

  it('повертає маску у роздільності входу моделі', async () => {
    const m = await seg.segment(circleImage(256));
    expect(m.width).toBe(320);
    expect(m.height).toBe(320);
  });

  it('знаходить неживий об'єкт — U²-Netp загальнопризначений', async () => {
    const m = await seg.segment(circleImage(256));
    const center = m.data[Math.floor(320 / 2) * 320 + Math.floor(320 / 2)]!;
    expect(center).toBeGreaterThan(200);
  });

  it('фон лишається прозорим', async () => {
    const m = await seg.segment(circleImage(256));
    expect(m.data[0]).toBeLessThan(40);
  });

  it('bbox приблизно збігається з кругом', async () => {
    const m = await seg.segment(circleImage(256));
    const box = maskBBox(m);
    // круг займає ~56 % сторони, з запасом на неточність моделі
    expect(box.width / 320).toBeGreaterThan(0.4);
    expect(box.width / 320).toBeLessThan(0.75);
  });

  it('повторний load не падає', async () => {
    await expect(seg.load()).resolves.toBeUndefined();
  });

  it('dispose звільняє сесію', async () => {
    const s = createSegmenter('fast');
    await s.load();
    await s.dispose();
    await expect(s.segment(circleImage(64))).rejects.toThrow(/не завантажена/);
  });
});
```

- [ ] **Крок 5: Запустити**

```bash
pnpm install
pnpm approve-builds onnxruntime-node
pnpm vitest run packages/segmenter-node/test/segmenter.test.ts
```

Очікується: `6 passed`. Перший прогін завантажує 4,4 МБ моделі — тому таймаут 180 с у `beforeAll`.

- [ ] **Крок 6: Коміт**

```bash
git add -A
git commit -m "feat(segmenter-node): сегментатор на onnxruntime-node із кешем на диску"
```

---

### Task 5: Розміщення моделей на R2

**Goal:** Три моделі лежать на `models.obrobka.dobrovolskyi.com.ua` із заголовками, які не заблокує COEP.

**Files:**
- Create: `scripts/publish-models.sh`
- Modify: `docs/deploy.md`

**Acceptance Criteria:**
- [ ] Бакет R2 створено й прив'язано до субдомену
- [ ] Три файли доступні по HTTPS
- [ ] Відповідь містить `Cross-Origin-Resource-Policy: cross-origin`
- [ ] Відповідь містить `Access-Control-Allow-Origin: *`
- [ ] `Cache-Control` дозволяє довічне кешування — імена файлів стабільні
- [ ] Скрипт публікації повторюваний і не перезаливає незмінене

**Verify:** `curl -sI https://models.obrobka.dobrovolskyi.com.ua/u2netp.onnx | grep -iE 'cross-origin-resource|access-control'` → обидва заголовки

**Steps:**

- [ ] **Крок 1: Створити бакет**

Ключі вже лежать у `~/.config/cloudflare/env` (`CLOUDFLARE_ACCESS_KEY_ID`,
`CLOUDFLARE_SECRET_ACCESS_KEY`, `CLOUDFLARE_S3_API_ENDPOINT`).

```bash
set -a; . ~/.config/cloudflare/env; set +a
pnpm exec wrangler r2 bucket create obrobka-models
```

- [ ] **Крок 2: Прив'язати субдомен**

```bash
pnpm exec wrangler r2 bucket domain add obrobka-models \
  --domain models.obrobka.dobrovolskyi.com.ua --zone-id "$ZONE_ID"
```

`ZONE_ID` для `dobrovolskyi.com.ua` береться так:

```bash
curl -s -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" \
  "https://api.cloudflare.com/client/v4/zones?name=dobrovolskyi.com.ua" \
  | python3 -c "import sys,json;print(json.load(sys.stdin)['result'][0]['id'])"
```

- [ ] **Крок 3: Скрипт публікації**

`scripts/publish-models.sh`:

```bash
#!/usr/bin/env bash
# Завантажує моделі з HuggingFace і кладе в R2.
#
# Імена в бакеті стабільні, тож Cache-Control ставимо на рік: модель
# за конкретним іменем ніколи не змінюється, а нова версія отримає
# нове ім'я. Cross-Origin-Resource-Policy обов'язковий — без нього
# COEP credentialless заблокує завантаження з нашого походження.
set -euo pipefail

BUCKET=obrobka-models
TMP=$(mktemp -d)
trap 'rm -rf "$TMP"' EXIT

declare -A SOURCES=(
  [u2netp.onnx]="https://huggingface.co/BritishWerewolf/U-2-Netp/resolve/main/onnx/model.onnx"
  [modnet.onnx]="https://huggingface.co/Xenova/modnet/resolve/main/onnx/model_quantized.onnx"
  [isnet-general.onnx]="https://huggingface.co/imgly/isnet-general-onnx/resolve/main/onnx/model_fp16.onnx"
)

for name in "${!SOURCES[@]}"; do
  if pnpm exec wrangler r2 object get "$BUCKET/$name" --file /dev/null >/dev/null 2>&1; then
    echo "  вже в бакеті: $name"
    continue
  fi
  echo "  качаю: $name"
  curl -sL --fail --max-time 900 -o "$TMP/$name" "${SOURCES[$name]}"
  printf '  розмір: %s МБ\n' "$(du -m "$TMP/$name" | cut -f1)"
  pnpm exec wrangler r2 object put "$BUCKET/$name" \
    --file "$TMP/$name" \
    --content-type application/octet-stream \
    --cache-control 'public, max-age=31536000, immutable'
done
echo "готово"
```

```bash
chmod +x scripts/publish-models.sh
set -a; . ~/.config/cloudflare/env; set +a
./scripts/publish-models.sh
```

- [ ] **Крок 4: Налаштувати CORS на бакеті**

`wrangler` не має команди для CORS, тож через API:

```bash
set -a; . ~/.config/cloudflare/env; set +a
curl -s -X PUT \
  -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" \
  -H "Content-Type: application/json" \
  "https://api.cloudflare.com/client/v4/accounts/$CLOUDFLARE_ACCOUNT_ID/r2/buckets/obrobka-models/cors" \
  --data '{
    "rules": [{
      "allowed": {
        "origins": ["https://obrobka.dobrovolskyi.com.ua", "http://localhost:4488"],
        "methods": ["GET", "HEAD"],
        "headers": ["*"]
      },
      "maxAgeSeconds": 86400
    }]
  }' | python3 -c "import sys,json;print('success:', json.load(sys.stdin)['success'])"
```

- [ ] **Крок 5: Додати CORP через Transform Rule**

R2 не віддає `Cross-Origin-Resource-Policy` сам. Заголовок додається правилом
на зоні:

```bash
set -a; . ~/.config/cloudflare/env; set +a
ZONE=$(curl -s -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" \
  "https://api.cloudflare.com/client/v4/zones?name=dobrovolskyi.com.ua" \
  | python3 -c "import sys,json;print(json.load(sys.stdin)['result'][0]['id'])")

curl -s -X PUT \
  -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" -H "Content-Type: application/json" \
  "https://api.cloudflare.com/client/v4/zones/$ZONE/rulesets/phases/http_response_headers_transform/entrypoint" \
  --data '{
    "rules": [{
      "action": "rewrite",
      "expression": "(http.host eq \"models.obrobka.dobrovolskyi.com.ua\")",
      "description": "CORP для моделей — без нього COEP credentialless блокує завантаження",
      "action_parameters": {
        "headers": {
          "Cross-Origin-Resource-Policy": { "operation": "set", "value": "cross-origin" }
        }
      }
    }]
  }' | python3 -c "import sys,json;d=json.load(sys.stdin);print('success:', d['success'], d.get('errors'))"
```

- [ ] **Крок 6: Перевірити**

```bash
for f in u2netp.onnx modnet.onnx isnet-general.onnx; do
  echo "--- $f"
  curl -sI "https://models.obrobka.dobrovolskyi.com.ua/$f" \
    | grep -iE '^HTTP|content-length|cross-origin-resource|cache-control'
done
```

Очікується для кожного: `HTTP/2 200`, `cross-origin-resource-policy: cross-origin`,
`cache-control: public, max-age=31536000, immutable`.

- [ ] **Крок 7: Дописати docs/deploy.md і закомітити**

Додати розділ про R2: як публікувати нову модель, чому потрібен CORP,
і що імена файлів незмінні.

```bash
git add -A
git commit -m "chore: публікація моделей у R2 з CORP і CORS"
```

---

### Task 6: Сегментатор для браузера

**Goal:** Реалізація `Segmenter` на `onnxruntime-web` із кешем у Cache Storage і чесним показом провайдера виконання.

**Files:**
- Create: `packages/segmenter-web/package.json`, `tsconfig.json`
- Create: `packages/segmenter-web/src/cache.ts`
- Create: `packages/segmenter-web/src/index.ts`

**Acceptance Criteria:**
- [ ] Модель кешується в Cache Storage і не качається вдруге
- [ ] Прогрес завантаження повідомляється часткою 0..1
- [ ] Провайдери виконання пробуються по черзі: `webgpu` → `wasm`
- [ ] Обраний провайдер доступний як властивість — інтерфейс має його показати
- [ ] `env.wasm.proxy` вимкнено — він несумісний з WebGPU

**Verify:** перевіряється контрактом у Task 7 та e2e в Task 10

**Steps:**

- [ ] **Крок 1: Створити пакет**

`packages/segmenter-web/package.json`:

```json
{
  "name": "@obrobka/segmenter-web",
  "version": "0.0.0",
  "type": "module",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "dependencies": {
    "@obrobka/core": "workspace:*",
    "@obrobka/models": "workspace:*",
    "onnxruntime-web": "1.27.0"
  }
}
```

`packages/segmenter-web/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src", "outDir": "dist",
    "lib": ["ES2023", "DOM", "DOM.Iterable"]
  },
  "include": ["src/**/*"],
  "references": [{ "path": "../core" }, { "path": "../models" }]
}
```

- [ ] **Крок 2: Кеш у Cache Storage**

`packages/segmenter-web/src/cache.ts`:

```ts
const CACHE = 'obrobka-models-v1';

/**
 * Дає байти моделі, кешуючи їх між сесіями.
 *
 * Cache Storage, а не IndexedDB: моделі — це саме HTTP-відповіді,
 * і сервісворкер бачить той самий кеш, тож сторінка «Мої моделі»
 * зможе показати й почистити його без окремого обліку.
 */
export async function fetchModel(
  url: string, onProgress?: (fraction: number) => void,
): Promise<ArrayBuffer> {
  const cache = await caches.open(CACHE);
  const hit = await cache.match(url);
  if (hit !== undefined) {
    onProgress?.(1);
    return hit.arrayBuffer();
  }

  const res = await fetch(url);
  if (!res.ok) throw new Error(`Не вдалося завантажити модель: HTTP ${res.status}`);

  const total = Number(res.headers.get('content-length') ?? 0);
  const reader = res.body?.getReader();
  if (reader === undefined) throw new Error('Порожня відповідь при завантаженні моделі');

  const chunks: Uint8Array[] = [];
  let received = 0;
  for (;;) {
    const { done, value } = await reader.read();
    if (done) break;
    chunks.push(value);
    received += value.length;
    if (total > 0) onProgress?.(received / total);
  }

  const bytes = new Uint8Array(received);
  let at = 0;
  for (const c of chunks) { bytes.set(c, at); at += c.length; }

  // Кладемо в кеш власноруч зібрану відповідь: оригінальний потік уже вичитаний
  await cache.put(url, new Response(bytes, {
    headers: { 'content-type': 'application/octet-stream', 'content-length': String(received) },
  }));
  onProgress?.(1);
  return bytes.buffer as ArrayBuffer;
}

/** Скільки місця займають закешовані моделі. */
export async function cachedModels(): Promise<{ url: string; bytes: number }[]> {
  const cache = await caches.open(CACHE);
  const out: { url: string; bytes: number }[] = [];
  for (const req of await cache.keys()) {
    const res = await cache.match(req);
    out.push({ url: req.url, bytes: Number(res?.headers.get('content-length') ?? 0) });
  }
  return out;
}

export async function clearModels(): Promise<void> {
  await caches.delete(CACHE);
}
```

- [ ] **Крок 3: Сегментатор**

`packages/segmenter-web/src/index.ts`:

```ts
import * as ort from 'onnxruntime-web/webgpu';
import type { Mask, RasterImage, Segmenter } from '@obrobka/core';
import {
  modelById, preprocess, postprocess,
  type ModelDescriptor, type Tier,
} from '@obrobka/models';
import { fetchModel } from './cache.js';

export { cachedModels, clearModels } from './cache.js';

const BASE = 'https://models.obrobka.dobrovolskyi.com.ua';

/**
 * Проксі-воркер ORT несумісний з WebGPU: буфери відеокарти не можна
 * передати між потоками. Ми й так виконуємось у власному воркері,
 * тож вимикаємо його явно.
 */
ort.env.wasm.proxy = false;

export type Provider = 'webgpu' | 'wasm';

class WebSegmenter implements Segmenter {
  readonly id: string;
  readonly inputSize: number;
  /** Який провайдер зрештою запрацював — інтерфейс має сказати це чесно. */
  provider: Provider | null = null;

  #d: ModelDescriptor;
  #session: ort.InferenceSession | null = null;

  constructor(d: ModelDescriptor) {
    this.#d = d;
    this.id = d.id;
    this.inputSize = d.sizing.size;
  }

  async load(onProgress?: (fraction: number) => void): Promise<void> {
    if (this.#session !== null) { onProgress?.(1); return; }
    const bytes = await fetchModel(`${BASE}/${this.#d.file}`, onProgress);

    const order: Provider[] = 'gpu' in navigator ? ['webgpu', 'wasm'] : ['wasm'];
    let last: unknown;
    for (const ep of order) {
      try {
        this.#session = await ort.InferenceSession.create(bytes, { executionProviders: [ep] });
        this.provider = ep;
        return;
      } catch (e) {
        last = e;
      }
    }
    throw new Error(
      `Не вдалося запустити модель ${this.id} на жодному провайдері: ${String(last)}`,
    );
  }

  async segment(img: RasterImage): Promise<Mask> {
    if (this.#session === null) {
      throw new Error(`Модель ${this.id} не завантажена — спершу викличте load()`);
    }
    const t = preprocess(img, this.#d);
    const feeds = {
      [this.#session.inputNames[0]!]:
        new ort.Tensor('float32', t.data, [1, 3, t.height, t.width]),
    };
    const out = await this.#session.run(feeds);
    const name = this.#session.outputNames[this.#d.outputIndex]!;
    return postprocess(out[name]!.data as Float32Array, t.width, t.height);
  }

  async dispose(): Promise<void> {
    await this.#session?.release();
    this.#session = null;
    this.provider = null;
  }
}

export function createSegmenter(tier: Tier): Segmenter & { provider: Provider | null } {
  return new WebSegmenter(modelById(tier));
}
```

- [ ] **Крок 4: Зібрати й закомітити**

```bash
pnpm install
pnpm typecheck
git add -A
git commit -m "feat(segmenter-web): сегментатор на onnxruntime-web із Cache Storage"
```

---

### Task 7: Контрактні тести сегментатора

**Goal:** Один набір тестів проти обох реалізацій — те, заради чого будувалась межа порту.

**Files:**
- Create: `packages/contract-tests/src/segmenter.contract.ts`
- Modify: `packages/contract-tests/src/index.ts`
- Create: `packages/segmenter-node/test/contract.test.ts`

**Acceptance Criteria:**
- [ ] Контракт перевіряє форму маски, діапазон значень і знаходження суб'єкта
- [ ] Контракт не залежить від конкретної реалізації
- [ ] Node-адаптер проходить контракт для всіх трьох рівнів
- [ ] Портретна модель позначена як така — контракт не вимагає від неї неживих об'єктів

**Verify:** `pnpm vitest run packages/segmenter-node/test/contract.test.ts` → 12 passed

**Steps:**

- [ ] **Крок 1: Написати контракт**

`packages/contract-tests/src/segmenter.contract.ts`:

```ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import type { Mask, RasterImage, Segmenter } from '@obrobka/core';

function circleImage(size: number): RasterImage {
  const data = new Uint8ClampedArray(size * size * 4);
  const r = size * 0.28;
  for (let y = 0; y < size; y++) {
    for (let x = 0; x < size; x++) {
      const dx = x - size / 2, dy = y - size / 2;
      const inside = dx * dx + dy * dy < r * r;
      const i = (y * size + x) * 4;
      data[i] = inside ? 242 : 20;
      data[i + 1] = inside ? 38 : 20;
      data[i + 2] = inside ? 38 : 22;
      data[i + 3] = 255;
    }
  }
  return { data, width: size, height: size };
}

function centerOf(m: Mask): number {
  return m.data[Math.floor(m.height / 2) * m.width + Math.floor(m.width / 2)]!;
}

export interface SegmenterCase {
  readonly label: string;
  readonly make: () => Segmenter;
  /**
   * Чи вміє модель знаходити неживі об'єкти.
   *
   * MODNet навчений лише на людях: на синтетичному крузі він дає
   * центр 0.022. Вимагати від нього загального результату означало б
   * писати тест, приречений падати.
   */
  readonly general: boolean;
}

/** Контракт сегментатора. Виконується проти будь-якої реалізації порту. */
export function testSegmenterContract(suite: string, cases: readonly SegmenterCase[]): void {
  for (const c of cases) {
    describe(`Segmenter contract: ${suite} / ${c.label}`, () => {
      const seg = c.make();
      beforeAll(async () => { await seg.load(); }, 300_000);
      afterAll(async () => { await seg.dispose(); });

      it('маска має роздільність входу моделі', async () => {
        const m = await seg.segment(circleImage(256));
        expect(m.width).toBeGreaterThan(0);
        expect(m.data.length).toBe(m.width * m.height);
      });

      it('значення лежать у 0..255', async () => {
        const m = await seg.segment(circleImage(256));
        let min = 255, max = 0;
        for (const v of m.data) { if (v < min) min = v; if (v > max) max = v; }
        expect(min).toBeGreaterThanOrEqual(0);
        expect(max).toBeLessThanOrEqual(255);
      });

      it('розмір входу не впливає на форму виходу', async () => {
        const a = await seg.segment(circleImage(128));
        const b = await seg.segment(circleImage(384));
        expect({ w: a.width, h: a.height }).toEqual({ w: b.width, h: b.height });
      });

      if (c.general) {
        it('знаходить неживий об'єкт', async () => {
          const m = await seg.segment(circleImage(256));
          expect(centerOf(m)).toBeGreaterThan(180);
        });
      } else {
        it('портретна модель не претендує на неживі об'єкти', async () => {
          const m = await seg.segment(circleImage(256));
          expect(centerOf(m)).toBeLessThan(120);
        });
      }
    });
  }
}
```

Додати до `packages/contract-tests/src/index.ts`:

```ts
export { testSegmenterContract, type SegmenterCase } from './segmenter.contract.js';
```

- [ ] **Крок 2: Підключити в Node**

`packages/segmenter-node/test/contract.test.ts`:

```ts
import { testSegmenterContract } from '@obrobka/contract-tests';
import { createSegmenter } from '../src/index.js';

testSegmenterContract('node', [
  { label: 'fast / U²-Netp', make: () => createSegmenter('fast'), general: true },
  { label: 'portrait / MODNet', make: () => createSegmenter('portrait'), general: false },
  { label: 'quality / isnet-general', make: () => createSegmenter('quality'), general: true },
]);
```

Додати `@obrobka/contract-tests: workspace:*` у `devDependencies` пакета
`@obrobka/segmenter-node`.

- [ ] **Крок 3: Запустити**

```bash
pnpm install
pnpm vitest run packages/segmenter-node/test/contract.test.ts
```

Очікується: `12 passed`. Перший прогін завантажує 99 МБ моделей —
тому таймаут 300 с у `beforeAll`.

- [ ] **Крок 4: Коміт**

```bash
git add -A
git commit -m "test: контракт сегментатора для трьох рівнів"
```

---

### Task 8: Операції M2 у пайплайні

**Goal:** `Job` уміє описувати видалення фону, аутлайн і розумну обрізку.

**Files:**
- Modify: `packages/core/src/types.ts`
- Modify: `packages/core/src/ops/pipeline.ts`
- Modify: `packages/core/src/ports/index.ts`
- Create: `packages/core/test/pipeline-m2.test.ts`

**Acceptance Criteria:**
- [ ] `Op` розширено трьома операціями
- [ ] `Context` приймає необов'язкову фабрику сегментаторів
- [ ] Маска рахується **один раз** на всі операції, що її потребують
- [ ] Операція, яка потребує маски без сегментатора в контексті, дає зрозумілу помилку
- [ ] Порядок операцій зберігається

**Verify:** `pnpm vitest run packages/core/test/pipeline-m2.test.ts` → 6 passed

**Steps:**

- [ ] **Крок 1: Розширити типи**

У `packages/core/src/types.ts` замінити `Op`:

```ts
export type Tier = 'fast' | 'portrait' | 'quality';

export type Op =
  | ({ readonly type: 'fit' } & FitOptions)
  | { readonly type: 'crop'; readonly rect: Rect }
  | {
      readonly type: 'removeBackground';
      readonly tier?: Tier;
      readonly feather?: number;
    }
  | ({ readonly type: 'outline' } & OutlineOptions)
  | ({ readonly type: 'smartCrop' } & SmartCropOptions);
```

У `packages/core/src/ports/index.ts` розширити `Context`:

```ts
export interface Context {
  readonly codec: Codec;
  readonly resampler?: Resampler;
  /**
   * Створює сегментатор потрібного рівня. Необов'язковий: операції M1
   * працюють без нього, а операції з маскою дадуть зрозумілу помилку.
   */
  readonly segmenter?: (tier: Tier) => Segmenter;
}
```

- [ ] **Крок 2: Написати падаючі тести**

`packages/core/test/pipeline-m2.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { runJob } from '../src/ops/pipeline.js';
import type { Codec, Segmenter } from '../src/ports/index.js';
import type { Job, Mask, RasterImage } from '../src/types.js';
import { solidImage } from './helpers.js';

const RED = { r: 255, g: 0, b: 0, a: 255 } as const;

function fakeCodec(source: RasterImage): Codec {
  return {
    canDecode: () => true,
    canEncode: () => true,
    decode: async () => source,
    encode: async (img) =>
      new TextEncoder().encode(JSON.stringify({ w: img.width, h: img.height })),
  };
}

/** Маска-заглушка: центральний квадрат. */
function fakeSegmenter(calls: { n: number }): Segmenter {
  return {
    id: 'fake',
    inputSize: 64,
    load: async () => {},
    segment: async (): Promise<Mask> => {
      calls.n += 1;
      const data = new Uint8ClampedArray(64 * 64);
      for (let y = 16; y < 48; y++) for (let x = 16; x < 48; x++) data[y * 64 + x] = 255;
      return { data, width: 64, height: 64 };
    },
    dispose: async () => {},
  };
}

function decode(bytes: Uint8Array): { w: number; h: number } {
  return JSON.parse(new TextDecoder().decode(bytes)) as { w: number; h: number };
}

describe('runJob із маскою', () => {
  const source = solidImage(128, 128, RED);
  const input = new Uint8Array(0);

  it('видаляє фон', async () => {
    const calls = { n: 0 };
    const ctx = { codec: fakeCodec(source), segmenter: () => fakeSegmenter(calls) };
    const job: Job = {
      ops: [{ type: 'removeBackground' }],
      output: { format: 'png' },
    };
    const out = decode(await runJob(input, 'image/png', job, ctx));
    expect(out).toEqual({ w: 128, h: 128 });
    expect(calls.n).toBe(1);
  });

  it('рахує маску один раз на кілька операцій', async () => {
    const calls = { n: 0 };
    const ctx = { codec: fakeCodec(source), segmenter: () => fakeSegmenter(calls) };
    const job: Job = {
      ops: [
        { type: 'removeBackground' },
        { type: 'outline', width: 2, color: { r: 0, g: 0, b: 255, a: 255 }, expand: false },
        { type: 'smartCrop', aspectRatio: 1 },
      ],
      output: { format: 'png' },
    };
    await runJob(input, 'image/png', job, ctx);
    expect(calls.n).toBe(1);
  });

  it('аутлайн розширює полотно', async () => {
    const calls = { n: 0 };
    const ctx = { codec: fakeCodec(source), segmenter: () => fakeSegmenter(calls) };
    const job: Job = {
      ops: [{ type: 'outline', width: 4, color: { r: 0, g: 0, b: 255, a: 255 } }],
      output: { format: 'png' },
    };
    expect(decode(await runJob(input, 'image/png', job, ctx))).toEqual({ w: 136, h: 136 });
  });

  it('розумна обрізка дає квадрат', async () => {
    const calls = { n: 0 };
    const ctx = { codec: fakeCodec(source), segmenter: () => fakeSegmenter(calls) };
    const job: Job = {
      ops: [{ type: 'smartCrop', aspectRatio: 1 }],
      output: { format: 'png' },
    };
    const out = decode(await runJob(input, 'image/png', job, ctx));
    expect(out.w).toBe(out.h);
  });

  it('без сегментатора дає зрозумілу помилку', async () => {
    const ctx = { codec: fakeCodec(source) };
    const job: Job = { ops: [{ type: 'removeBackground' }], output: { format: 'png' } };
    await expect(runJob(input, 'image/png', job, ctx)).rejects.toThrow(/сегментатор/i);
  });

  it('операції M1 працюють без сегментатора', async () => {
    const ctx = { codec: fakeCodec(source) };
    const job: Job = {
      ops: [{ type: 'fit', width: 64, height: 64, mode: 'contain' }],
      output: { format: 'png' },
    };
    expect(decode(await runJob(input, 'image/png', job, ctx))).toEqual({ w: 64, h: 64 });
  });
});
```

- [ ] **Крок 3: Переписати pipeline**

`packages/core/src/ops/pipeline.ts`:

```ts
import type { Job, Mask, Op, RasterImage, Tier } from '../types.js';
import type { Context } from '../ports/index.js';
import { fit } from './fit.js';
import { crop } from './crop.js';
import { applyMask } from './applyMask.js';
import { outline } from './outline.js';
import { smartCrop } from './smartCrop.js';
import { featherMask } from './mask.js';

const NEEDS_MASK: ReadonlySet<Op['type']> = new Set(['removeBackground', 'outline', 'smartCrop']);

function tierOf(ops: readonly Op[]): Tier {
  for (const op of ops) {
    if (op.type === 'removeBackground' && op.tier !== undefined) return op.tier;
  }
  return 'fast';
}

/**
 * Готує маску один раз на весь Job.
 *
 * Видалення фону, аутлайн і розумна обрізка спираються на ту саму маску.
 * Рахувати її тричі означало б утричі довше чекати найдорожчий крок
 * пайплайна — для isnet це 665 мс проти 2 с.
 */
async function buildMask(img: RasterImage, job: Job, ctx: Context): Promise<Mask> {
  if (ctx.segmenter === undefined) {
    throw new Error(
      'Операція потребує маски, але сегментатор не переданий у контекст. ' +
      'Додайте ctx.segmenter — без нього доступні лише геометричні операції.',
    );
  }
  const seg = ctx.segmenter(tierOf(job.ops));
  await seg.load();
  try {
    return await seg.segment(img);
  } finally {
    await seg.dispose();
  }
}

function applyOp(img: RasterImage, op: Op, mask: Mask | null): RasterImage {
  switch (op.type) {
    case 'fit': return fit(img, op);
    case 'crop': return crop(img, op.rect);
    case 'removeBackground': {
      const m = op.feather !== undefined && op.feather > 0
        ? featherMask(mask!, op.feather)
        : mask!;
      return applyMask(img, m);
    }
    case 'outline': return outline(img, mask!, op);
    case 'smartCrop': return smartCrop(img, mask!, op);
    default: {
      const unknown = op as { type: string };
      throw new Error(`Невідома операція: ${unknown.type}`);
    }
  }
}

/** Виконує Job: декодування → операції по черзі → кодування. */
export async function runJob(
  input: Uint8Array, mime: string, job: Job, ctx: Context,
): Promise<Uint8Array> {
  if (!ctx.codec.canDecode(mime)) {
    throw new Error(`Формат ${mime} не підтримується для читання`);
  }
  if (!ctx.codec.canEncode(job.output.format)) {
    throw new Error(`Формат ${job.output.format} не підтримується для запису`);
  }

  let img = await ctx.codec.decode(input, mime);
  const mask = job.ops.some((o) => NEEDS_MASK.has(o.type))
    ? await buildMask(img, job, ctx)
    : null;

  for (const op of job.ops) img = applyOp(img, op, mask);
  return ctx.codec.encode(img, job.output);
}
```

- [ ] **Крок 4: Запустити — має пройти**

```bash
pnpm vitest run packages/core/test/pipeline-m2.test.ts
```

Очікується: `6 passed`.

- [ ] **Крок 5: Коміт**

```bash
git add -A
git commit -m "feat(core): операції з маскою в пайплайні, маска рахується один раз"
```

---

### Task 9: MCP-інструменти

**Goal:** Агент уміє знімати фон, малювати обведення й кадрувати за суб'єктом.

**Files:**
- Modify: `apps/mcp/package.json`, `apps/mcp/src/tools.ts`, `apps/mcp/src/index.ts`
- Create: `apps/mcp/test/segment-tools.test.ts`

**Acceptance Criteria:**
- [ ] `remove_background` знімає фон і пише PNG із прозорістю
- [ ] `remove_background` приймає опційний аутлайн одним викликом
- [ ] `smart_crop` кадрує під співвідношення за суб'єктом
- [ ] Рівень моделі обирається параметром і описаний у схемі
- [ ] Результат повідомляє, який рівень використано і скільки часу зайняло

**Verify:** `pnpm vitest run apps/mcp/test/segment-tools.test.ts` → 5 passed

**Steps:**

- [ ] **Крок 1: Підключити сегментатор**

Додати в `apps/mcp/package.json`:

```json
"@obrobka/segmenter-node": "workspace:*"
```

У `apps/mcp/src/tools.ts` розширити контекст:

```ts
import { createSegmenter } from '@obrobka/segmenter-node';

const ctx = { codec: nodeCodec, segmenter: createSegmenter };
```

- [ ] **Крок 2: Додати інструменти**

Додати в `apps/mcp/src/tools.ts`:

```ts
import type { Op, Tier } from '@obrobka/core';

export interface RemoveBgArgs {
  readonly input: string;
  readonly output: string;
  readonly tier?: Tier | undefined;
  readonly feather?: number | undefined;
  readonly outlineWidth?: number | undefined;
  readonly outlineColor?: string | undefined;
  readonly format?: OutputFormat | undefined;
}

export async function removeBackground(args: RemoveBgArgs): Promise<ToolResult> {
  const ops: Op[] = [{
    type: 'removeBackground',
    tier: args.tier ?? 'fast',
    ...(args.feather === undefined ? {} : { feather: args.feather }),
  }];
  if (args.outlineWidth !== undefined && args.outlineWidth > 0) {
    ops.push({
      type: 'outline',
      width: args.outlineWidth,
      color: parseColor(args.outlineColor ?? '#ffffff'),
    });
  }
  // Формат мусить тримати альфу; jpeg її не має.
  const format = args.format ?? 'png';
  if (format === 'jpeg') {
    throw new Error('JPEG не підтримує прозорість — оберіть png, webp або avif');
  }
  return runAndReport(args.input, args.output, { ops, output: { format } });
}

export interface SmartCropArgs {
  readonly input: string;
  readonly output: string;
  readonly aspectRatio: number;
  readonly padding?: number | undefined;
  readonly tier?: Tier | undefined;
  readonly format: OutputFormat;
  readonly quality?: number | undefined;
}

export async function smartCropImage(args: SmartCropArgs): Promise<ToolResult> {
  // Лише smartCrop, без removeBackground: маска будується від самої
  // наявності операції, що її потребує, а кадр вирізається з оригіналу —
  // фон при цьому зберігається. Користувач просив обрізку, не видалення фону.
  return runAndReport(args.input, args.output, {
    ops: [{
      type: 'smartCrop',
      aspectRatio: args.aspectRatio,
      ...(args.padding === undefined ? {} : { padding: args.padding }),
    }],
    output: args.quality === undefined
      ? { format: args.format }
      : { format: args.format, quality: args.quality },
  });
}
```

**Звідки береться рівень моделі.** `tierOf` у пайплайні читає `tier` лише з
операції `removeBackground`. Коли її немає — як у `smart_crop` — повертається
`'fast'`. Це саме те, що потрібно: для пошуку прямокутника суб'єкта точність
країв не має значення, а 4,4 МБ проти 84 МБ — має.

Щоб параметр `tier` у `smart_crop` не був декоративним, `tierOf` треба
навчити читати його і з `smartCrop`. Додати в `packages/core/src/ops/pipeline.ts`:

```ts
function tierOf(ops: readonly Op[]): Tier {
  for (const op of ops) {
    if (op.type === 'removeBackground' && op.tier !== undefined) return op.tier;
    if (op.type === 'smartCrop' && op.tier !== undefined) return op.tier;
  }
  return 'fast';
}
```

І відповідно розширити `SmartCropOptions` у `types.ts`:

```ts
export interface SmartCropOptions {
  readonly aspectRatio: number;
  readonly padding?: number;
  readonly threshold?: number;
  /** Рівень моделі для пошуку суб'єкта. Типово fast. */
  readonly tier?: Tier;
}
```

- [ ] **Крок 3: Зареєструвати в сервері**

Додати в `apps/mcp/src/index.ts`:

```ts
const TIER = z.enum(['fast', 'portrait', 'quality']);

server.registerTool('remove_background', {
  description:
    'Видаляє фон, лишаючи прозорість. Опційно малює кольорове обведення навколо ' +
    'суб'єкта. Рівні моделі: fast — 4 МБ, будь-який сюжет; portrait — 6 МБ, ' +
    'лише люди, краще тримає волосся; quality — 84 МБ, чіткіші краї.',
  inputSchema: z.object({
    input: z.string().describe('Абсолютний шлях до вхідного файлу'),
    output: z.string().describe('Абсолютний шлях для запису результату'),
    tier: TIER.optional().describe('Рівень моделі. Типово fast.'),
    feather: z.number().min(0).max(20).optional()
      .describe('Пом'якшення краю маски в пікселях. Типово 0.'),
    outlineWidth: z.number().int().min(0).max(200).optional()
      .describe('Товщина обведення в пікселях. 0 або відсутнє — без обведення.'),
    outlineColor: z.string().optional()
      .describe('Колір обведення як #rrggbb або #rrggbbaa. Типово білий.'),
    format: z.enum(['png', 'webp', 'avif']).optional()
      .describe('Формат результату. JPEG недоступний — він не має альфа-каналу.'),
  }),
  outputSchema: RESULT,
}, async (args) => report(await removeBackground(args)));

server.registerTool('smart_crop', {
  description:
    'Кадрує зображення під задане співвідношення сторін, тримаючи суб'єкт у кадрі. ' +
    'Фон при цьому зберігається — знімається лише маска для пошуку суб'єкта.',
  inputSchema: z.object({
    input: z.string().describe('Абсолютний шлях до вхідного файлу'),
    output: z.string().describe('Абсолютний шлях для запису результату'),
    aspectRatio: z.number().positive().describe('Ширина поділена на висоту, напр. 1 або 1.7778'),
    padding: z.number().min(0).max(2).optional()
      .describe('Запас навколо суб'єкта як частка його розміру. Типово 0.08.'),
    tier: TIER.optional().describe('Рівень моделі для пошуку суб'єкта. Типово fast.'),
    format: FORMAT.describe('Формат результату'),
    quality: z.number().int().min(1).max(100).optional().describe('Якість 1..100'),
  }),
  outputSchema: RESULT,
}, async (args) => report(await smartCropImage(args)));
```

- [ ] **Крок 4: Написати тести**

`apps/mcp/test/segment-tools.test.ts`:

```ts
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { mkdtemp, rm, writeFile } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { nodeCodec } from '@obrobka/codecs/node';
import { removeBackground, smartCropImage } from '../src/tools.js';

let dir = '';
let src = '';

beforeAll(async () => {
  dir = await mkdtemp(join(tmpdir(), 'obrobka-seg-'));
  src = join(dir, 'circle.png');
  const size = 256;
  const data = new Uint8ClampedArray(size * size * 4);
  const r = size * 0.28;
  for (let y = 0; y < size; y++) {
    for (let x = 0; x < size; x++) {
      const dx = x - size / 2, dy = y - size / 2;
      const inside = dx * dx + dy * dy < r * r;
      const i = (y * size + x) * 4;
      data[i] = inside ? 242 : 20;
      data[i + 1] = inside ? 38 : 20;
      data[i + 2] = inside ? 38 : 22;
      data[i + 3] = 255;
    }
  }
  await writeFile(src, await nodeCodec.encode({ data, width: size, height: size }, { format: 'png' }));
}, 300_000);

afterAll(async () => { await rm(dir, { recursive: true, force: true }); });

describe('remove_background', () => {
  it('зберігає розмір і робить фон прозорим', async () => {
    const out = join(dir, 'nobg.png');
    const res = await removeBackground({ input: src, output: out });
    expect(res.width).toBe(256);
    const back = await nodeCodec.decode(
      new Uint8Array(await (await import('node:fs/promises')).readFile(out)), 'image/png');
    expect(back.data[3]).toBeLessThan(60);                       // кут прозорий
    const c = (128 * 256 + 128) * 4;
    expect(back.data[c + 3]).toBeGreaterThan(190);               // центр непрозорий
  }, 300_000);

  it('додає обведення й розширює полотно', async () => {
    const res = await removeBackground({
      input: src, output: join(dir, 'outlined.png'),
      outlineWidth: 6, outlineColor: '#0000ff',
    });
    expect(res.width).toBe(256 + 12);
  }, 300_000);

  it('відхиляє jpeg — він не має альфи', async () => {
    await expect(removeBackground({
      input: src, output: join(dir, 'bad.jpg'), format: 'jpeg' as never,
    })).rejects.toThrow(/прозор/);
  });
});

describe('smart_crop', () => {
  it('дає квадратний кадр', async () => {
    const res = await smartCropImage({
      input: src, output: join(dir, 'square.png'), aspectRatio: 1, format: 'png',
    });
    expect(res.width).toBe(res.height);
  }, 300_000);

  it('дає широкий кадр для 16:9', async () => {
    const res = await smartCropImage({
      input: src, output: join(dir, 'wide.png'), aspectRatio: 16 / 9, format: 'png',
    });
    expect(res.width / res.height).toBeCloseTo(16 / 9, 1);
  }, 300_000);
});
```

- [ ] **Крок 5: Запустити й перевірити сервер**

```bash
pnpm install
pnpm vitest run apps/mcp/test/segment-tools.test.ts
pnpm --filter obrobka-mcp exec tsc --build
printf '%s\n' '{"jsonrpc":"2.0","id":1,"method":"tools/list","params":{}}' \
  | node apps/mcp/dist/index.js | python3 -c "
import sys,json
for l in sys.stdin:
    m=json.loads(l)
    if m.get('id')==1: print([t['name'] for t in m['result']['tools']])
"
```

Очікується: `5 passed` і чотири інструменти в списку.

- [ ] **Крок 6: Коміт**

```bash
git add -A
git commit -m "feat(mcp): remove_background і smart_crop"
```

---

### Task 10: Інтерфейс

**Goal:** Видалення фону у віджеті — з вибором рівня, чесним прогресом і показом провайдера.

**Files:**
- Create: `apps/web/src/components/TierPicker.svelte`
- Modify: `apps/web/src/components/Widget.svelte`
- Modify: `apps/web/src/lib/worker.ts`, `apps/web/src/lib/worker-api.ts`
- Modify: `apps/web/package.json`
- Create: `apps/web/e2e/segment.spec.ts`

**Acceptance Criteria:**
- [ ] Перемикач «прибрати фон» вмикає операцію
- [ ] Вибір рівня показує вагу моделі **до** завантаження
- [ ] Прогрес завантаження моделі видно у відсотках
- [ ] Після першого завантаження модель береться з кешу без мережі
- [ ] Обраний провайдер виконання показано користувачу
- [ ] Аутлайн вмикається окремо, з товщиною й кольором
- [ ] Формат перемикається на PNG автоматично, якщо обрано JPEG із видаленням фону

**Verify:** `pnpm exec playwright test -g фон` → 4 passed

**Steps:**

- [ ] **Крок 1: Підключити пакет**

Додати в `apps/web/package.json`:

```json
"@obrobka/segmenter-web": "workspace:*"
```

Оновити `apps/web/src/lib/worker.ts`:

```ts
import * as Comlink from 'comlink';
import { runJob, type Job, type RasterImage } from '@obrobka/core';
import { browserCodec } from '@obrobka/codecs/browser';
import { createSegmenter, type Provider } from '@obrobka/segmenter-web';

let lastProvider: Provider | null = null;

/**
 * Сегментатори кешуються між викликами: створювати сесію ONNX заново
 * на кожну зміну повзунка означало б компілювати модель щоразу.
 */
const pool = new Map<string, ReturnType<typeof createSegmenter>>();

function segmenterFor(tier: 'fast' | 'portrait' | 'quality') {
  let s = pool.get(tier);
  if (s === undefined) { s = createSegmenter(tier); pool.set(tier, s); }
  return s;
}

const ctx = {
  codec: browserCodec,
  segmenter: (tier: 'fast' | 'portrait' | 'quality') => {
    const s = segmenterFor(tier);
    // Пайплайн викликає dispose після кожного Job, а нам треба тримати
    // сесію живою між викликами. Обгортку будуємо явно: спред екземпляра
    // класу (`{ ...s }`) скопіював би лише власні поля й загубив методи,
    // що живуть у прототипі.
    return {
      id: s.id,
      inputSize: s.inputSize,
      load: (onProgress?: (f: number) => void) => s.load(onProgress),
      segment: (img: RasterImage) => s.segment(img),
      dispose: async () => { lastProvider = s.provider; },
    };
  },
};

const api = {
  async process(bytes: ArrayBuffer, mime: string, job: Job): Promise<ArrayBuffer> {
    const result = await runJob(new Uint8Array(bytes), mime, job, ctx);
    const out = new Uint8Array(result.length);
    out.set(result);
    return Comlink.transfer(out.buffer, [out.buffer]);
  },

  /** Прогрів моделі з прогресом — щоб інтерфейс не мовчав під час завантаження. */
  async warmUp(tier: 'fast' | 'portrait' | 'quality', onProgress: (f: number) => void): Promise<Provider | null> {
    const s = segmenterFor(tier);
    await s.load(onProgress);
    lastProvider = s.provider;
    return s.provider;
  },

  provider(): Provider | null { return lastProvider; },
};

Comlink.expose(api);
```

Прогрес передається у воркер через `Comlink.proxy` — оновити `WorkerApi`
в `worker-api.ts`:

```ts
export interface WorkerApi {
  process(bytes: ArrayBuffer, mime: string, job: Job): Promise<ArrayBuffer>;
  warmUp(tier: Tier, onProgress: (fraction: number) => void): Promise<Provider | null>;
  provider(): Promise<Provider | null>;
}
```

Виклик із компонента:

```ts
await getWorker().warmUp(tier, Comlink.proxy((f) => { downloadProgress = f; }));
```

- [ ] **Крок 2: Компонент вибору рівня**

`apps/web/src/components/TierPicker.svelte`:

```svelte
<script lang="ts">
  import { MODELS, type Tier } from '@obrobka/models';

  let { value = $bindable<Tier>('fast'), progress = 0, provider = null, onchange }:
    { value: Tier; progress?: number; provider?: string | null; onchange?: () => void } = $props();

  function mb(bytes: number): string {
    return `${(bytes / 1_048_576).toFixed(1)} МБ`;
  }
</script>

<fieldset class="tiers">
  <legend>Модель</legend>
  <div class="row">
    {#each MODELS as m (m.id)}
      <button
        type="button"
        class="tier"
        class:on={value === m.id}
        aria-pressed={value === m.id}
        onclick={() => { value = m.id; onchange?.(); }}
      >
        <span class="label">{m.label}</span>
        <span class="scope">{m.scope}</span>
        <span class="size">{mb(m.bytes)}</span>
      </button>
    {/each}
  </div>

  {#if progress > 0 && progress < 1}
    <div class="bar" role="progressbar" aria-valuenow={Math.round(progress * 100)}
         aria-valuemin="0" aria-valuemax="100">
      <span style={`width: ${progress * 100}%`}></span>
    </div>
    <p class="note">Завантажую модель — {Math.round(progress * 100)} %</p>
  {:else if provider === 'wasm'}
    <p class="note warn">
      WebGPU недоступний — рахую на процесорі. Це помітно повільніше.
    </p>
  {:else if provider === 'webgpu'}
    <p class="note ok">Рахую на відеокарті</p>
  {/if}
</fieldset>

<style>
  .tiers { border: 0; padding: 0; margin: 0; min-width: 0; }
  legend {
    padding: 0; margin-bottom: 0.5rem;
    font-size: 0.78rem; font-weight: 600;
    letter-spacing: 0.04em; text-transform: uppercase;
    color: var(--fg-muted);
  }
  .row { display: grid; grid-template-columns: repeat(auto-fit, minmax(10rem, 1fr)); gap: 0.5rem; }

  .tier {
    display: grid; gap: 0.15rem; text-align: start;
    padding: 0.7rem 0.85rem;
    border: 1px solid var(--line);
    border-radius: var(--r-md);
    background: var(--bg-raised);
    cursor: pointer;
    transition: transform var(--dur-fast) var(--ease-spring),
                border-color var(--dur) var(--ease-out);
  }
  .tier:hover { transform: translateY(-2px); border-color: var(--line-strong); }
  .tier.on {
    border-color: var(--accent);
    background: color-mix(in oklab, var(--accent-bg) 12%, var(--bg-raised));
    box-shadow: 0 0 0 1px var(--accent), 0 6px 18px var(--accent-glow);
  }
  .label { font-weight: 600; font-size: 0.9rem; }
  .scope { font-size: 0.75rem; color: var(--fg-muted); }
  .size { font-family: var(--font-mono); font-size: 0.72rem; color: var(--fg-faint); }
  .tier.on .size { color: var(--accent); }

  .bar {
    margin-top: 0.6rem; height: 4px; border-radius: 999px;
    background: var(--bg-sunken); overflow: hidden;
  }
  .bar span {
    display: block; height: 100%;
    background: var(--accent-bg);
    transition: width var(--dur) var(--ease-out);
  }
  .note { margin-top: 0.4rem; font-size: 0.78rem; color: var(--fg-faint); }
  .note.warn { color: var(--gold-600); }
  .note.ok { color: var(--fg-muted); }
</style>
```

- [ ] **Крок 3: Розширити стан віджета**

У `WidgetState` (`worker-api.ts`) додати:

```ts
  readonly removeBg: boolean;
  readonly tier: Tier;
  readonly feather: number;
  readonly outlineOn: boolean;
  readonly outlineWidth: number;
  readonly outlineColor: string;
```

І в `buildJob` додати операції **перед** `fit` — фон знімається з оригіналу,
а вписування відбувається вже з готовою прозорістю:

```ts
export function buildJob(s: WidgetState): Job {
  const ops: Op[] = [];
  if (s.removeBg) {
    ops.push({ type: 'removeBackground', tier: s.tier, feather: s.feather });
    if (s.outlineOn && s.outlineWidth > 0) {
      ops.push({
        type: 'outline',
        width: s.outlineWidth,
        color: parseHexColor(s.outlineColor),
      });
    }
  }
  ops.push({
    type: 'fit',
    width: s.width, height: s.height, mode: s.mode,
    pad: s.padTransparent ? 'transparent' : parseHexColor(s.padColor),
    allowUpscale: s.allowUpscale,
  });
  return {
    ops,
    output: s.format === 'png' ? { format: 'png' } : { format: s.format, quality: s.quality },
  };
}
```

- [ ] **Крок 4: Додати керування у Widget.svelte**

Після перемикачів вставити:

```svelte
<label class="switch">
  <input type="checkbox" bind:checked={state.removeBg} onchange={onBgToggle} />
  <span class="track" aria-hidden="true"></span>
  Прибрати фон
</label>

{#if state.removeBg}
  <TierPicker
    bind:value={state.tier}
    progress={downloadProgress}
    provider={providerName}
    onchange={onTierChange}
  />

  <div class="controls">
    <label class="field">
      <span>Пом'якшити край <em>{state.feather}</em></span>
      <input type="range" min="0" max="8" bind:value={state.feather} onchange={process} />
    </label>
  </div>

  <div class="toggles">
    <label class="switch">
      <input type="checkbox" bind:checked={state.outlineOn} onchange={process} />
      <span class="track" aria-hidden="true"></span>
      Обведення
    </label>
    {#if state.outlineOn}
      <label class="field">
        <span>Товщина <em>{state.outlineWidth}</em></span>
        <input type="range" min="1" max="40" bind:value={state.outlineWidth} onchange={process} />
      </label>
      <label class="switch color">
        <input type="color" bind:value={state.outlineColor} onchange={process} />
        Колір
      </label>
    {/if}
  </div>
{/if}
```

І обробники в `<script>`:

```ts
  let downloadProgress = $state(0);
  let providerName = $state<string | null>(null);

  /**
   * JPEG не має альфа-каналу: увімкнувши видалення фону з ним, користувач
   * отримав би чорний фон замість прозорого. Перемикаємо мовчки на PNG —
   * це те, чого він хотів.
   */
  async function onBgToggle(): Promise<void> {
    if (state.removeBg && state.format === 'jpeg') state.format = 'png';
    if (state.removeBg) await warmUp();
    await process();
  }

  async function onTierChange(): Promise<void> {
    await warmUp();
    await process();
  }

  async function warmUp(): Promise<void> {
    downloadProgress = 0.001;
    try {
      providerName = await getWorker().warmUp(
        state.tier,
        Comlink.proxy((f: number) => { downloadProgress = f; }),
      );
    } finally {
      downloadProgress = 0;
    }
  }
```

- [ ] **Крок 5: Написати e2e**

`apps/web/e2e/segment.spec.ts`:

```ts
import { test, expect, type Page } from '@playwright/test';
import { join } from 'node:path';

const FIXTURE = join(import.meta.dirname, 'circle.png');

async function ready(page: Page): Promise<void> {
  await page.goto('/');
  await expect(page.locator('input[type=file][data-ready="true"]')).toBeAttached({ timeout: 60_000 });
  await page.setInputFiles('input[type=file]', FIXTURE);
  await expect(page.getByTestId('result')).toBeVisible({ timeout: 60_000 });
}

test('фон знімається й результат лишається того самого розміру', async ({ page }) => {
  await ready(page);
  await page.getByLabel('Прибрати фон').check();
  const img = page.getByTestId('result');
  await expect.poll(
    () => img.evaluate((el) => (el as HTMLImageElement).naturalWidth),
    { timeout: 120_000 },
  ).toBe(512);
});

test('фон показує рівень моделі з вагою', async ({ page }) => {
  await ready(page);
  await page.getByLabel('Прибрати фон').check();
  await expect(page.getByRole('button', { name: /Швидко/ })).toBeVisible();
  await expect(page.getByText('4.4 МБ')).toBeVisible();
});

test('фон із jpeg перемикає формат на png', async ({ page }) => {
  await ready(page);
  await page.getByLabel('Формат').selectOption('jpeg');
  await page.getByLabel('Прибрати фон').check();
  await expect(page.getByLabel('Формат')).toHaveValue('png');
});

test('фон повідомляє провайдер виконання', async ({ page }) => {
  await ready(page);
  await page.getByLabel('Прибрати фон').check();
  await expect(page.locator('.note')).toContainText(/відеокарті|процесорі/, { timeout: 120_000 });
});
```

Фікстуру створити тим самим кодеком:

```bash
node -e "
const {nodeCodec}=await import('./packages/codecs/dist/node.js');
const {writeFile}=await import('node:fs/promises');
const size=256, d=new Uint8ClampedArray(size*size*4), r=size*0.28;
for(let y=0;y<size;y++)for(let x=0;x<size;x++){
  const dx=x-size/2, dy=y-size/2, inside=dx*dx+dy*dy<r*r, i=(y*size+x)*4;
  d[i]=inside?242:20; d[i+1]=inside?38:20; d[i+2]=inside?38:22; d[i+3]=255;
}
await writeFile('apps/web/e2e/circle.png', await nodeCodec.encode({data:d,width:size,height:size},{format:'png'}));
console.log('фікстура готова');
" --input-type=module
```

- [ ] **Крок 6: Запустити**

```bash
pnpm install
pnpm --filter @obrobka/web build
pnpm exec playwright test -g фон
```

Очікується: `4 passed`.

- [ ] **Крок 7: Коміт**

```bash
git add -A
git commit -m "feat(web): видалення фону з вибором рівня, прогресом і аутлайном"
```

---

### Task 11: Лендінги під нові задачі

**Goal:** Чотири сторінки під запити, що з'явились разом із видаленням фону.

**Files:**
- Create: `apps/web/src/data/tools/remove-background.{uk,en}.yaml`
- Create: `apps/web/src/data/tools/smart-crop.{uk,en}.yaml`
- Modify: `apps/web/src/content.config.ts`, `apps/web/src/layouts/Tool.astro`
- Modify: `apps/web/test/seo.test.ts`

**Acceptance Criteria:**
- [ ] Схема колекції приймає операції видалення фону в пресеті
- [ ] Пресет доїжджає у віджет і вмикає потрібні перемикачі
- [ ] Чотири нові сторінки збираються зі структурованою розміткою
- [ ] Українська й англійська версії посилаються одна на одну

**Verify:** `pnpm --filter @obrobka/web build && pnpm vitest run apps/web/test/seo.test.ts` → 9 passed

**Steps:**

- [ ] **Крок 1: Розширити схему пресета**

У `apps/web/src/content.config.ts`:

```ts
    preset: z.object({
      width: z.number().int().positive(),
      height: z.number().int().positive(),
      mode: z.enum(['contain', 'cover', 'fill', 'inside', 'outside']),
      format: z.enum(['png', 'jpeg', 'webp', 'avif']),
      padTransparent: z.boolean().default(true),
      removeBg: z.boolean().default(false),
      tier: z.enum(['fast', 'portrait', 'quality']).default('fast'),
      outlineOn: z.boolean().default(false),
      outlineWidth: z.number().int().min(0).default(0),
    }),
```

- [ ] **Крок 2: Створити сторінки**

`apps/web/src/data/tools/remove-background.uk.yaml`:

```yaml
slug: видалити-фон
locale: uk
pair: remove-background.en
title: >-
  Видалити фон із фото онлайн — безкоштовно, без завантаження на сервер
description: >-
  Приберіть фон із зображення просто в браузері. Три моделі на вибір: швидка
  для будь-яких сюжетів, портретна для людей і точна для складних країв.
h1: Видалення фону із зображення
intro: >-
  Обробка виконується у вашій вкладці — фото не надсилається на жоден сервер.
  Результат зберігається у PNG із прозорим фоном.
preset:
  width: 1024
  height: 1024
  mode: inside
  format: png
  padTransparent: true
  removeBg: true
  tier: fast
steps:
  - Оберіть зображення або перетягніть його у вікно
  - Увімкніть «Прибрати фон» — модель завантажиться один раз і залишиться в кеші
  - За потреби змініть рівень моделі й завантажте PNG
faq:
  - q: Скільки важить модель?
    a: >-
      Швидка — 4,4 МБ, портретна — 6,3 МБ, точна — 84 МБ. Перша качається
      один раз і далі береться з кеша браузера.
  - q: Чому портретна модель не працює на предметах?
    a: >-
      MODNet навчений виключно на людях. На товарі чи логотипі він поверне
      порожню маску — для таких сюжетів беріть швидку або точну модель.
  - q: Чи можна додати обведення?
    a: >-
      Так, увімкніть «Обведення» й задайте товщину та колір. Полотно
      розшириться, щоб обведення не обрізалось по краю.
```

`apps/web/src/data/tools/smart-crop.uk.yaml`:

```yaml
slug: розумна-обрізка
locale: uk
pair: smart-crop.en
title: >-
  Розумна обрізка фото онлайн — кадр за суб'єктом, а не за центром
description: >-
  Обрізає зображення під потрібне співвідношення сторін так, щоб головний
  об'єкт лишився в кадрі. Працює в браузері, без завантаження на сервер.
h1: Обрізка за суб'єктом
intro: >-
  Звичайна обрізка ріже по центру й часто відтинає голову чи край товару.
  Тут кадр будується навколо того, що модель визначила як головний об'єкт.
preset:
  width: 1080
  height: 1080
  mode: cover
  format: png
  padTransparent: true
  removeBg: false
  tier: fast
steps:
  - Оберіть зображення
  - Задайте потрібне співвідношення сторін
  - Завантажте кадр, побудований навколо суб'єкта
faq:
  - q: Чи видаляється фон?
    a: >-
      Ні. Модель використовується лише щоб знайти суб'єкт, а кадр вирізається
      з оригіналу разом із фоном.
  - q: Що буде, якщо об'єктів кілька?
    a: >-
      Кадр охопить їх усі: береться прямокутник, що вміщує всю знайдену маску.
```

`apps/web/src/data/tools/remove-background.en.yaml`:

```yaml
slug: remove-background
locale: en
pair: remove-background.uk
title: >-
  Remove image background online — free, nothing is uploaded
description: >-
  Strip the background from an image right in your browser. Three models to
  pick from: fast for any subject, portrait for people, precise for hard edges.
h1: Background removal
intro: >-
  Processing happens in your tab — the photo is never sent to any server.
  The result is saved as a PNG with a transparent background.
preset:
  width: 1024
  height: 1024
  mode: inside
  format: png
  padTransparent: true
  removeBg: true
  tier: fast
steps:
  - Pick an image or drag it into the window
  - Turn on "Remove background" — the model downloads once and stays cached
  - Switch the model tier if you need to, then download the PNG
faq:
  - q: How large is the model?
    a: >-
      Fast is 4.4 MB, portrait is 6.3 MB, precise is 84 MB. The first one
      downloads once and is then served from your browser cache.
  - q: Why does the portrait model fail on objects?
    a: >-
      MODNet was trained on people only. On a product or a logo it returns an
      empty mask — use the fast or precise model for those subjects.
  - q: Can I add an outline?
    a: >-
      Yes, turn on "Outline" and set width and colour. The canvas grows so the
      outline is not clipped at the edge.
```

`apps/web/src/data/tools/smart-crop.en.yaml`:

```yaml
slug: smart-crop
locale: en
pair: smart-crop.uk
title: >-
  Smart image cropping online — framed on the subject, not the centre
description: >-
  Crops an image to the aspect ratio you need while keeping the main subject
  in frame. Runs in your browser, nothing is uploaded.
h1: Subject-aware cropping
intro: >-
  Plain cropping cuts through the centre and often clips a head or the edge of
  a product. Here the frame is built around whatever the model identifies as
  the main subject.
preset:
  width: 1080
  height: 1080
  mode: cover
  format: png
  padTransparent: true
  removeBg: false
  tier: fast
steps:
  - Pick an image
  - Set the aspect ratio you need
  - Download the frame built around the subject
faq:
  - q: Is the background removed?
    a: >-
      No. The model is only used to locate the subject; the crop is taken from
      the original, background included.
  - q: What happens with several objects?
    a: >-
      The frame covers all of them: the rectangle enclosing the whole detected
      mask is used.
```

- [ ] **Крок 3: Оновити тести SEO**

Додати до `apps/web/test/seo.test.ts` перевірку нових сторінок:

```ts
it('сторінка видалення фону згенерована', async () => {
  const html = await readFile(join(dist, 'видалити-фон', 'index.html'), 'utf8');
  expect(html).toContain('Видалення фону');
  expect(html).toContain('hreflang="en"');
});

it('сторінка розумної обрізки згенерована', async () => {
  const html = await readFile(join(dist, 'розумна-обрізка', 'index.html'), 'utf8');
  expect(html).toContain('Обрізка за суб');
});

it('пресет видалення фону доїхав у віджет', async () => {
  const html = await readFile(join(dist, 'видалити-фон', 'index.html'), 'utf8');
  expect(html).toMatch(/removeBg&quot;:\[\d+,true\]/);
});
```

- [ ] **Крок 4: Зібрати, перевірити, закомітити**

```bash
pnpm --filter @obrobka/web build
pnpm vitest run apps/web/test/seo.test.ts
git add -A
git commit -m "feat(web): лендінги видалення фону й розумної обрізки"
```

---

### Task 12: Деплой

**Goal:** M2 живе на продакшні й перевірено на живому сайті.

**Files:**
- Modify: `docs/deploy.md`

**Acceptance Criteria:**
- [ ] Три моделі доступні з R2 по HTTPS
- [ ] Видалення фону працює на живому сайті
- [ ] Модель кешується — друге завантаження сторінки не качає її знову
- [ ] Сторінка лишається `crossOriginIsolated`
- [ ] Усі десять сторінок віддають 200

**Verify:** `pnpm exec playwright test --config playwright.prod.config.ts` → усі passed

**Steps:**

- [ ] **Крок 1: Викотити**

```bash
set -a; . ~/.config/cloudflare/env; set +a
pnpm --filter @obrobka/web build
pnpm exec wrangler pages deploy apps/web/dist \
  --project-name obrobka --branch main --commit-dirty=true
```

- [ ] **Крок 2: Перевірити моделі й сторінки**

```bash
for f in u2netp.onnx modnet.onnx isnet-general.onnx; do
  curl -s -o /dev/null -w "  %{http_code}  %{size_download} байт  $f\n" \
    "https://models.obrobka.dobrovolskyi.com.ua/$f"
done

for p in "/" "/en/" "/видалити-фон/" "/en/remove-background/" \
         "/розумна-обрізка/" "/en/smart-crop/" "/стікер-для-телеграма/" \
         "/en/telegram-sticker/" "/змінити-розмір-зображення/" "/en/resize-image/"; do
  curl -s -o /dev/null -w "  %{http_code}  $p\n" "https://obrobka.dobrovolskyi.com.ua$p"
done
```

- [ ] **Крок 3: Прогнати e2e проти живого**

```bash
pnpm exec playwright test --config playwright.prod.config.ts --reporter=list
```

- [ ] **Крок 4: Перевірити кешування моделі вручну**

Відкрити сайт, увімкнути «Прибрати фон», дочекатись результату.
Перезавантажити сторінку, увімкнути ще раз — у вкладці «Мережа» запиту
до `models.obrobka.dobrovolskyi.com.ua` бути не повинно.

- [ ] **Крок 5: Оновити docs/deploy.md і закомітити**

```bash
git add -A
git commit -m "docs: R2 і публікація моделей у процедурі деплою"
```

---

## Що лишається за межами M2

| Етап | Обсяг | Передумова |
|---|---|---|
| M3 | Батч, ZIP, HEIC, EXIF | пул воркерів |
| M4 | Апскейл на Swin2SR, тайлова обробка, WebGPU-профілювання | M2 покаже реальну поведінку ONNX у воркері |
| M5 | Повна SEO-матриця ~130 сторінок, llms.txt | шаблон із M1 |

**BiRefNet_lite** можна переглянути в M4: якщо шлях через WebGPU покаже,
що ваги лягають у пам'ять відеокарти й не чіпають купу WASM, модель стане
придатною. Поки що вимірювання каже однозначно — на CPU вона не влазить
у 4 ГБ.

---

## Ризики

| Ризик | Імовірність | Пом'якшення |
|---|---|---|
| isnet 84 МБ відлякує користувачів | висока | типовий рівень — 4,4 МБ; вага показана до завантаження |
| WebGPU недоступний, isnet рахує 5+ с | середня | чесне повідомлення про провайдер; типовий рівень швидкий |
| Пам'ять на слабких телефонах | висока | isnet бере 472 МБ на CPU; для мобільних лишити fast і portrait |
| CORP на R2 не застосується | середня | перевірка в Task 5 крок 6 блокує подальші кроки |
| Маска моделі груба на волоссі | середня | feather і рівень portrait; повна відповідь — у M4 |
| Синтетичний круг ≠ реальні фото | висока | тести доводять працездатність, не якість; перед релізом прогнати на десятку реальних знімків вручну |
